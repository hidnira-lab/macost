---
quick_id: 260705-0mm
description: Tetapkan aturan Hidayat pegang penuh akun Vercel/Railway/Supabase untuk Phase 2/3/4, dan perinci docs/PANDUAN_TEKNIKAL_TIM.md
date: 2026-07-05
---

## Task 1: Lock the platform-ownership rule into CLAUDE.md and PROJECT.md

<files>
- CLAUDE.md
- .claude/CLAUDE.md
- .planning/PROJECT.md
</files>

<action>
Hidayat is the sole account holder for three external platforms: Vercel (frontend deploy), Railway (backend deploy), and Supabase (database + auth) — confirmed by STATE.md history (Supabase project creation, Railway domain generation, Vercel env var updates were all Hidayat-only actions in Phase 1/01.1). Starting Phase 2, 3, and 4, any task/worktree requiring a new env var or a dashboard-setting change on one of these three platforms must be scoped as a separate, Hidayat-only task, kept out of regular dev tasks — so Fertika/Khayyira/Zarra never block waiting on Hidayat; they use a placeholder/mock value first and wire up the real value after Hidayat finishes the platform-side setup. This rule must be captured in the files that `/gsd-discuss-phase` and `/gsd-plan-phase` read for Phase 2/3/4, so future planning runs honor it automatically.

Add this exact bullet to two places (use `Edit`, not `Write`, on both — do not touch surrounding content):

1. `.planning/PROJECT.md` — inside the `### Constraints` list (the canonical source), immediately after the existing `**MVP target (final, 2026-07-04):**` bullet, add:
   `- **Platform ownership (Vercel/Railway/Supabase, Phase 2/3/4):** Hidayat adalah pemegang akun tunggal untuk tiga platform eksternal ini. Task yang butuh env var baru atau ubah dashboard setting di salah satunya wajib di-scope terpisah khusus Hidayat — tidak boleh memblokir Fertika/Khayyira/Zarra; mereka pakai placeholder/mock dulu, baru wiring setelah Hidayat selesai setup. Alur detail: \`docs/PANDUAN_TEKNIKAL_TIM.md\` Section 2a.`

2. `.claude/CLAUDE.md` — this file is auto-assembled from PROJECT.md's Constraints (marked by the `<!-- GSD:project-start source:PROJECT.md -->` / `<!-- GSD:project-end -->` comment pair). Add the identical bullet (word-for-word the same as PROJECT.md's, for consistency) inside that block's `### Constraints` list, in the same position (right after the `**MVP target (final, 2026-07-04):**` bullet, before the `<!-- GSD:project-end -->` marker).

Then add a short one-line pointer (not the full bullet prose) to `CLAUDE.md` (root), because it already has its own compact `## Pembagian Kerja (Parallel Development)` table and per the "prefer one canonical detailed location + short cross-references elsewhere" rule, it should not duplicate the full Constraints bullet. Add this new paragraph immediately after the existing "Setiap orang kerja di branch sendiri (...)" paragraph that follows the Pembagian Kerja table, and before the `## Sumber Desain UI` heading:

`**Platform ownership (Phase 2/3/4):** Hidayat adalah pemegang akun tunggal Vercel, Railway, dan Supabase — task yang butuh env var baru atau ubah dashboard setting di tiga platform itu wajib di-scope terpisah khusus Hidayat, tidak boleh memblokir Fertika/Khayyira/Zarra. Alur detail (termasuk cara pakai placeholder/mock sementara): \`docs/PANDUAN_TEKNIKAL_TIM.md\` Section 2a.`

Finally, in `.planning/PROJECT.md`'s `## Context` section, under the existing `**Tim & pembagian kerja:**` bullet list (after the "Zarra → ..." line, before the "Setiap orang kerja di branch sendiri (...)" paragraph that follows it), add one more bullet:

`- Hidayat juga pemegang akun tunggal untuk Vercel, Railway, dan Supabase — lihat Constraints untuk aturan scoping task platform-only Phase 2/3/4.`

Note: Task 2 (below) creates the "Section 2a" heading referenced by these pointers in `docs/PANDUAN_TEKNIKAL_TIM.md` — both tasks run in this same plan, so the cross-reference resolves correctly once the plan finishes.
</action>

<verify>
grep -l "Platform ownership" CLAUDE.md .claude/CLAUDE.md .planning/PROJECT.md — must list all three files. Also: grep "pemegang akun tunggal" .planning/PROJECT.md returns at least 2 matches (Constraints bullet + Context bullet).
</verify>

<done>
`.planning/PROJECT.md` (Constraints + Context), `.claude/CLAUDE.md` (Constraints), and root `CLAUDE.md` (short pointer under Pembagian Kerja) all state that Hidayat is the sole owner of Vercel/Railway/Supabase and that Phase 2/3/4 platform-config tasks (new env vars, dashboard changes) must be scoped separately to Hidayat so other developers are never blocked. Future `/gsd-discuss-phase` and `/gsd-plan-phase` runs for Phase 2/3/4 will read this constraint automatically since it lives in the standard project-context files.
</done>

## Task 2: Add detailed platform-ownership workflow section to docs/PANDUAN_TEKNIKAL_TIM.md

<files>
- docs/PANDUAN_TEKNIKAL_TIM.md
</files>

<action>
Insert a new subsection titled `### 2a. Platform Ownership — Vercel, Railway, Supabase (Hidayat-only, berlaku Phase 2/3/4)` immediately after the end of the existing `## 2. Pembagian Kerja per Orang (Phase 2: Core Product Loop)` section's content (right before the `---` separator that precedes `## 3. Setup Awal Tiap Orang`) — mirroring the doc's existing convention of letter-suffixed addenda subsections (it already has `### 1a. ...` after Section 1).

The new section must cover, in Bahasa Indonesia matching the doc's direct/copy-paste style, all of the following:

1. **The rule itself:** Hidayat is the sole account holder for Vercel, Railway, and Supabase. Starting Phase 2/3/4, any task/worktree that needs a new env var or a dashboard-setting change on one of these three platforms must be scoped as a separate task assigned only to Hidayat — never mixed into Fertika/Khayyira/Zarra's regular dev tasks. Explain why: only Hidayat has dashboard access (cite the Section 1 history — Supabase project creation, Railway domain generation, Vercel env var fix — as concrete precedent of this pattern already happening in Phase 1/01.1).

2. **A concrete numbered step-by-step workflow** for a teammate who discovers mid-task that they need a new env var or dashboard setting:
   - Step 1: Do not stop working — proceed with steps 2 and 3 in parallel rather than waiting.
   - Step 2: Use a placeholder/mock in the meantime — for a missing env var, add a dummy value to your local `.env`/`.env.local` (never commit it) or add a safe fallback/stub in code so it doesn't crash (referencing the existing `USE_MOCK` pattern already used in `apps/web/lib/api/client.ts` as the template for this kind of stubbing); for a dashboard change (e.g., a new Supabase table/column needing a migration on the live project), write the migration SQL as a file ready to hand off, but do not run it against the live Supabase project yourself.
   - Step 3: Report the need to Hidayat via the team channel with exactly four things: (a) the precise env var name or dashboard setting needed, (b) which platform (Vercel/Railway/Supabase), (c) which FR/feature needs it, (d) what placeholder/mock is being used meanwhile so Hidayat knows the requester isn't blocked.
   - Step 4: Keep developing against the placeholder — do not wait for a reply before continuing.
   - Step 5: Hidayat batches platform-only requests into separate, dedicated tasks per phase checkpoint (not mixed into any feature PR).
   - Step 6: Hidayat signals completion back in the team channel — for env vars, the real value is added to the Notion API Keys workspace page referenced in Section 3; for dashboard changes, Hidayat states the concrete outcome (e.g., "column X added to table Y, migration applied on live Supabase").
   - Step 7: The teammate swaps the placeholder for the real value/config, re-tests end-to-end, removes any temporary stub code, then proceeds to `/gsd-verify-work` as normal.

3. A short closing note citing the Phase 1/01.1 precedent (Supabase project + `001_create_dompet.sql` migration, Railway domain generation, Vercel `NEXT_PUBLIC_API_BASE_URL` fix) as real examples of exactly this pattern already having happened, done by Hidayat separately from feature code while the rest of the team kept working unblocked.

Keep formatting consistent with the rest of the document (numbered/bulleted lists, bold key terms, no new top-level `##` heading — use `###` to match the `1a.` addendum pattern).
</action>

<verify>
grep "2a. Platform Ownership" docs/PANDUAN_TEKNIKAL_TIM.md returns exactly 1 match. grep -c "placeholder" docs/PANDUAN_TEKNIKAL_TIM.md is greater than the pre-edit count (confirms the workflow section was actually added, not just the heading).
</verify>

<done>
`docs/PANDUAN_TEKNIKAL_TIM.md` has a new Section 2a explaining the Hidayat-only platform-ownership rule and a concrete numbered workflow a teammate follows when they discover they need a new env var or dashboard setting on Vercel/Railway/Supabase — including the placeholder/mock approach, who to notify with what information, and how Hidayat signals back when the real value/config is ready.
</done>
