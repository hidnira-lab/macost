---
phase: quick-260704-jd2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/native/src-tauri/tauri.conf.json
  - apps/web/.env.example
autonomous: true
requirements: [OPEN-TAURI-CSP-RAILWAY]
must_haves:
  truths:
    - "Tauri desktop webview's CSP allowlists the live Railway backend host, so requests to https://macost-production.up.railway.app are not blocked by connect-src"
    - "The dead Render host (https://macost-api.onrender.com) no longer appears anywhere in the CSP string"
    - "apps/web/.env.example documents that Tauri desktop builds require NEXT_PUBLIC_API_BASE_URL=https://macost-production.up.railway.app at build time, distinct from local dev's localhost:8000"
  artifacts:
    - apps/native/src-tauri/tauri.conf.json
    - apps/web/.env.example
  key_links:
    - "tauri.conf.json app.security.csp connect-src -> https://macost-production.up.railway.app (matches live Railway backend documented in .planning/STATE.md and CLAUDE.md)"
---

<objective>
Fix the stale Render backend URL baked into the Tauri desktop app's Content-Security-Policy, and document the Tauri-build-time env var requirement in `apps/web/.env.example`.

Purpose: The project pivoted from Render to Railway on 2026-07-03 (see `.planning/STATE.md` Decisions log, entry `[01.1-03]`). The Render host `https://macost-api.onrender.com` is confirmed dead (404 on direct curl). Two config files still reference or fail to clarify this: `apps/native/src-tauri/tauri.conf.json`'s CSP whitelists only the dead Render URL for `connect-src`/`default-src` (blocking any request the Tauri desktop webview makes to the real Railway backend), and `apps/web/.env.example` doesn't warn that Tauri's `beforeBuildCommand` bakes in whatever `NEXT_PUBLIC_API_BASE_URL` is present in the build-time environment (Next.js `NEXT_PUBLIC_*` vars are inlined at build, not runtime).

Output: `tauri.conf.json` CSP updated to whitelist the live Railway host instead of the dead Render host; `.env.example` gains a documentation comment clarifying the Tauri desktop build requirement. No application code changes.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Relevant STATE.md excerpts already absorbed into this plan:
- Decision `[01.1-03]`: "Backend deploy target switched from Render to Railway ... Live at `https://macost-production.up.railway.app`. render.yaml removed."
- Open blocker (Tauri desktop .exe): "`apps/native/src-tauri/tauri.conf.json`'s `security.csp` `connect-src` whitelist still only allows `https://macost-api.onrender.com` (the dead Render URL) + Supabase + `localhost:8000` — it does not include `https://macost-production.up.railway.app`, so the desktop webview will CSP-block any request to the real backend even after rebuilding."

Current file states (read prior to planning):

`apps/native/src-tauri/tauri.conf.json` line 22 (`app.security.csp`):
`default-src 'self' ipc: asset: https://macost-api.onrender.com; connect-src ipc: asset: https://macost-api.onrender.com https://*.supabase.co http://localhost:8000; style-src 'self' 'unsafe-inline'; img-src 'self' data: asset: blob:`

`apps/web/.env.example` (2 lines, no comments):
```
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
NEXT_PUBLIC_USE_MOCK=false
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dead Render URL with live Railway URL in Tauri CSP</name>
  <files>apps/native/src-tauri/tauri.conf.json</files>
  <action>
Open `apps/native/src-tauri/tauri.conf.json` and edit the `app.security.csp` string value (currently on line 22). Replace every occurrence of the substring `https://macost-api.onrender.com` with `https://macost-production.up.railway.app` — this occurs twice: once in `default-src` and once in `connect-src`. Do not add the Railway URL alongside the Render one; fully replace each occurrence (Render is confirmed dead via direct curl returning 404, no reason to keep it whitelisted).

Leave every other token in the CSP string byte-for-byte unchanged: `'self'`, `ipc:`, `asset:`, `https://*.supabase.co`, `http://localhost:8000` (needed for local dev against the FastAPI dev server), `style-src 'self' 'unsafe-inline'`, `img-src 'self' data: asset: blob:`. Do not reformat the JSON, do not change indentation, do not touch any other key in the file (`productName`, `build.*`, `app.windows`, `bundle.android`).

After the edit, the `csp` value must read exactly:
`default-src 'self' ipc: asset: https://macost-production.up.railway.app; connect-src ipc: asset: https://macost-production.up.railway.app https://*.supabase.co http://localhost:8000; style-src 'self' 'unsafe-inline'; img-src 'self' data: asset: blob:`
  </action>
  <verify>
    <automated>node -e "const c=require('D:/COLLEGE/UII S4/ZEPHYRA/CODE/macost/apps/native/src-tauri/tauri.conf.json'); const csp=c.app.security.csp; if (csp.includes('onrender.com')) { console.error('FAIL: Render URL still present'); process.exit(1);} if (!csp.includes('macost-production.up.railway.app')) { console.error('FAIL: Railway URL missing'); process.exit(1);} if ((csp.match(/macost-production\.up\.railway\.app/g)||[]).length !== 2) { console.error('FAIL: expected exactly 2 occurrences (default-src + connect-src)'); process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <done>`app.security.csp` in `apps/native/src-tauri/tauri.conf.json` contains `https://macost-production.up.railway.app` exactly twice (default-src and connect-src) and contains zero occurrences of `onrender.com`; all other CSP tokens and JSON structure unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Document Tauri production build env var requirement in .env.example</name>
  <files>apps/web/.env.example</files>
  <action>
Open `apps/web/.env.example` (currently 2 lines: `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000` and `NEXT_PUBLIC_USE_MOCK=false`). Add a comment block directly above the `NEXT_PUBLIC_API_BASE_URL` line explaining the dual meaning of this variable depending on build target.

The comment must communicate, in plain prose comment lines (`#`-prefixed), the following facts:
- `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000` is correct for local web dev (`npm run dev` against the local FastAPI server).
- Building the Tauri desktop app for real use is different: `apps/native/src-tauri/tauri.conf.json`'s `beforeBuildCommand` runs `cd ../web && npm run build`, and Next.js inlines `NEXT_PUBLIC_*` variables at build time (not runtime) — so whatever value is present in the environment/`.env.local` at that moment gets permanently baked into the built desktop app.
- For a Tauri desktop build intended for real use, `NEXT_PUBLIC_API_BASE_URL` must be set to `https://macost-production.up.railway.app` (the live Railway backend) before running the Tauri build, otherwise the packaged app will keep calling `localhost:8000`, which does not exist on an end user's machine.

Do not modify or touch `apps/web/.env.local` — it is gitignored, per-developer, and out of scope. Do not change the actual default value on the `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000` line (it stays correct for local dev) or the `NEXT_PUBLIC_USE_MOCK=false` line — only add the explanatory comment above them.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('D:/COLLEGE/UII S4/ZEPHYRA/CODE/macost/apps/web/.env.example','utf8'); const hasComment=/^#/m.test(t); const hasRailway=t.includes('macost-production.up.railway.app'); const hasLocalhost=t.includes('NEXT_PUBLIC_API_BASE_URL=http://localhost:8000'); const hasMock=t.includes('NEXT_PUBLIC_USE_MOCK=false'); if(!hasComment||!hasRailway||!hasLocalhost||!hasMock){console.error('FAIL', {hasComment,hasRailway,hasLocalhost,hasMock}); process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <done>`apps/web/.env.example` retains both original variable lines unchanged, plus a new comment block (above `NEXT_PUBLIC_API_BASE_URL`) explaining that Tauri desktop production builds must set this to `https://macost-production.up.railway.app` at build time due to Next.js `NEXT_PUBLIC_*` inlining, distinct from the `localhost:8000` default used for local dev.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri desktop webview -> backend API | CSP `connect-src` governs which hosts the packaged desktop app's webview may issue XHR/fetch requests to |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick260704jd2-01 | Tampering | `apps/native/src-tauri/tauri.conf.json` CSP | low | accept | Config-only edit restricted to swapping one already-public hostname for another already-public hostname; no secrets, no new permissions, no widened `connect-src` scope (still limited to Railway backend, Supabase, and localhost dev) |
| T-quick260704jd2-02 | Information Disclosure | `apps/web/.env.example` | low | accept | `.env.example` is a template file with no real secrets (documented convention across the repo); added comment is documentation-only, does not introduce credentials |
</threat_model>

<verification>
1. Run the Task 1 automated verify command — confirms zero `onrender.com` occurrences and exactly two `macost-production.up.railway.app` occurrences in the CSP string.
2. Run the Task 2 automated verify command — confirms the explanatory comment and Railway URL reference exist in `.env.example` while both original lines remain intact.
3. Manual spot-check: `apps/web/.env.local` is untouched (`git status` shows no change to that gitignored file, since it should not even be tracked).
</verification>

<success_criteria>
- `apps/native/src-tauri/tauri.conf.json`'s CSP whitelists `https://macost-production.up.railway.app` in both `default-src` and `connect-src`, with zero remaining references to `https://macost-api.onrender.com`.
- All other CSP directives (`ipc:`, `asset:`, `https://*.supabase.co`, `http://localhost:8000`, `style-src`, `img-src`) are unchanged.
- `apps/web/.env.example` documents the Tauri build-time env var requirement without altering its existing default values.
- `apps/web/.env.local` is untouched.
</success_criteria>

<output>
Create `.planning/quick/260704-jd2-fix-tauri-csp-dan-env-example-untuk-back/260704-jd2-SUMMARY.md` when done
</output>