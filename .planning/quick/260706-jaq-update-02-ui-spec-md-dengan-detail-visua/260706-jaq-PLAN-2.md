---
phase: 02-core-product-loop
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-core-product-loop/02-UI-SPEC.md
autonomous: true
requirements: [TRAN-01, DASH-01, DASH-02, GOAL-01, GOAL-02, GOAL-03, GOAL-04, GOAL-05, SAW-03, ALLOC-01, ALLOC-02, ALLOC-03, ALLOC-04, ALLOC-05]

must_haves:
  truths:
    - "A developer reading 02-UI-SPEC.md sees the 4 previously-open Figma-vs-spec conflicts (Dashboard KPI order, SAW default weights, Pending Suggestions scope, Transaction History nav tab) recorded as explicitly RESOLVED decisions dated 2026-07-06 Round 2, each citing the user-directed resolution — not still framed as open questions."
    - "Open-question item #3 from round 1 (Create First Goal full onboarding page vs. D-06's lightweight empty state) still reads as an open, unresolved item, unchanged in substance — not addressed this round."
    - "A developer sees full per-page visual-layout detail (not thin/placeholder text) for Transaction History (156:1526), Goal Detail (156:558), Create/Edit Goal Form (156:713), and a superseded full-structure description for the Smart Allocation Modal (156:653)."
    - "A developer sees a new Pending Suggestions (156:1646) visual-layout subsection documenting it as the Smart Allocation confirmation queue — reusing 156:1646's card/list layout pattern but with replaced content semantics (goal name, suggested amount+%, Review/Confirm action opening the 156:653 modal) — not the AI Assistant nudge feed the frame's literal copy shows."
    - "A developer sees an explicit, clearly-flagged Palette A/B color-token discrepancy note in the Design System > Color section, with both palettes' exact hex values, a canonical recommendation (Palette A), and a normalization flag for Transaction History/Pending Suggestions — not a silent merge."
    - "Every previously-locked interaction/state/copy/timing contract, the round-1 Theme Resolution callout, and the round-1 Figma Reference frame map remain intact — this round is additive/refining on top of round 1, not a rewrite."
  artifacts:
    - .planning/phases/02-core-product-loop/02-UI-SPEC.md
  key_links:
    - "FIGMA-CONTEXT-2.md constraints #1-#4 (user-directed resolutions) -> 02-UI-SPEC.md new 'Resolved Decisions (2026-07-06, Round 2)' section"
    - "FIGMA-CONTEXT-2.md Per-Frame Detail sections 2/5/6/7/8 -> 02-UI-SPEC.md per-page 'Figma Reference' Visual Layout subsections (Transaction History, Goal Detail, Create Goal Form, Smart Allocation Modal, new Pending Suggestions)"
    - "FIGMA-CONTEXT-2.md Palette A/B discrepancy finding -> 02-UI-SPEC.md Design System > Color section"
    - "Round-1 Open Questions item #3 (Create First Goal vs D-06) -> remains open, unchanged, cross-referenced correctly from the Goals visual-layout paragraph"
---

<objective>
Round-2 update to `.planning/phases/02-core-product-loop/02-UI-SPEC.md`: convert 4 of round-1's 5 flagged Figma-vs-spec open questions into explicitly RESOLVED decisions per the user's round-2 instructions (Dashboard KPI order, SAW default weights, Pending Suggestions scope, Transaction History nav tab), add full per-page visual-layout detail for the 5 frames round 1 only screenshotted or under-detailed (Transaction History, Goal Detail, Create/Edit Goal Form, Smart Allocation Modal, plus a new Pending Suggestions subsection), and flag a newly-discovered Palette A/B color-token discrepancy in the Design System.

Purpose: Round 1 replaced guessed dark-theme tokens with real Figma-sourced detail but left 5 conflicts unresolved and 5 frames only screenshot-level described. The user has since given explicit resolutions for 4 of the conflicts, and the orchestrator re-pulled full design context (not just screenshots) for all 8 frames this round. This plan's job is a pure documentation transcription/reconciliation task on top of round 1's work — apply the resolutions, deepen the visual detail, and flag the new palette discrepancy, without touching source code or re-litigating what round 1 already locked.

Output: `.planning/phases/02-core-product-loop/02-UI-SPEC.md` updated in place — no source code changes, no other files touched.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-product-loop/02-UI-SPEC.md
@.planning/phases/02-core-product-loop/02-CONTEXT.md
@.planning/quick/260706-jaq-update-02-ui-spec-md-dengan-detail-visua/260706-jaq-FIGMA-CONTEXT.md
@.planning/quick/260706-jaq-update-02-ui-spec-md-dengan-detail-visua/260706-jaq-FIGMA-CONTEXT-2.md
@.planning/quick/260706-jaq-update-02-ui-spec-md-dengan-detail-visua/260706-jaq-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Resolve 4 round-1 open questions as explicit Round-2 decisions; leave item #3 open</name>
  <files>.planning/phases/02-core-product-loop/02-UI-SPEC.md</files>
  <action>
  In the existing "## Open Questions / Flagged Discrepancies (Post-Figma-Extraction, 2026-07-06)" section (currently 5 numbered items, intro sentence "The following 5 items were surfaced by the Figma extraction (quick task 260706-jaq) and are explicitly unresolved..."), restructure per the user's explicit round-2 resolutions recorded in FIGMA-CONTEXT-2.md's numbered constraints 1-4:

  1. Insert a new section immediately before the existing "## Open Questions..." heading, titled exactly "## Resolved Decisions (2026-07-06, Round 2 — User-Directed)", with an intro sentence stating these 4 items were flagged open in the round-1 Figma extraction (2026-07-06, quick task 260706-jaq) and are now resolved by explicit user instruction in round 2 (same date). List exactly 4 items, each starting with a bolded resolved-label in this exact pattern so it is greppable:
     - "**Dashboard KPI order — RESOLVED (2026-07-06, Round 2):**" RESEARCH.md's locked order wins over Figma's visual placement — the page-level SECTION ORDER of the 5 KPIs stays (1) expense breakdown, (2) goal progress, (3) monthly trend, (4) overspending alert, (5) total balance. Figma's `156:198` frame visually places the Overspending Alert directly below the header/period filter, above Expense Breakdown, but this placement is explicitly overridden by the user. Only the Overspending Alert component's own visual card styling is taken from Figma: `#ffdad6` background, `#ba1a1a` border, `#93000a` text, dismissible via an "X" button.
     - "**SAW default weights — RESOLVED (2026-07-06, Round 2, reconfirmed):**" the canonical survey n=62 values are final: personal_importance 22.5%, progress_gap 21.9%, saving_capacity 21.5%, urgency 17.8%, target_amount 16.2%. The Goal Prioritization Settings frame (`156:824`) shows illustrative-only placeholder values (25/20/20/15/20) that must never be adopted as `saw_engine.py` defaults.
     - "**Pending Suggestions scope — RESOLVED (2026-07-06, Round 2):**" the Pending Suggestions page is the Smart Allocation confirmation queue (per PRD/CONTEXT.md), not the AI Assistant nudge feed shown in Figma frame `156:1646`. The page reuses `156:1646`'s visual card/list pattern (icon chip + title + description + amount + pill-shaped CTA + "Dismiss all" text link at the bottom) but with different content semantics: each card represents one queued side-income allocation suggestion — goal name, suggested amount + percentage, and a "Review"/"Confirm" action that opens the Smart Allocation modal (`156:653`) for that specific item, not a spending-pattern nudge. Full visual detail is added by Task 2 of this plan.
     - "**Transaction History nav tab — RESOLVED (2026-07-06, Round 2):**" "History" IS its own bottom-nav destination (confirmed by the 5th-tab-position variant in frame `156:1526`), not a pushed sub-page reached from Home.
  2. Rename the remaining "## Open Questions / Flagged Discrepancies (Post-Figma-Extraction, 2026-07-06)" heading to "## Open Questions / Flagged Discrepancies (Still Open)", update its intro sentence to state that only 1 item remains open (down from the original 5 — 4 were resolved 2026-07-06 Round 2, see the "Resolved Decisions" section above), and delete items 1, 2, 4, 5 from the numbered list, keeping ONLY the original item 3 ("Create First Goal" full onboarding page vs. D-06's lightweight empty state), unchanged in substance, renumbered as the sole item "1.".
  3. Fix two now-stale cross-references elsewhere in the document that pointed at the old numbering: in the "### Visual Layout — Dashboard (`156:198`)" paragraph, replace the phrase "see Open Questions #1 for the conflict with this spec's locked KPI order" with "resolved 2026-07-06 Round 2 — see 'Resolved Decisions' section above; the page-level KPI order still follows RESEARCH.md, not this frame's literal layout". In the "Create First Goal (empty state)" paragraph under "### Visual Layout — Goals", replace "See Open Questions #3 for the conflict with D-06's lightweight section-scoped empty-state contract." with "See 'Open Questions / Flagged Discrepancies (Still Open)' below for the conflict with D-06's lightweight section-scoped empty-state contract — still unresolved."
  4. Update the addendum note under the existing "## Checker Sign-Off" heading (added in round 1, currently stating the 5 discrepancies are pending team resolution) to reflect that 4 of the original 5 discrepancies were resolved 2026-07-06 (Round 2, user-directed) and only 1 (Create First Goal vs D-06) remains pending team resolution before execution of the affected area. Do not alter the existing checkbox/approval lines below it.

  Do NOT touch the Frame Node-ID Map table, the Transaction History / Allocation-modal Visual Layout paragraphs, or the Design System > Color section in this task — those belong to Tasks 2 and 3.
  </action>
  <verify>
    <automated>grep -c "Resolved Decisions (2026-07-06, Round 2" .planning/phases/02-core-product-loop/02-UI-SPEC.md; grep -c "RESOLVED (2026-07-06, Round 2" .planning/phases/02-core-product-loop/02-UI-SPEC.md; grep -c "Still Open" .planning/phases/02-core-product-loop/02-UI-SPEC.md</automated>
  </verify>
  <done>02-UI-SPEC.md contains a new "Resolved Decisions (2026-07-06, Round 2 — User-Directed)" section stating all 4 round-2 resolutions as decisions (not questions); the "Open Questions" section is renamed to "(Still Open)" and retains only the Create First Goal vs D-06 item, unchanged in substance; both stale cross-references (Dashboard paragraph, Create First Goal paragraph) point at the correct section; the Checker Sign-Off addendum reflects the updated resolved/open count (4 resolved, 1 open).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add full per-page visual-layout detail for Transaction History, Goal Detail, Create/Edit Goal Form, Smart Allocation Modal, and new Pending Suggestions subsection</name>
  <files>.planning/phases/02-core-product-loop/02-UI-SPEC.md</files>
  <action>
  Using FIGMA-CONTEXT-2.md's "Per-Frame Detail" section (items 2, 5, 6, 7, 8 — all pulled via full Figma design context this round, not just screenshots), replace the current thin/screenshot-only paragraphs in 02-UI-SPEC.md's "## Figma Reference — Frame Map & Visual Detail" section with full structural detail, and bring Pending Suggestions newly into scope per Task 1's resolution:

  1. Frame Node-ID Map table: add a new row "| Allocation modal | Pending Suggestions (Smart Allocation confirmation queue) | `156:1646` |" directly after the existing "Smart Allocation Suggestion" row. Replace the paragraph beginning "Explicitly out of Allocation-modal scope (see Open Questions #4 below): `156:1646` "Pending Suggestions" is recommended OUT of this scope..." with a resolved statement: `156:1646` is now IN scope as the Smart Allocation confirmation queue page (resolved 2026-07-06, Round 2 — see "Resolved Decisions" section above); its literal AI-Assistant-nudge copy/content does not apply — only its card/list visual layout pattern is reused.

  2. Replace the existing "**Transaction History (`156:1526`):**" paragraph under "### Visual Layout — Transactions" with full detail: flag that this frame uses Palette B, not the Palette A tokens used elsewhere (see the Design System > Color discrepancy note added by Task 3); search bar (white input, `border-[#e5e2e1]`, rounded-8px, search icon left-inset, placeholder "Search transactions..." in `#c0c6d6`); filter button (48x48 square, `bg-[#f6f3f2]`, rounded-8px, sliders icon); date-group headers (small-caps bold `#c0c6d6`, 12px, tracked, "TODAY"/"YESTERDAY"); rows grouped inside one bordered container per date (`border-[#f6f3f2]`, rounded-12px) with thin `border-b border-[#f6f3f2]` row dividers, not individually-boxed cards like other lists; row anatomy — circular category-tinted icon chip + merchant/title (semibold 16px `#1b1b1c`) + category (14px `#414753`) on the left, amount (semibold 16px, `#ba1a1a` "- Rp X" for expense / `#005bb0` "+ Rp X" for income) + time (14px `#717785`) right-aligned. State that the bottom-nav "History" tab is confirmed as its own destination (resolved — see "Resolved Decisions" above), and note the active-pill color should use Palette A's `#298dff` per the normalization recommendation rather than this frame's literal `#0074dc`.

  3. Replace the existing "**Goal Detail (`156:558`):**" paragraph under "### Visual Layout — Goals" with full detail: confirm Palette A tokens here (`#1e1e1e`, `#298dff`, `#ff8929`, `#fcfcfc`, `rgba(30,30,30,0.15)` borders); hero graphic (256px tall, rounded-12px, orange gradient `#ffb787`→`#ff8929` at 80% opacity with a `mix-blend-overlay` pixel-art image, large drop shadow); Progress Card ("CURRENT PROGRESS" 12px bold uppercase muted label + big stat in Bricolage Grotesque ExtraBold 32px `#298dff` + circular icon badge top-right on `rgba(255,137,41,0.2)` background; progress bar orange gradient `#ffb787`→`#ff8929`, 12px height, rounded-full; below a border-top divider, a 2-column COLLECTED/TARGET stats row in 16px semibold `#1e1e1e`, then a full-width DEADLINE row with a calendar icon); Allocation History section (20px semibold heading + individually-boxed rounded-8px cards — not grouped like Transaction History — each with an icon chip tinted orange `rgba(250,179,135,0.2)` for manual saving or blue `rgba(41,141,255,0.1)` for auto-roundup, a date (16px medium) + type label (14px muted) on the left, and a signed amount in `#ff8929` ("+Rp X") on the right); footer actions (centered row: circular grey `rgba(30,30,30,0.05)` "Edit" icon-button with pencil icon + 12px bold caption below, and circular red-tinted `rgba(255,218,214,0.5)` "Delete" icon-button with trash icon + `#ba1a1a` 12px bold caption below — both icon-with-caption style, not plain text buttons); header is back arrow + goal name as page title (Bricolage Grotesque ExtraBold, `#298dff`, centered).

  4. Replace the existing "**Create Goal Form (`156:713`):**" paragraph under "### Visual Layout — Goals" with full detail: header (back arrow + "Create New Goal" in Bricolage Grotesque ExtraBold `#298dff` + right-aligned overflow-menu "⋮" button); Quick Start section (20px semibold label + horizontal-scroll row of 128px-wide rounded-12px template chip cards, white background, `rgba(30,30,30,0.15)` border, each with a circular icon badge tinted per template — Emergency Fund `#ffdad6`, Vacation `rgba(41,141,255,0.2)`, Health `rgba(255,137,41,0.14)`, Laptop/Gadget `rgba(41,141,255,0.14)` — plus a centered up-to-2-line 14px semibold label); form section (single bordered rounded-12px card, 25px padding, containing: Goal Name text input; Target Amount input with a fixed "Rp" prefix at 16px semibold and placeholder "10,000,000"; Deadline input styled as a 3-segment "mm / dd / yyyy" placeholder with a calendar-icon button right-inset; an Importance slider labeled "How important is this to you?" with a bold blue `#298dff` current-value readout right-aligned, an `rgba(30,30,30,0.08)` 8px-height rounded track, tick numbers 1-5 below, and endpoint labels "Nice to have"/"Critical"); CTA (full-width gradient `#298dff`→`#065fc5` button, rounded-12px, "+" icon + "Create Goal" 20px semibold white label, drop shadow). Note that this frame suppresses the bottom nav bar, confirming Create/Edit Goal is a pushed full-screen flow, not a tab-nav destination.

  5. Replace the existing "### Visual Layout — Allocation modal (`156:653`)" paragraph with full structural detail (superseding round 1's screenshot-only description): dimmed+blurred simulated background (`rgba(30,30,30,0.2)` overlay, `backdrop-blur-[1px]`) behind a centered modal card; modal card `max-w-[384px]`, `rounded-24px` top corners, `bg-[rgba(252,252,252,0.85)]` with `backdrop-blur-[12px]` frosted-glass effect, large shadow; header illustration band (128px tall, `bg-[rgba(41,141,255,0.2)]`, two blurred decorative circle shapes, centered white circular badge with a sparkle/stars icon); content ("Smart Allocation" heading, Bricolage Grotesque ExtraBold 24px centered, body copy composing mixed-weight inline spans — muted regular text with bold/semibold emphasis on the amounts and goal name, e.g. "Your side income of **Rp 500.000** just came in! We suggest allocating **Rp 175.000 (35%)** to your **New Laptop** goal — it's your top priority right now."); mini progress-bar context card (bordered white rounded-12px card showing goal name + "Priority 1" 12px bold blue badge on the top row, then a progress bar split into a "Current Progress" segment `rgba(30,30,30,0.22)` dark grey followed by a "Suggested Addition" segment `#298dff` solid, then a bottom row with the current amount on the left and the delta "+ Rp 175K" in blue on the right); exactly 3 stacked actions confirming the never-auto-execute contract — "Confirm Allocation" (solid `#298dff` pill, white text, primary), "Change Amount" (outlined pill, `#1e1e1e` text, secondary), "Not Now" (plain text link, muted, tertiary, no border/background).

  6. Add a new subsection immediately after the Allocation modal subsection, titled "### Visual Layout — Pending Suggestions (`156:1646`)", documenting it as the resolved-scope Smart Allocation confirmation queue (per the "Resolved Decisions" section from Task 1): flag that this frame uses Palette B (`#414753`/`#717785` text, `#0074dc` blue — normalize to Palette A's `#298dff` per the Design System discrepancy note added by Task 3); note the greeting-style header ("Hey there! 👋") is NOT reused (that belongs to the AI Assistant feed, out of scope), but the list-of-cards pattern IS reused: a high-priority first card with a decorative orange corner-blob overlay (`rgba(255,137,41,0.2)`, top-right, rounded-bl-full) versus standard plain bordered cards for subsequent items, each card containing a circular icon chip (goal icon, not an activity icon), a title (20px semibold — the goal name), a short one-line description (14px muted, e.g. "Rp 175.000 (35%) dari side income baru" instead of the frame's spending-nudge description), a bottom row with the amount (16px semibold, blue) on the left and a solid pill CTA on the right labeled "Review"/"Konfirmasi" (use Palette A's `#298dff` solid pill, not the frame's literal `#0074dc`/orange), which opens the Smart Allocation modal (`156:653`) for that specific queued item; footer is a centered "Dismiss all" text link (14px muted, border-bottom underline style). State explicitly that each card = one queued side-income allocation suggestion (goal name, suggested amount + %, Review/Confirm action), not a spending-pattern nudge.
  </action>
  <verify>
    <automated>grep -c "156:1646" .planning/phases/02-core-product-loop/02-UI-SPEC.md; grep -c "backdrop-blur-\[12px\]" .planning/phases/02-core-product-loop/02-UI-SPEC.md; grep -c "mix-blend-overlay" .planning/phases/02-core-product-loop/02-UI-SPEC.md; grep -c "Palette B" .planning/phases/02-core-product-loop/02-UI-SPEC.md</automated>
  </verify>
  <done>02-UI-SPEC.md's Figma Reference section contains full structural visual detail (not thin placeholder text) for Transaction History, Goal Detail, Create/Edit Goal Form, and a superseded Smart Allocation Modal description, plus a new "Visual Layout — Pending Suggestions (156:1646)" subsection documenting it as the resolved-scope Smart Allocation confirmation queue reusing 156:1646's card/list layout with replaced content semantics. The Frame Node-ID Map table lists 156:1646 as in-scope under Allocation modal.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Flag the Palette A/B color-token discrepancy in the Design System > Color section</name>
  <files>.planning/phases/02-core-product-loop/02-UI-SPEC.md</files>
  <action>
  In the "## Color" section of 02-UI-SPEC.md, immediately after the existing "**Secondary interactive/semantic color:** accent orange (`#ff8929`)..." paragraph and before the "**OLD/stale dark-theme table...**" heading, insert a new clearly-flagged subsection titled exactly "**⚠ Palette A/B Discrepancy (flagged 2026-07-06, Round 2) — do not silently normalize:**" documenting: two distinct color-token sets exist across the 8 frames pulled in round 2, likely two different design-iteration passes within the same Figma file.

  Palette A (majority — 6 of 8 frames: Dashboard `156:198`, Home/Add-Transaction `156:65`, Goal List `156:430`, Goal Detail `156:558`, Create Goal Form `156:713`, Smart Allocation Modal `156:653`) matches the target color roles already documented above in this Color section: text `#1e1e1e` primary / `rgba(30,30,30,0.65)` muted, blue `#298dff`→`#065fc5`, orange `#ff8929`→`#ffb787`, background `#fcfcfc`, borders `rgba(30,30,30,0.15)`, Bricolage Grotesque ExtraBold headings.

  Palette B (minority — 2 of 8 frames: Transaction History `156:1526`, Pending Suggestions `156:1646`) uses different tokens: text `#1b1b1c` primary / `#414753` secondary / `#717785` tertiary / `#c0c6d6` placeholder; blue `#005bb0` (headings/amounts) and `#0074dc` (buttons/active nav); background `#fcf9f8` (warmer off-white vs Palette A's cooler `#fcfcfc`); borders `#e5e2e1`, card fill `#f6f3f2`; destructive red `#ba1a1a` (same value in both palettes); Bricolage Grotesque heading weight is inconsistent even within Palette B itself (ExtraBold on Transaction History, plain Regular on Pending Suggestions).

  State the explicit recommendation: treat Palette A as canonical (majority usage plus the two most structurally central screens, Dashboard and Goal List) and flag Palette B frames (Transaction History, Pending Suggestions) as needing normalization to Palette A tokens during implementation — e.g. build with `#1e1e1e`/`#298dff`/`#fcfcfc`/`rgba(30,30,30,0.15)` instead of the frame's literal `#1b1b1c`/`#005bb0`/`#fcf9f8`/`#e5e2e1`. Use the exact phrase "normalization to Palette A tokens" somewhere in this recommendation sentence. State this is an explicit flagged recommendation pending team confirmation, not a silent substitution — do not implement Palette B's literal hex values in production code without team sign-off.
  </action>
  <verify>
    <automated>grep -c "Palette A/B Discrepancy" .planning/phases/02-core-product-loop/02-UI-SPEC.md; grep -c "#1b1b1c" .planning/phases/02-core-product-loop/02-UI-SPEC.md; grep -c "normalization to Palette A tokens" .planning/phases/02-core-product-loop/02-UI-SPEC.md</automated>
  </verify>
  <done>The Color section contains an explicit, clearly-flagged Palette A/B discrepancy note (not a silent merge), documenting both token sets' exact hex values, recommending Palette A as canonical, and flagging Transaction History/Pending Suggestions frames for normalization pending team confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a documentation-only update to a Markdown design-contract file (`02-UI-SPEC.md`). No source code, API surface, dependency, or trust boundary is touched.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02q2-01 | N/A | .planning/phases/02-core-product-loop/02-UI-SPEC.md (docs only) | low | accept | No code executes as a result of this change; no new attack surface. Not applicable — documentation edit only. |
</threat_model>

<verification>
After all 3 tasks complete, confirm:
1. `.planning/phases/02-core-product-loop/02-UI-SPEC.md` contains a "Resolved Decisions (2026-07-06, Round 2 — User-Directed)" section with all 4 items (Dashboard KPI order, SAW default weights, Pending Suggestions scope, Transaction History nav tab) stated as RESOLVED, each citing the user-directed round-2 resolution.
2. The "Open Questions" section is renamed to reflect only 1 remaining item (Create First Goal vs D-06), unchanged in substance from round 1.
3. Transaction History, Goal Detail, Create/Edit Goal Form, and Smart Allocation Modal each have full structural visual-layout detail (not thin placeholder/screenshot-only text), and a new Pending Suggestions (156:1646) subsection exists documenting the resolved scope.
4. The Design System > Color section contains an explicit, clearly-flagged Palette A/B discrepancy note with both palettes' hex values and a canonical recommendation.
5. `git diff .planning/phases/02-core-product-loop/02-UI-SPEC.md` shows the "## Interaction & State Contracts (Phase 2-specific)" section, "## Copywriting Contract" table, round-1 "Theme Resolution" callout, and the Figma file/page identity + frame node-ID map are all unchanged in substance — only additive/refining edits were made.
</verification>

<success_criteria>
- All 4 resolvable round-1 open questions (Dashboard KPI order, SAW default weights, Pending Suggestions scope, Transaction History nav tab) are documented as explicit RESOLVED decisions, not open questions.
- Open-question item #3 (Create First Goal vs D-06) remains open and unchanged.
- Transaction History, Goal Detail, Create/Edit Goal Form, and Smart Allocation Modal have full structural visual-layout detail; a new Pending Suggestions subsection exists with the resolved-scope content semantics.
- The Palette A/B color-token discrepancy is flagged explicitly with a canonical recommendation, not silently merged.
- All previously-locked interaction/state/copy/timing contracts and round-1 content remain intact.
- No files outside `.planning/phases/02-core-product-loop/02-UI-SPEC.md` are modified.
</success_criteria>

<output>
Create `.planning/quick/260706-jaq-update-02-ui-spec-md-dengan-detail-visua/260706-jaq-SUMMARY-2.md` when done.
</output>
