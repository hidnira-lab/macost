---
quick_id: 260704-h5i
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting '/' no longer shows the create-next-app scaffold (Next.js logo, 'edit the page.tsx file' heading, Templates/Documentation links)"
    - "Visiting '/' sends the user to /login"
    - "The static export build (apps/web's output: 'export', required for the Tauri desktop wrapper) still succeeds after this change"
  artifacts:
    - path: apps/web/app/page.tsx
      provides: "Server Component (no 'use client') that calls next/navigation's redirect('/login') unconditionally, replacing the create-next-app template body"
  key_links:
    - from: "apps/web/app/page.tsx"
      to: "apps/web/app/(auth)/login/page.tsx"
      via: "redirect('/login') resolved by the App Router to the (auth) route group's /login route"
      pattern: "redirect\\("
---

<objective>
Replace the unedited create-next-app scaffold at `apps/web/app/page.tsx` with a server-side redirect to `/login`, so visiting the app root sends users straight into the auth flow instead of showing placeholder template content.

Purpose: The root route is the first thing any visitor (browser or Tauri desktop WebView) hits. Right now it renders the default Next.js/Vercel getting-started template — dead content that was never replaced after `create-next-app` scaffolding. There is no dashboard/home page built yet in this project (Phase 1 only shipped auth + wallets), so the correct behavior for "/" today is an unconditional redirect to the existing `/login` page — not a session-check branch to a route that doesn't exist yet.
Output: `apps/web/app/page.tsx` rewritten as a Server Component that calls `redirect('/login')` from `next/navigation`, with the old scaffold JSX and `next/image` import fully removed.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# Current scaffold being replaced
@apps/web/app/page.tsx

# Confirms /login exists and the App Router conventions used elsewhere in this route tree (route group layout, no scaffold leftovers)
@apps/web/app/(auth)/login/page.tsx
@apps/web/app/(auth)/layout.tsx

# apps/web is built with output: 'export' (static export for the Tauri desktop wrapper) — redirect() in a plain Server Component is supported here: Next.js bakes it into the exported HTML as a `<meta id="__next-page-redirect" httpEquiv="refresh">` tag plus a client-side replace on hydration, since there is no live request/response at static-export build time to issue a real 307.
@apps/web/next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace root page scaffold with a redirect to /login</name>
  <files>apps/web/app/page.tsx</files>
  <action>
Rewrite apps/web/app/page.tsx completely, removing the entire create-next-app scaffold: the `import Image from "next/image"` line, and the `Home` component's JSX body (the Next.js logo image, the "get started" heading and paragraph with Templates/Learning links, and the Deploy Now / Documentation link row).

Keep the file a plain Server Component — do NOT add `'use client'` and do NOT use `useEffect`/`useRouter` for this; that pattern belongs to interactive client pages like apps/web/app/(auth)/login/page.tsx, not an unconditional redirect. Import `redirect` from `next/navigation` and call `redirect('/login')` inside the default-exported page function. Since `redirect()` throws internally and never returns, the function body needs nothing after the call — no `return` statement, no JSX, no `useEffect`.

Do not build any session-check or "already logged in -> dashboard" branch: there is no dashboard/home route anywhere under apps/web/app/ yet (only `(auth)/login`, `(auth)/register`, and `wallets` exist), so an unconditional redirect to `/login` is the entire scope of this change.
  </action>
  <verify>
    <automated>cd apps/web && grep -q "next/navigation" app/page.tsx && grep -q "redirect(" app/page.tsx && npm run lint && npm run build && grep -q "__next-page-redirect" out/index.html && grep -q "login" out/index.html && echo ROOT_REDIRECT_OK</automated>
  </verify>
  <done>apps/web/app/page.tsx imports redirect from next/navigation and calls redirect('/login') unconditionally with no leftover scaffold JSX/imports; `npm run lint` and `npm run build` both succeed; the exported apps/web/out/index.html contains the Next.js-generated `__next-page-redirect` meta-refresh tag pointing at /login.</done>
</task>

</tasks>

<verification>
- [ ] apps/web/app/page.tsx no longer contains any create-next-app scaffold content (Next.js logo, "get started" heading, Templates/Documentation links)
- [ ] apps/web/app/page.tsx imports `redirect` from `next/navigation` and calls it with `/login`
- [ ] `cd apps/web && npm run lint` exits 0 (no unused-import or other lint errors from the rewrite)
- [ ] `cd apps/web && npm run build` exits 0 (static export still works for the Tauri wrapper)
- [ ] apps/web/out/index.html contains the `__next-page-redirect` meta tag targeting `/login`
</verification>

<success_criteria>
- Visiting "/" in the built app (web or Tauri desktop) redirects to /login instead of showing the Next.js scaffold
- No dashboard/session-check logic was added (out of scope — no dashboard route exists yet)
- Static export build for the Tauri desktop wrapper is unaffected
</success_criteria>

<output>
Create `.planning/quick/260704-h5i-ganti-root-page-apps-web-app-page-tsx-da/260704-h5i-SUMMARY.md` when done
</output>
