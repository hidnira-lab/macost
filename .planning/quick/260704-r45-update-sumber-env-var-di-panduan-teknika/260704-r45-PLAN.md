---
quick_id: 260704-r45
description: Update sumber env var di PANDUAN_TEKNIKAL_TIM.md dari minta-ke-Hidayat jadi rujuk Notion workspace Zephyra
date: 2026-07-04
---

## Task 1: Update env var source reference in docs/PANDUAN_TEKNIKAL_TIM.md

<files>
- docs/PANDUAN_TEKNIKAL_TIM.md
</files>

<action>
User is creating a dedicated Notion page in the Zephyra team workspace to hold API keys/secrets (Supabase, AI Vision), so teammates no longer need to DM Hidayat directly for env values. The Notion page link isn't finalized yet, so use a generic placeholder reference rather than inventing a URL.

Update Section 3 ("Setup Awal Tiap Orang") where it currently says "Isi value asli di `backend/.env` (minta ke Hidayat — project Supabase-nya sudah live...)" — change the sourcing instruction from "minta ke Hidayat" to referencing the team's Notion workspace (a page dedicated to API keys, name/link TBD — team already knows how to find the Zephyra workspace). Keep the list of required keys (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY, AI_VISION_API_KEY) unchanged, and keep the "never commit this file" warning.

Also check apps/web's env section (NEXT_PUBLIC_API_BASE_URL / NEXT_PUBLIC_USE_MOCK) further down in Section 3 for any similar "ask Hidayat" phrasing that should be updated consistently — apply the same Notion reference there if present, otherwise leave as-is since those two values are documented directly in-doc already (localhost:8000 or the Railway URL) and aren't secrets.

Use a clearly-marked placeholder for the actual page name/link (e.g. "[Nama/link halaman Notion API Keys — TBD]") so Hidayat can fill it in later without needing another doc pass, and add a one-line note that this is pending.
</action>

<verify>
grep "Notion" in the updated file — present in the env-setup section.
</verify>

<done>
docs/PANDUAN_TEKNIKAL_TIM.md's env setup instructions reference the team's Notion workspace instead of "ask Hidayat directly", with a placeholder for the specific page link.
</done>
