---
phase: quick-260704-o31
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/native/src-tauri/Cargo.toml
autonomous: true
requirements: [DEBUG-DEVTOOLS-01]
must_haves:
  truths:
    - "Tauri's release build compiles with the WebView DevTools inspector enabled, so the running desktop app's console/network panel can be opened to diagnose the 'Tidak dapat terhubung ke server' login failure"
    - "No other dependency, feature flag, or file in the Tauri project is modified"
  artifacts:
    - apps/native/src-tauri/Cargo.toml
  key_links:
    - "Cargo.toml [dependencies].tauri.features -> [\"devtools\"] (Tauri 2's devtools Cargo feature is independent of debug_assertions, so it stays active in release builds, not just debug builds)"
---

<objective>
Enable the Tauri `devtools` Cargo feature so the WebView's DevTools (console + network tab) can be opened inside the running release build, to diagnose why login fails with a network error inside the packaged desktop app despite the backend URL, CORS, and a plain browser all checking out fine.

Purpose: We're mid-debugging a Tauri-WebView-specific login failure ("Tidak dapat terhubung ke server"). Backend URL is confirmed correct, CORS preflight from `tauri://` and `https://tauri.localhost` succeed via curl, and a regular browser reaches the backend fine — so the remaining unknown is what the WebView process itself is doing at request time. DevTools is currently disabled in this build, so there is no way to inspect the actual browser-level console/network error from inside the app. This is a temporary debugging aid, not a permanent feature.

Output: `apps/native/src-tauri/Cargo.toml`'s `tauri` dependency has `features = ["devtools"]` instead of `features = []`. Nothing else in the file changes.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Current file state (read prior to planning), `apps/native/src-tauri/Cargo.toml`:
```
[package]
name = "macost"
version = "0.1.0"
edition = "2021"

[lib]
name = "macost_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-store = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
```

Only line 17 (`tauri = { version = "2", features = [] }`) is in scope. Every other line — `tauri-build` build-dependency, `tauri-plugin-store`, `serde`, `serde_json`, `[package]`/`[lib]` sections — must remain byte-for-byte unchanged.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable Tauri devtools Cargo feature</name>
  <files>apps/native/src-tauri/Cargo.toml</files>
  <action>
Open `apps/native/src-tauri/Cargo.toml` and edit the `[dependencies]` section's `tauri` line (currently line 17: `tauri = { version = "2", features = [] }`). Change the empty `features = []` array to `features = ["devtools"]`, so the line reads exactly: `tauri = { version = "2", features = ["devtools"] }`.

Do not touch any other line in the file: `[package]` section (`name`, `version`, `edition`), `[lib]` section (`name`, `crate-type`), the `[build-dependencies]` section's `tauri-build` line, or the `tauri-plugin-store`, `serde`, `serde_json` lines under `[dependencies]`. Do not add, remove, or reformat any other feature flag.

Tauri 2's `devtools` Cargo feature is independent of Rust's `debug_assertions` — enabling it activates the WebView inspector (right-click "Inspect" / F12, or programmatic `window.open_devtools()`) in release builds too, not only debug builds. That is intentional here: the bug under investigation is specific to a release build, so debug-only DevTools would not help. This is a temporary debugging aid — flag in the summary that the team should consider reverting this feature flag once the underlying login/network bug is diagnosed and fixed, so DevTools is not left enabled in whatever build gets shipped to the Expo demo.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const t=fs.readFileSync('D:/COLLEGE/UII S4/ZEPHYRA/CODE/macost/apps/native/src-tauri/Cargo.toml','utf8'); const lines=t.split(/\r?\n/); const tauriLine=lines.find(l=>l.trim().startsWith('tauri =')); const ok = tauriLine === 'tauri = { version = \"2\", features = [\"devtools\"] }'; const buildDepLine=lines.find(l=>l.trim().startsWith('tauri-build =')); const buildDepOk = buildDepLine === 'tauri-build = { version = \"2\", features = [] }'; const storeOk = t.includes('tauri-plugin-store = \"2\"'); const serdeOk = t.includes('serde = { version = \"1\", features = [\"derive\"] }'); const serdeJsonOk = t.includes('serde_json = \"1\"'); if(!ok||!buildDepOk||!storeOk||!serdeOk||!serdeJsonOk){ console.error('FAIL', {ok, tauriLine, buildDepOk, storeOk, serdeOk, serdeJsonOk}); process.exit(1);} console.log('PASS');"</automated>
  </verify>
  <done>`apps/native/src-tauri/Cargo.toml`'s `tauri` dependency line reads exactly `tauri = { version = "2", features = ["devtools"] }`; the `tauri-build` build-dependency, `tauri-plugin-store`, `serde`, and `serde_json` lines are all byte-for-byte unchanged from before the edit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tauri WebView (packaged desktop app) -> local machine | DevTools, when enabled, lets whoever runs the `.exe` open a full browser inspector (console, network, DOM, storage) against the running app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick260704o31-01 | Information Disclosure | `apps/native/src-tauri/Cargo.toml` (`tauri` devtools feature) | medium | accept | Feature is enabled deliberately and temporarily to diagnose a specific release-build login bug on a developer machine, not for the build distributed at Expo. Flagged in the plan objective and summary as a debugging aid that should be reverted (`features = []`) once the underlying network bug is fixed, before any build is shared beyond the debugging session. No secrets are exposed by DevTools itself — it surfaces network/console output already visible to a browser hitting the same backend. |
</threat_model>

<verification>
1. Run the Task 1 automated verify command — confirms the `tauri` dependency line reads exactly `tauri = { version = "2", features = ["devtools"] }` and every other dependency line in the file is unchanged.
2. Manual follow-up (outside this plan's scope, next debugging step): rebuild the Tauri desktop app (`cd apps/native/src-tauri && cargo build` or the project's existing Tauri build command) and open DevTools in the running app to inspect the console/network error during login.
</verification>

<success_criteria>
- `apps/native/src-tauri/Cargo.toml`'s `tauri` dependency has `features = ["devtools"]`.
- No other line in `Cargo.toml` is modified.
- No other file in the repository is modified.
</success_criteria>

<output>
Create `.planning/quick/260704-o31-aktifkan-devtools-sementara-di-tauri-bui/260704-o31-SUMMARY.md` when done
</output>
