---
quick_id: 260702-qs7
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/native/src-tauri/target/debug/macost.exe
autonomous: false

must_haves:
  truths:
    - "Running the desktop build command from apps/native produces a native Windows executable (target/debug/macost.exe) without touching the Android NDK/emulator toolchain"
    - "Launching the built executable opens a window that renders visible UI content, not a blank white screen — the desktop counterpart to Phase 1 success criterion #4, now scoped to desktop per the 2026-07-02 MVP-scope revision"
  artifacts:
    - path: apps/native/src-tauri/target/debug/macost.exe
      provides: "Native Windows executable proving the tauri.conf.json build.frontendDist wiring (apps/web/out/) renders correctly under WebView2, independent of the unresolved Android WebView blank-screen bug"
  key_links:
    - from: "apps/web/out/index.html (Next.js static export)"
      to: "apps/native/src-tauri/target/debug/macost.exe (compiled desktop binary)"
      via: "tauri.conf.json build.frontendDist=\"../../web/out\", resolved at build time and loaded by WebView2 at runtime"
      pattern: "frontendDist"
---

<objective>
Verify that the Tauri **desktop** build actually builds and renders — closing out Plan 01-01 Track A Task 3 under the revised MVP scope (desktop-only; Android/mobile descoped to backlog Phase 999.1 per `.planning/todos/pending/2026-07-02-verify-android-ndk-toolchain-and-build-test-apk.md`).

This is a verification/diagnostic task, not a feature build. If the build or render fails, capture the error/diagnostic for a follow-up todo — do not attempt a deep fix here.

Purpose: ROADMAP Phase 1 success criterion #4 ("app launches to the auth screen without a blank screen") now targets `tauri build` (desktop), not `tauri android build`. Desktop uses Microsoft Edge WebView2 to render — an entirely different runtime path from Android's WebView — so this check is independent of the still-unresolved `libmacost_lib.so`-never-loads Android bug and must be verified on its own.

Output: A debug desktop executable at `apps/native/src-tauri/target/debug/macost.exe`, launched and visually confirmed (by Hidayat) to render real UI content instead of a blank window — or, if it fails, a captured error/diagnostic ready to become a new backlog todo.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@apps/native/src-tauri/tauri.conf.json
@apps/native/package.json

# Prior Android blank-screen investigation (different runtime path — WebView, not WebView2 — kept here only as contrast/context, not something this task re-investigates)
@.planning/todos/pending/2026-07-02-verify-android-ndk-toolchain-and-build-test-apk.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Tauri desktop app in debug mode (no installer bundling)</name>
  <files>apps/native/src-tauri/target/debug/macost.exe</files>
  <read_first>
    apps/native/src-tauri/tauri.conf.json — confirm build.frontendDist is "../../web/out" (already corrected from the original "../out" per the Android investigation todo) before building
    apps/native/package.json — scripts.tauri proxies straight to the tauri CLI binary already installed in apps/native/node_modules
  </read_first>
  <action>
    From apps/native, run the desktop debug build without creating a Windows installer package: `npm run tauri -- build --debug --no-bundle`. This invokes the Tauri CLI scoped to the current host target (Windows x64 desktop) — it does not touch the Android NDK, AVD emulator, or `gen/android/` toolchain from the separate Android investigation. Tauri's configured `beforeBuildCommand` ("cd ../web && npm run build") re-runs the Next.js static export first, refreshing apps/web/out/, then compiles the Rust binary in the debug profile (target/debug/, not target/release/) — debug mode skips optimization passes, keeping this diagnostic build fast. The `--no-bundle` flag skips producing an MSI/NSIS installer, which needs WiX/NSIS toolchains not yet verified present and is unnecessary for a build-and-render check — only the raw executable is needed.

    Do not modify next.config.ts, tauri.conf.json, Cargo.toml, or any source file as part of this task. If the build command exits non-zero, do not attempt a fix: capture the full stderr/stdout tail verbatim (last ~40-60 lines) so it can be recorded as a diagnostic in the SUMMARY and, if needed, turned into a new backlog todo — this task's job is to observe and report, not to debug Rust/Cargo/Tauri build failures inline.
  </action>
  <verify>
    <automated>cd "C:/Users/hiday/WebstormProjects/Zephyra/macost/apps/native" && npm run tauri -- build --debug --no-bundle 2>&1 | tee /tmp/macost-desktop-build.log | tail -60; test -f src-tauri/target/debug/macost.exe && echo "PASS: desktop executable produced at src-tauri/target/debug/macost.exe" || echo "FAIL: build did not produce macost.exe — see captured output above for diagnosis"</automated>
  </verify>
  <done>`npm run tauri -- build --debug --no-bundle` has been run to completion (success or failure) with no source files touched. If it exits 0, `apps/native/src-tauri/target/debug/macost.exe` exists on disk. If it fails, the full error output has been captured verbatim for the SUMMARY — no code changes attempted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Launch the desktop executable and visually confirm it renders (not blank)</name>

  <what-built>
    Task 1 compiled apps/native/src-tauri/target/debug/macost.exe from the current apps/web/out/ static export (which already includes the login, register, and wallets routes) using the same tauri.conf.json frontendDist wiring that a release `tauri build` would use. This is the Windows-native desktop counterpart to the blocked `tauri android build` render check — desktop uses Microsoft Edge WebView2, a completely separate rendering runtime from Android's WebView, so a working desktop render neither confirms nor is blocked by the unresolved Android `libmacost_lib.so` bug.
  </what-built>

  <how-to-verify>
    1. Open a terminal in apps/native/src-tauri and run: ./target/debug/macost.exe (or double-click the file directly in File Explorer at apps/native/src-tauri/target/debug/macost.exe).
    2. Confirm a window titled "Macost" opens and stays open (it should not crash, panic, or close immediately after opening).
    3. Look at the window content: it should show the Next.js root page content — e.g. the login/auth screen or whatever apps/web/app/page.tsx currently renders — with visible text/UI, NOT a solid blank/white rectangle with zero content.
    4. If Windows prompts to install the WebView2 Runtime, note that explicitly — most Windows 11 installs already have it bundled with Edge, but flag it if the prompt appears since it would mean a missing system dependency, not a build/code bug.
    5. Close the window once you've confirmed what it shows.
  </how-to-verify>

  <resume-signal>Type "approved" if the window opened and rendered visible content (auth screen or root page). If it was blank white, crashed, showed an error dialog, or triggered a WebView2 install prompt, describe exactly what you saw (or paste any error text) so it can be logged as a new, separate diagnostic rather than confused with the resolved-out-of-scope Android bug.</resume-signal>
</task>

</tasks>

<verification>
- [ ] `npm run tauri -- build --debug --no-bundle` run from apps/native to completion (pass or fail, captured either way)
- [ ] If build succeeded: apps/native/src-tauri/target/debug/macost.exe exists on disk
- [ ] Hidayat has launched the executable and confirmed (or refuted) that it renders visible UI content, not a blank window
- [ ] No source files (next.config.ts, tauri.conf.json, Cargo.toml, lib.rs, main.rs) were modified by this task
</verification>

<success_criteria>
- Desktop build path (`tauri build`, not `tauri android build`) is confirmed working or its failure is captured with enough detail to file a follow-up todo
- If rendering succeeds: Phase 1 success criterion #4 (desktop-scoped) is satisfied and Plan 01-01 Track A Task 3 can be closed under the revised scope
- If rendering fails or is blank: a clear, specific diagnostic (build log tail, or visual symptom description) exists to open a new backlog todo — distinct from the existing Android blank-screen todo, since desktop uses a different render pipeline (WebView2 vs Android WebView)
</success_criteria>

<output>
Create `.planning/quick/260702-qs7-verify-tauri-desktop-build-works/260702-qs7-SUMMARY.md` when done
</output>
