---
phase: 02-core-product-loop
plan: 15
type: execute
wave: 6
depends_on: ["02-14", "02-06", "02-07", "02-08", "02-03", "02-13"]
files_modified: []
autonomous: false
requirements: [ALLOC-01]
user_setup:
  - service: railway
    why: "Verify the Railway hobby-plan Serverless toggle is OFF before measuring live latency — an accidental cold-start would corrupt the ≤2s measurement and risk a live-demo failure"
    dashboard_config:
      - task: "Confirm Railway service Settings -> the 'Serverless' toggle is OFF (UptimeRobot keep-alive from Phase 01.1 already mitigates this independently, but confirm explicitly before this session)"
        location: "Railway Dashboard -> macost-production service -> Settings"

must_haves:
  truths:
    - "The full Phase 2 loop (transaction -> SAW ranking -> allocation suggestion -> confirm/skip -> goal progress update) works end-to-end against the live Railway + Supabase stack with USE_MOCK=false, not just against mocks"
    - "The allocation-suggestion modal appears within 2 seconds of a side-income transaction POST, measured against the live deployment"
    - "The full flow works inside the built Tauri desktop app, not only in a browser"
  artifacts: []
  key_links:
    - "apps/web/.env.local (NEXT_PUBLIC_USE_MOCK=false, not committed) -> real Railway backend -> real Supabase project (pushed in 02-06-PLAN.md)"
</must_haves>
---

<objective>
Run the Phase 2 integration session: flip `NEXT_PUBLIC_USE_MOCK=false` locally, verify the full core loop against the live Railway backend + Supabase project, measure the allocation-suggestion latency against the ≤2 second ROADMAP.md success criterion, and smoke-test the complete flow inside the built Tauri desktop app.

Purpose: This is Hidayat's integration-track closing task for Phase 2 — the moment every mock-first frontend assumption gets validated against the real backend, and the moment the phase's headline success criteria (allocation modal ≤2s, full loop working end-to-end) get their first real proof.
Output: A verified, working end-to-end Phase 2 core loop on the live stack and inside the Tauri desktop build.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- None (integration/verification task — no new repo files; exercises the already-built Phase 2 stack end-to-end)
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-product-loop/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure allocation-suggestion latency against the live Railway + Supabase stack</name>
  <files></files>
  <read_first>.planning/phases/02-core-product-loop/02-RESEARCH.md ("Latency Risk Assessment" section)</read_first>
  <action>
With a valid Supabase JWT for a test account (register/login against the live backend if needed), run a timed sequence against `https://macost-production.up.railway.app`: `POST /api/transactions` with a side-income category, then immediately `GET /api/transactions/{id}/allocation-suggestion`, measuring wall-clock time from the POST request start to the GET response arrival. Run this 3 times to rule out a one-off cold path. Record all 3 timings.

If any measurement exceeds 2000ms, investigate whether the cause is Railway cold-start (check the Serverless toggle per `user_setup` above — should be OFF, and UptimeRobot's 5-minute keep-alive from Phase 01.1 should already prevent this), an N+1 query regression (re-check `goal_service.fetch_and_rank_goals` and `dashboard`/`wallets` derived-SUM logic for accidental per-row loops), or genuine Railway<->Supabase network latency — document which.
  </action>
  <verify>
    <automated>MISSING — this is a live-network timing measurement against a deployed service; there is no meaningful offline automated test for it. Record the 3 timings and their average in the plan SUMMARY.</automated>
  </verify>
  <acceptance_criteria>
    - 3 timed `POST /api/transactions` -> `GET .../allocation-suggestion` round trips are recorded against the live Railway deployment
    - Average of the 3 timings is documented in the SUMMARY, along with a pass/fail verdict against the ≤2000ms ROADMAP.md success criterion
  </acceptance_criteria>
  <done>Real, live-stack latency numbers exist and are compared against the 2-second target, with root-cause investigation performed if the target is missed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full core loop on live stack + Tauri desktop</name>
  <files>none (verification-only task; no repo files created or modified)</files>
  <what-built>The complete Phase 2 core product loop — transaction entry, SAW-ranked goals, Smart Allocation suggest-and-confirm, dashboard KPIs — built across `02-01` through `02-14`, now switched from mock data to the live Railway + Supabase backend.</what-built>
  <how-to-verify>
    1. Set `NEXT_PUBLIC_USE_MOCK=false` in a local, non-committed `.env.local` (or verify the Vercel deployment's env var is already `false`).
    2. Build and launch the Tauri desktop app (`tauri build` or `tauri dev` from `apps/native/`), or use the Vercel-deployed web app if the desktop build is not yet rebuilt with this phase's frontend code.
    3. Register/log in with a fresh or existing test account.
    4. Create at least 2 goals with different `skor_keinginan`/`deadline`/`nominal_target` values (to get a meaningful SAW ranking).
    5. Go to Transactions, add a side-income transaction (pick a category with `flag_pemasukan = "Flexible Side Income"`). Confirm the "Menghitung saran alokasi..." overlay appears, then the Allocation Suggestion Modal opens showing the top-ranked goal and a 29-40% suggested amount.
    6. Tap "Konfirmasi Alokasi" — confirm the goal's progress bar updates immediately on the Goals page.
    7. Add a second side-income transaction and this time tap "Lewati" — confirm it appears on the Pending Allocations page.
    8. Toggle the SAW strategy (Quick Win <-> Importance-First) on the Goals page — confirm the rank order visibly changes.
    9. Visit the Dashboard — confirm all 5 KPIs render in the fixed order with real data reflecting the transactions/goals just created.
    10. Confirm every step above works identically inside the Tauri desktop app window (not just a browser tab).
  </how-to-verify>
  <resume-signal>Type "approved" if every step above works end-to-end on the live stack and inside the Tauri desktop app, or describe exactly which step failed and what happened instead.</resume-signal>
  <action>Perform the 10-step manual verification in `&lt;how-to-verify&gt;` above against the live Railway + Supabase stack (`NEXT_PUBLIC_USE_MOCK=false`) and the built Tauri desktop app.</action>
  <verify>
    <automated>MISSING — this is a human-driven end-to-end UAT pass across a real desktop app and live backend; no automated command substitutes for it. Human confirmation via the resume-signal above is the verification.</automated>
  </verify>
  <done>All 10 verification steps pass on the live stack and inside the Tauri desktop app, confirming ROADMAP.md Phase 2 success criteria 1-5 are genuinely true end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev machine (USE_MOCK=false) -> live Railway backend -> live Supabase project | First real end-to-end exercise of every Phase 2 write path against production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-11 | Tampering | Test data pollution in the live Supabase project from this integration session | low | accept | Use a dedicated test account (not a real demo account) for this session; test transactions/goals can be deleted afterward via the app's own DELETE endpoints (already built and IDOR-safe per `02-09`/`02-10-PLAN.md`) |

</threat_model>

<verification>
- 3 live-latency timings recorded and compared to the ≤2000ms target
- Human confirms the full 10-step flow works end-to-end on the live stack and inside the Tauri desktop app
</verification>

<success_criteria>
- ROADMAP.md Phase 2 success criteria 1-5 are all verified true against the live stack, not just against mocks
- Allocation-suggestion latency is measured and meets (or a documented root cause for missing) the ≤2 second target
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-15-SUMMARY.md` when done
</output>
