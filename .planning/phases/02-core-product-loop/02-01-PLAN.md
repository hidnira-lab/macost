---
phase: 02-core-product-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/migrations/002_create_kategori.sql
  - backend/migrations/003_create_transaksi.sql
  - backend/migrations/004_create_goal.sql
  - backend/migrations/005_create_alokasi.sql
  - backend/migrations/006_create_goal_settings.sql
  - backend/migrations/007_seed_kategori.sql
  - backend/requirements.txt
  - backend/pytest.ini
  - backend/tests/__init__.py
  - backend/tests/conftest.py
  - backend/tests/test_smoke.py
autonomous: true
requirements: [TRAN-02, GOAL-01, SAW-01, SAW-02, SAW-03, ALLOC-04, ALLOC-05]

must_haves:
  truths:
    - "All 5 new Phase 2 tables (kategori, transaksi, goal, alokasi, goal_settings) have migration files ready for Hidayat to push to the live Supabase project"
    - "A backend developer can run pytest from repo root and get a real pass/fail signal before any Phase 2 feature code exists"
  artifacts:
    - backend/migrations/002_create_kategori.sql
    - backend/migrations/003_create_transaksi.sql
    - backend/migrations/004_create_goal.sql
    - backend/migrations/005_create_alokasi.sql
    - backend/migrations/006_create_goal_settings.sql
    - backend/migrations/007_seed_kategori.sql
    - backend/tests/conftest.py
  key_links:
    - "backend/migrations/001_create_dompet.sql RLS/PK conventions -> mirrored exactly in all 5 new tables"
    - "backend/tests/conftest.py fixtures (sample_goals, sample_weights, fake supabase client) -> consumed by every Phase 2 backend test file written in later plans"
</must_haves>
---

<objective>
Write the 6 SQL migration files this phase requires (kategori, transaksi, goal, alokasi, goal_settings, category seed data) following the exact `001_create_dompet.sql` convention, and stand up the backend's first test framework (pytest + httpx + shared fixtures) so every subsequent backend plan in this phase has a working, fast automated-verify loop from the start.

Purpose: This is Wave 0/1 enabling work — no downstream backend plan (transactions, goals, allocations, dashboard) can write meaningful automated tests, and Hidayat cannot push schema to the live Supabase project, until this plan lands.
Output: 6 ready-to-paste `.sql` migration files (not yet executed against live Supabase — that is a separate Hidayat-only blocking plan, `02-06-PLAN.md`), plus a working `pytest` setup with shared fixtures.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `backend/migrations/002_create_kategori.sql`, `003_create_transaksi.sql`, `004_create_goal.sql`, `005_create_alokasi.sql`, `006_create_goal_settings.sql`, `007_seed_kategori.sql` (new files)
- `backend/pytest.ini`, `backend/tests/__init__.py` (new files)
- `backend/tests/conftest.py` — fixtures `fake_supabase_client`, `sample_goals`, `sample_weights`
- `backend/tests/test_smoke.py` — `test_pytest_infra_works`
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-product-loop/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migrations 002-007 (kategori, transaksi, goal, alokasi, goal_settings, seed)</name>
  <files>backend/migrations/002_create_kategori.sql, backend/migrations/003_create_transaksi.sql, backend/migrations/004_create_goal.sql, backend/migrations/005_create_alokasi.sql, backend/migrations/006_create_goal_settings.sql, backend/migrations/007_seed_kategori.sql</files>
  <read_first>backend/migrations/001_create_dompet.sql, API_CONTRACT.md (sections 3-7), .planning/phases/02-core-product-loop/02-RESEARCH.md ("Supabase Schema Recommendation" and "Team-Confirmed Decisions" TC-05 sections)</read_first>
  <action>
Create 6 files, each starting with the exact header comment line `-- Run via: Supabase Dashboard -> SQL Editor -> paste and execute` (matching 001's convention verbatim — this project does not use the Supabase CLI).

`002_create_kategori.sql`: table `kategori` (id_kategori UUID PK DEFAULT gen_random_uuid(), nama_kategori TEXT NOT NULL, tipe TEXT NOT NULL CHECK IN ('Pemasukan','Pengeluaran'), flag_pemasukan TEXT NULL, flag_pengeluaran TEXT NULL). Enable RLS with exactly one policy `kategori_select_all` (FOR SELECT USING (true)) — no insert/update/delete policy for regular users; read-only for MVP per API_CONTRACT.md §3.

`003_create_transaksi.sql`: table `transaksi` (id_transaksi UUID PK DEFAULT gen_random_uuid(), id_pengguna UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, tipe_transaksi TEXT NOT NULL CHECK IN ('Pemasukan','Pengeluaran'), nominal INTEGER NOT NULL CHECK (nominal > 0), tanggal_transaksi DATE NOT NULL, metode_input TEXT NOT NULL DEFAULT 'Manual', dompet_id UUID NOT NULL REFERENCES public.dompet(id_dompet) ON DELETE CASCADE, kategori_id UUID NOT NULL REFERENCES public.kategori(id_kategori), source_label TEXT NULL, catatan TEXT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()). Enable RLS with 4 policies named `transaksi_select_own`/`transaksi_insert_own`/`transaksi_update_own`/`transaksi_delete_own`, each scoped `auth.uid() = id_pengguna`, mirroring 001's dompet policies exactly.

`004_create_goal.sql`: table `goal` (id_goal UUID PK DEFAULT gen_random_uuid(), id_pengguna UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, nama_goal TEXT NOT NULL, nominal_target INTEGER NOT NULL CHECK (nominal_target > 0), deadline DATE NOT NULL, skor_keinginan INTEGER NOT NULL CHECK (skor_keinginan BETWEEN 1 AND 5), created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()). Add a SQL comment noting `skor_kepentingan`/`nominal_terkumpul`/`progress_pct`/`rank` are deliberately NOT columns — always computed in application code (never persisted, to avoid staleness). RLS 4 policies `goal_select_own`/`goal_insert_own`/`goal_update_own`/`goal_delete_own`.

`005_create_alokasi.sql`: table `alokasi` (id_alokasi UUID PK DEFAULT gen_random_uuid(), id_pengguna UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, goal_id UUID NOT NULL REFERENCES public.goal(id_goal) ON DELETE CASCADE, transaksi_id UUID NOT NULL REFERENCES public.transaksi(id_transaksi) ON DELETE CASCADE, nominal_alokasi INTEGER NOT NULL CHECK (nominal_alokasi > 0), tanggal_alokasi TIMESTAMPTZ NOT NULL DEFAULT NOW()). RLS 4 policies `alokasi_select_own`/`alokasi_insert_own`/`alokasi_update_own`/`alokasi_delete_own`.

`006_create_goal_settings.sql`: table `goal_settings` (id_pengguna UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE, strategy TEXT NOT NULL DEFAULT 'quick_win' CHECK IN ('quick_win','importance_first'), weights JSONB NOT NULL DEFAULT the exact JSON `{"personal_importance":0.225,"progress_gap":0.219,"saving_capacity":0.215,"urgency":0.178,"target_amount":0.162}`). RLS with `goal_settings_select_own`, `goal_settings_insert_own`, `goal_settings_update_own` only (no delete policy — a user's settings row is never deleted, only get-or-create'd/updated in application code).

`007_seed_kategori.sql`: INSERT statements implementing TC-05's exact taxonomy — 5 expense rows (tipe='Pengeluaran'): "Makan & Minum" (flag_pengeluaran='Kebutuhan'), "Transportasi" ('Kebutuhan'), "Hiburan" ('Keinginan'), "Keperluan Kuliah" ('Kebutuhan'), "Tempat Tinggal" ('Kebutuhan') — all with flag_pemasukan=NULL; 2 income rows (tipe='Pemasukan'): "Uang Saku / Kiriman Orang Tua" (flag_pemasukan='Fixed Routine'), "Freelance / Kerja Sampingan" (flag_pemasukan='Flexible Side Income') — both with flag_pengeluaran=NULL.
  </action>
  <verify>
    <automated>grep -l "ENABLE ROW LEVEL SECURITY" backend/migrations/00{2,3,4,5,6}_*.sql | wc -l | grep -qx 5 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 6 files exist at the exact paths listed in `<files>`
    - Each file's first non-blank line matches `-- Run via: Supabase Dashboard -> SQL Editor -> paste and execute`
    - `grep -c "CREATE POLICY" backend/migrations/003_create_transaksi.sql` returns 4 (and same for 004/005 files)
    - `grep -c "CREATE POLICY" backend/migrations/002_create_kategori.sql` returns 1
    - `grep -c "INSERT INTO" backend/migrations/007_seed_kategori.sql` returns at least 1, and the file contains both `'Fixed Routine'` and `'Flexible Side Income'` literals
  </acceptance_criteria>
  <done>All 6 migration files exist, follow the 001 convention exactly, and are ready for Hidayat to paste into Supabase Dashboard SQL Editor in `02-06-PLAN.md`.</done>
</task>

<task type="auto">
  <name>Task 2: Backend test infrastructure — pytest, httpx, shared fixtures</name>
  <files>backend/requirements.txt, backend/pytest.ini, backend/tests/__init__.py, backend/tests/conftest.py, backend/tests/test_smoke.py</files>
  <read_first>backend/requirements.txt, backend/core/supabase.py, .planning/phases/02-core-product-loop/02-VALIDATION.md ("Wave 0 Requirements" section)</read_first>
  <action>
Append `pytest>=8.0.0` and `httpx>=0.27.0` to `backend/requirements.txt` (keep existing entries unchanged) — these are the standard Python testing stack already recommended by RESEARCH.md's own Validation Architecture section, not a new external service dependency.

Create `backend/pytest.ini` with a `[pytest]` section and `testpaths = backend/tests` — this makes `pytest` runnable from repo root (matching the existing `from backend.X import Y` absolute-import convention used by `backend/main.py`/`backend/routers/wallets.py`, and matching `backend/Dockerfile`'s `WORKDIR /app` + `uvicorn backend.main:app` invocation style), not from inside `backend/`.

Create `backend/tests/__init__.py` as an empty marker file, matching the existing `backend/{core,dependencies,models,routers}/__init__.py` convention.

Create `backend/tests/conftest.py` with three pytest fixtures:
1. `fake_supabase_client` — a fixture returning an in-memory fake object that implements the chainable interface used throughout this codebase (`.table(name).select(...).eq(...).eq(...).in_(...).insert(...).update(...).delete(...).execute()`, each returning an object with a `.data` list attribute), backed by a per-test dict of `{table_name: [row_dicts]}` the test can seed directly — so unit tests for routers/services never hit the live Supabase project.
2. `sample_goals` — returns a list of 3 goal dicts with varying `skor_keinginan` (1-5), `deadline` (mix of near/far dates), `nominal_target`, and `nominal_terkumpul`, shaped to exercise SAW-01/02's known-ranking-order and identical-values cases.
3. `sample_weights` — returns the exact 5-key default weights dict: `personal_importance=0.225, progress_gap=0.219, saving_capacity=0.215, urgency=0.178, target_amount=0.162`.

Create `backend/tests/test_smoke.py` with one test function asserting `True`, purely to give this task a deterministic, real automated-verify signal (pytest infra either works or it doesn't).
  </action>
  <verify>
    <automated>cd /Users/macbookpro/macost && pip install -r backend/requirements.txt -q && pytest backend/tests/test_smoke.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/requirements.txt` contains lines matching `pytest` and `httpx`
    - `backend/tests/conftest.py` contains `def sample_goals(` and `def sample_weights(` and a fixture function providing the fake Supabase client
    - `pytest backend/tests/test_smoke.py -x` run from repo root exits 0
  </acceptance_criteria>
  <done>`pytest` runs successfully from repo root; `sample_goals`, `sample_weights`, and a fake-Supabase-client fixture are available to every backend test file written in later Phase 2 plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Application (FastAPI, service-role client) -> Supabase Postgres | Backend always uses `get_supabase_admin()` (service role, bypasses RLS) — RLS policies here are defense-in-depth, not the primary enforcement layer |
| Test suite -> "Supabase" | Unit tests use `fake_supabase_client`, never the live project — no live credentials touched by this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering / Information Disclosure | All 5 new tables (RLS policies) | high | mitigate | Every table gets 4 (or 1 for read-only `kategori`) RLS policies mirroring `001_create_dompet.sql` exactly, scoped `auth.uid() = id_pengguna` — backstop given the app itself always double-`.eq()`-scopes by user in later plans |
| T-2-SC | Tampering (supply chain) | `backend/requirements.txt` (pytest, httpx) | low | accept | Both are the de facto standard Python testing stack (millions of downloads, maintained by the Python core testing community / Encode), already explicitly recommended by this phase's own RESEARCH.md Validation Architecture section — no blocking human checkpoint needed for these two well-established libraries |

</threat_model>

<verification>
- `grep -l "ENABLE ROW LEVEL SECURITY" backend/migrations/00{2,3,4,5,6}_*.sql | wc -l` equals 5
- `pytest backend/tests/test_smoke.py -x` exits 0 from repo root
</verification>

<success_criteria>
- 6 migration files exist, ready for Hidayat to paste into Supabase Dashboard SQL Editor
- `pytest` runs from repo root with shared fixtures (`fake_supabase_client`, `sample_goals`, `sample_weights`) available for every downstream backend test
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-01-SUMMARY.md` when done
</output>
