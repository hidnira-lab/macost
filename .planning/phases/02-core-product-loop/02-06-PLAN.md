---
phase: 02-core-product-loop
plan: 06
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements: [TRAN-02, GOAL-01, SAW-01, ALLOC-04, ALLOC-05]
user_setup:
  - service: supabase
    why: "Phase 2 introduces 5 new tables (kategori, transaksi, goal, alokasi, goal_settings) that must exist in the live project before any USE_MOCK=false integration testing (02-15-PLAN.md) can run"
    dashboard_config:
      - task: "Run migrations 002-007 in order via Supabase Dashboard -> SQL Editor -> paste and execute, then confirm all 5 tables + seed rows exist in Table Editor"
        location: "Supabase Dashboard (project provisioned 2026-07-04, per STATE.md) -> SQL Editor"

must_haves:
  truths:
    - "The 5 new Phase 2 tables and the category seed data exist in the live Supabase project, matching backend/migrations/002-007 exactly"
  artifacts: []
  key_links:
    - "backend/migrations/002-007_*.sql (written in 02-01-PLAN.md) -> pasted verbatim into Supabase Dashboard SQL Editor by Hidayat -> live schema"
</must_haves>
---

<objective>
[BLOCKING, Hidayat-only] Push the 6 Phase 2 migration files (`002_create_kategori.sql` through `007_seed_kategori.sql`, written in `02-01-PLAN.md`) to the live Supabase project via Dashboard SQL Editor, in order, and confirm the tables exist.

Purpose: This project does not use the Supabase CLI (no `supabase/` directory, no `config.toml` — confirmed by direct filesystem check). The established convention (per `001_create_dompet.sql` and `docs/PANDUAN_TEKNIKAL_TIM.md` Section 2a) is: write the migration as a plain `.sql` file, and only Hidayat (sole Supabase account holder for this project) executes it against the live project. This task blocks Hidayat's own later integration testing (`02-15-PLAN.md`) against the real backend, but does NOT block Fertika from writing router/model/service code (those plans test against `backend/tests/conftest.py`'s fake Supabase client, never the live project) and does NOT block Khayyira/Zarra's frontend work (mock-first, per `apps/web/lib/api/client.ts`'s `USE_MOCK` toggle).
Output: Live Supabase project has `kategori` (seeded), `transaksi`, `goal`, `alokasi`, `goal_settings` tables with RLS policies active.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- None (operational task — no repo files created or modified; executes existing migration files from `02-01-PLAN.md` against the live Supabase project)
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/PANDUAN_TEKNIKAL_TIM.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>[BLOCKING] Push migrations 002-007 to live Supabase project</name>
  <what-built>6 ready-to-paste SQL migration files at `backend/migrations/002_create_kategori.sql` through `007_seed_kategori.sql` (written in `02-01-PLAN.md`), each following the exact `001_create_dompet.sql` convention (header comment, RLS policies).</what-built>
  <how-to-verify>
    1. Open the Supabase Dashboard for the Macost project (the same project provisioned 2026-07-04 per STATE.md — already holds the `dompet` table from `001_create_dompet.sql`).
    2. Navigate to SQL Editor.
    3. Paste and execute `backend/migrations/002_create_kategori.sql`, then `003_create_transaksi.sql`, then `004_create_goal.sql`, then `005_create_alokasi.sql`, then `006_create_goal_settings.sql`, then `007_seed_kategori.sql` — in this exact order (later files reference earlier tables via foreign keys, e.g. `transaksi.kategori_id -> kategori.id_kategori`, `alokasi.goal_id -> goal.id_goal`).
    4. Do NOT use `supabase db push` or any Supabase CLI command — this project has no CLI setup; Dashboard SQL Editor is the only path.
    5. After running all 6, open Table Editor and confirm all 5 tables exist: `kategori` (with 7 seeded rows), `transaksi`, `goal`, `alokasi`, `goal_settings` — and that each (except `kategori`) shows "RLS enabled" in the table list.
  </how-to-verify>
  <resume-signal>Type "pushed" once all 6 migrations have run successfully and all 5 tables are confirmed visible in Table Editor, or describe the specific SQL error if one occurred.</resume-signal>
  <files>none (this task executes existing SQL files against the live Supabase project; it creates no repo files)</files>
  <action>Paste and execute `backend/migrations/002_create_kategori.sql` through `007_seed_kategori.sql`, in that exact numeric order, via Supabase Dashboard -> SQL Editor -> paste and execute. Do not use the Supabase CLI. See `&lt;how-to-verify&gt;` above for the exact step sequence.</action>
  <verify>
    <automated>MISSING — this is a live Supabase Dashboard action with no CLI/API path in this project; verification is the manual Table Editor confirmation described in &lt;how-to-verify&gt;.</automated>
  </verify>
  <done>All 5 tables (`kategori`, `transaksi`, `goal`, `alokasi`, `goal_settings`) exist in the live Supabase project, `kategori` has 7 seeded rows, and RLS is enabled on all except the intentionally-shared `kategori` table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Hidayat's Supabase Dashboard session -> live production database | Direct DDL execution against the shared, live project all 4 developers and the eventual demo depend on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering / Information Disclosure | Live RLS policies on 5 new tables | high | mitigate | Migrations already encode the 4-policy (or 1-policy for `kategori`) RLS pattern verbatim from `001_create_dompet.sql`; this task's only job is to execute them unmodified — do not edit the SQL during paste |

</threat_model>

<verification>
- Manual: Supabase Table Editor shows all 5 new tables + 7 seeded `kategori` rows after this task completes
</verification>

<success_criteria>
- Live Supabase project schema matches `backend/migrations/002-007_*.sql` exactly
- Fertika's backend plans remain unblocked throughout (they never depended on this task); only `02-15-PLAN.md` (Hidayat's own integration test) depends on it
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-06-SUMMARY.md` when done
</output>
