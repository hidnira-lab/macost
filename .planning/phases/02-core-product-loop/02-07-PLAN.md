---
phase: 02-core-product-loop
plan: 07
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - apps/web/app/goals/[id]/page.tsx
  - apps/web/app/goals/new/page.tsx
autonomous: true
requirements: [GOAL-01, GOAL-03, GOAL-04, GOAL-05]

must_haves:
  truths:
    - "A user can open a goal's detail page and see its full field set plus allocation history"
    - "A user can create a new goal via a form with exactly the 4 required fields (nama_goal, nominal_target, deadline, skor_keinginan) and deadline must be a future date"
    - "A user can edit an existing goal using the same form, pre-filled"
    - "A user attempting to delete a goal that already has allocation history sees an inline error instead of the delete succeeding"
  artifacts:
    - apps/web/app/goals/[id]/page.tsx
    - apps/web/app/goals/new/page.tsx
  key_links:
    - "apps/web/app/goals/new/page.tsx (create mode) -> POST /api/goals; (edit mode, ?edit={id} or /goals/new/{id}) -> PUT /api/goals/{id}"
    - "apps/web/app/goals/[id]/page.tsx delete action -> DELETE /api/goals/{id} -> on 400 GOAL_HAS_ALLOCATIONS shows inline error, not the destructive-confirm dialog outcome"
</must_haves>
---

<objective>
Build the Goal Detail page (full fields + allocation history) and the Goal Create/Edit form (shared component/route for both flows), against the existing mock data (`goal-detail.json` from `02-02-PLAN.md`, `goals.json`).

Purpose: Completes GOAL-01/03/04/05 on the frontend — the remaining vertical slices of the Goals area not covered by the list page in `02-02-PLAN.md`.
Output: Two new working pages reachable from the Goals list.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `apps/web/app/goals/[id]/page.tsx` — goal detail + allocation history + guarded delete
- `apps/web/app/goals/new/page.tsx` — shared create/edit form (`?edit={id}`)
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product-loop/02-UI-SPEC.md
@.planning/phases/02-core-product-loop/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Goal Detail page — full fields, allocation history, delete-with-guard</name>
  <files>apps/web/app/goals/[id]/page.tsx</files>
  <read_first>apps/web/app/wallets/page.tsx, apps/web/lib/api/types.ts (`GoalDetailResponse`, `AllocationHistoryEntry`), apps/web/mocks/goal-detail.json, .planning/phases/02-core-product-loop/02-UI-SPEC.md (Copywriting Contract — goal deletion strings)</read_first>
  <action>
Create `apps/web/app/goals/[id]/page.tsx` (dynamic route) as a client component: auth-guard-on-mount, load via `apiFetch<GoalDetailResponse>('/api/goals/' + params.id)`. Render `nama_goal`, `nominal_target`/`nominal_terkumpul` (Rp-formatted), `progress_pct` bar, `deadline`, `skor_keinginan`, `skor_kepentingan`, `rank`, and an `allocation_history` list (each entry: `nominal_alokasi` Rp-formatted + `tanggal_alokasi` date).

Add "Edit" (`#298dff` inline link, navigates to `/goals/new?edit={id}`) and "Hapus" (`text-red-400`) actions matching `wallets/page.tsx`'s action-button styling exactly.

Delete flow: on click, `window.confirm('Hapus goal ini? Tindakan ini tidak dapat dibatalkan.')`; if confirmed, `apiMutate('/api/goals/' + id, 'DELETE', null)`. If the call throws a structured error with `error.code === 'GOAL_HAS_ALLOCATIONS'` (per Pitfall 10's 400 response), show an inline error banner with the exact copy "Goal ini punya riwayat alokasi, tidak bisa dihapus." INSTEAD of treating it as a generic delete failure — do not show the generic "Gagal menghapus" message for this specific error code. On any other error, show a generic delete-failure message. On success, redirect to `/goals`.
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/goals/[id]/page.tsx` exports `default function` and renders `allocation_history`
    - Delete handler branches on `error.code === 'GOAL_HAS_ALLOCATIONS'` specifically (grep confirms the literal string `GOAL_HAS_ALLOCATIONS` appears in this file)
    - `NEXT_PUBLIC_USE_MOCK=true npm run build` succeeds
  </acceptance_criteria>
  <done>Visiting `/goals/goal_001` (mock mode) shows full goal detail + 2 allocation history entries; simulating a `GOAL_HAS_ALLOCATIONS` error on delete shows the exact inline copy instead of a generic failure message.</done>
</task>

<task type="auto">
  <name>Task 2: Goal Create/Edit form (shared route, GOAL-01/04)</name>
  <files>apps/web/app/goals/new/page.tsx</files>
  <read_first>apps/web/app/wallets/page.tsx (form pattern lines 236-270), apps/web/lib/api/types.ts (`GoalCreateRequest`), .planning/phases/02-core-product-loop/02-UI-SPEC.md (Copywriting Contract — "Buat Goal"/"Simpan Perubahan"; Typography/Color tokens)</read_first>
  <action>
Create `apps/web/app/goals/new/page.tsx` handling both create and edit via a `?edit={id}` search param (read with `useSearchParams`): if present, fetch the existing goal via `apiFetch('/api/goals/' + editId)` and pre-fill the form; if absent, start blank.

Form fields: `nama_goal` (text, required), `nominal_target` (number, required, must be `> 0`), `deadline` (date, required, must be strictly after today — validate client-side with a clear inline error AND rely on the backend's own future-date validation as the authority), `skor_keinginan` (1-5 selector, required — e.g. 5 radio buttons or a labeled range input, not a free-text number field, since it is a bounded discrete score).

Submit: if `editId` present, `apiMutate('/api/goals/' + editId, 'PUT', payload)` and CTA copy "Simpan Perubahan"; else `apiMutate('/api/goals', 'POST', payload)` and CTA copy "Buat Goal" — both exactly per UI-SPEC's Copywriting Contract. On success, redirect to `/goals/{id}` (edit) or `/goals` (create). On failure, show "Gagal menyimpan goal. Coba lagi." per UI-SPEC.

Apply Design System tokens: `#ff8929` primary CTA + input focus border, dark card-on-dark-background layout matching `wallets/page.tsx`'s existing form styling.
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/goals/new/page.tsx` reads `?edit=` via `useSearchParams` and branches its submit target between POST and PUT
    - CTA text is "Buat Goal" when no `edit` param is present and "Simpan Perubahan" when it is (grep confirms both literal strings exist in the file)
    - `deadline` field has a client-side check rejecting today-or-earlier dates before submit
  </acceptance_criteria>
  <done>The same route/component handles both goal creation and editing correctly, with the right CTA copy and submit target for each mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (Goal form) -> `POST`/`PUT /api/goals` | User-controlled goal fields cross into a write endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-08 | Tampering | Client-side deadline/nominal_target validation bypass (e.g. disabled JS, direct API call) | low | accept | Client-side checks are UX-only; the backend's Pydantic validators (`02-10-PLAN.md`, `nominal_target > 0`, deadline-must-be-future) are the actual security/data-integrity boundary — documented here so this is not mistaken for the authoritative check |

</threat_model>

<verification>
- `NEXT_PUBLIC_USE_MOCK=true npm run build` succeeds with no new TypeScript errors
</verification>

<success_criteria>
- Goal detail page shows full fields + allocation history + guarded delete
- Goal create/edit form works in both modes with correct CTA copy and submit target
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-07-SUMMARY.md` when done
</output>
