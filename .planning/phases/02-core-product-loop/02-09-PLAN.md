---
phase: 02-core-product-loop
plan: 09
type: execute
wave: 3
depends_on: ["02-05"]
files_modified:
  - backend/routers/transactions.py
  - backend/routers/wallets.py
  - backend/tests/test_transactions.py
  - backend/tests/test_wallets.py
autonomous: true
requirements: [TRAN-04, TRAN-05]

must_haves:
  truths:
    - "A user can edit a saved transaction (same server-derivation rules as create apply on edit)"
    - "A user can delete a transaction they own; deleting another user's transaction is impossible (IDOR-safe)"
    - "Wallet balance (saldo) reflects the live sum of that wallet's transactions, not a stale manually-incremented column, so it stays correct after any transaction create/edit/delete"
  artifacts:
    - backend/routers/transactions.py
    - backend/routers/wallets.py
  key_links:
    - "GET /api/wallets -> now computes saldo = SUM(Pemasukan) - SUM(Pengeluaran) over that wallet's transaksi rows, batched via .in_(dompet_id list), never a stored/incrementally-mutated column (Pitfall 9 / A7)"
</must_haves>
---

<objective>
Add `PUT`/`DELETE /api/transactions/{id}` (same server-derivation and IDOR rules as `POST`, built in `02-05-PLAN.md`) and change `backend/routers/wallets.py`'s `GET /api/wallets` to compute `saldo` as a derived SUM over each wallet's transactions instead of the stale stored column Phase 1 shipped — resolving RESEARCH.md's Pitfall 9/Assumption A7.

Purpose: Completes TRAN-04/05, and makes "wallet balance updates after transaction" (an explicit ROADMAP.md success criterion / Zarra ownership item) actually true now that transactions exist.
Output: Full Transactions CRUD; wallet balances that are always correct, computed the same way goal progress is (Pattern 2 batched aggregation).
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `backend/routers/transactions.py` — adds `PUT /transactions/{transaction_id}`, `DELETE /transactions/{transaction_id}`
- `backend/routers/wallets.py` — `list_wallets` now computes `saldo` via derived SUM over `transaksi` (was a stored column)
- `backend/tests/test_transactions.py` (extended), `backend/tests/test_wallets.py` (new)
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product-loop/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PUT/DELETE /api/transactions/{id}</name>
  <files>backend/routers/transactions.py, backend/tests/test_transactions.py</files>
  <read_first>backend/routers/transactions.py (as written in 02-05-PLAN.md), backend/routers/wallets.py (PUT/DELETE pattern, lines 80-133)</read_first>
  <behavior>
    - `PUT /api/transactions/{id}` for a transaction owned by the caller updates the row and re-derives `tipe_transaksi`/`source_label` from the (possibly new) `kategori_id` exactly like `POST` — never trusts a `tipe_transaksi` value in the body
    - `PUT /api/transactions/{id}` for a transaction owned by ANOTHER user returns `404` (never `403` — avoids confirming the row exists, matching `wallets.py`'s existing convention)
    - `DELETE /api/transactions/{id}` for a transaction owned by the caller returns `204`; for another user's transaction it also returns `204` (no-op, matching `wallets.py`'s "204 regardless of existence" IDOR-safe convention) but does not actually delete the other user's row
  </behavior>
  <action>
Add `PUT /transactions/{transaction_id}` to `backend/routers/transactions.py`: same body shape as `TransactionCreate` (reuse `TransactionUpdate`), re-look-up the `kategori` row and re-derive `tipe_transaksi`/`source_label` exactly as `create_transaction` does, then `.update({...}).eq("id_transaksi", transaction_id).eq("id_pengguna", current_user_id).execute()`; if `result.data` is empty raise `404` with `{"error": {"code": "NOT_FOUND", "message": "Transaksi tidak ditemukan"}}`.

Add `DELETE /transactions/{transaction_id}`: `.delete().eq("id_transaksi", transaction_id).eq("id_pengguna", current_user_id).execute()`, return `Response(status_code=204)` unconditionally (matches `wallets.py`'s `delete_wallet` exactly).

Write RED-phase tests for both behaviors first, confirm they fail, implement to green.
  </action>
  <verify>
    <automated>cd /Users/macbookpro/macost && pytest backend/tests/test_transactions.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_transactions.py -x` passes, including the cross-user 404-on-PUT and no-op-204-on-DELETE cases
    - `PUT`/`DELETE` handlers both include `.eq("id_pengguna", current_user_id)` in addition to the row-id `.eq()`
  </acceptance_criteria>
  <done>Transactions CRUD is complete; edit re-derives labels correctly; delete/edit are IDOR-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wallet saldo — derived SUM over transactions, not a stale column</name>
  <files>backend/routers/wallets.py, backend/tests/test_wallets.py</files>
  <read_first>backend/routers/wallets.py, backend/models/wallet.py, .planning/phases/02-core-product-loop/02-RESEARCH.md ("Pattern 2: Batched aggregation" and "Pitfall 9: Wallet balance not recomputed" sections)</read_first>
  <behavior>
    - `GET /api/wallets` for a user with 1 wallet, 1 `Pemasukan` transaction of 500000, and 1 `Pengeluaran` transaction of 100000 on that wallet returns `saldo: 400000` for that wallet — computed live, not read from a stored column
    - `GET /api/wallets` for a user with 2 wallets and transactions split across both uses exactly 2 Supabase queries total (wallets, then `transaksi.in_(dompet_id list)`), never one query per wallet (Pitfall 4/5 — no N+1)
    - `GET /api/wallets` for a wallet with zero transactions returns `saldo: 0`, not an error
  </behavior>
  <action>
Modify `list_wallets` in `backend/routers/wallets.py`: after fetching `dompet` rows scoped by `id_pengguna`, collect `dompet_id`s, then run one additional query `supabase.table("transaksi").select("dompet_id, tipe_transaksi, nominal").in_("dompet_id", dompet_ids).execute()` (skip the query entirely if `dompet_ids` is empty, matching the goals Pattern 2 style). In Python, sum per `dompet_id`: `+nominal` for `tipe_transaksi == "Pemasukan"`, `-nominal` for `"Pengeluaran"`. Replace the `saldo` field in each `WalletResponse` with this computed value instead of `row["saldo"]`. Leave `POST /api/wallets` (`create_wallet`, which still inserts `saldo: 0` as a legacy placeholder column value that is now cosmetic/unused by reads) and `PUT /api/wallets/{id}` untouched — this task only changes what `GET` returns.

Write RED-phase tests first (seed `fake_supabase_client` with `dompet` + `transaksi` fixture rows matching the 3 behavior cases above), confirm they fail against the current stored-column implementation, then implement to green.
  </action>
  <verify>
    <automated>cd /Users/macbookpro/macost && pytest backend/tests/test_wallets.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_wallets.py -x` passes all 3 behavior cases
    - `list_wallets` issues exactly 2 Supabase table queries (grep confirms exactly 2 `.table(` calls in the function body, or 1 if `dompet_ids` is empty — a per-wallet loop calling `.table("transaksi")` inside a `for` loop is a FAIL)
  </acceptance_criteria>
  <done>Wallet balances are always correct post-transaction, computed via 2 batched queries, matching the same derived-aggregation pattern goal progress uses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `PUT`/`DELETE /api/transactions/{id}` | User-controlled transaction id crosses into mutating endpoints |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Tampering / Information Disclosure | `PUT`/`DELETE /api/transactions/{id}` IDOR | high | mitigate | Both handlers double-scope with `.eq("id_transaksi", id).eq("id_pengguna", current_user_id)`, matching `wallets.py`'s established pattern exactly |
| T-2-02 | Tampering | `PUT` re-derivation bypass attempt | high | mitigate | `PUT` re-runs the same kategori-lookup derivation as `POST` — never trusts a client-sent `tipe_transaksi` on edit either |

</threat_model>

<verification>
- `pytest backend/tests/test_transactions.py backend/tests/test_wallets.py -x` passes
</verification>

<success_criteria>
- Transactions CRUD complete and IDOR-safe (TRAN-04/05)
- Wallet balances always correct via derived SUM, no N+1 (Pitfall 9 resolved)
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-09-SUMMARY.md` when done
</output>
