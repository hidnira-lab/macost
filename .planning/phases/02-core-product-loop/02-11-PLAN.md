---
phase: 02-core-product-loop
plan: 11
type: execute
wave: 3
depends_on: ["02-02", "02-08"]
files_modified:
  - apps/web/lib/api/client.ts
  - apps/web/mocks/categories.json
  - apps/web/components/TransactionForm.tsx
  - apps/web/app/transactions/page.tsx
autonomous: true
requirements: [TRAN-01, TRAN-02, ALLOC-01]

must_haves:
  truths:
    - "A user can save a transaction using at most 3 required fields (nominal, kategori, dompet); tanggal_transaksi is pre-filled with today and editable but not counted as required"
    - "The frontend never sends tipe_transaksi or a source field to the backend"
    - "Saving a side-income transaction shows a blocking loading overlay with the exact locked copy, then transitions directly into the Allocation Suggestion Modal with no intermediate state, with no client-side timeout"
    - "If the allocation-suggestion fetch fails, the overlay dismisses, an error toast appears, and the transaction remains saved"
  artifacts:
    - apps/web/components/TransactionForm.tsx
    - apps/web/app/transactions/page.tsx
  key_links:
    - "TransactionForm.tsx submit -> POST /api/transactions -> if allocation_suggestion_available, apps/web/app/transactions/page.tsx's handleSaveTransaction -> GET /api/transactions/{id}/allocation-suggestion -> AllocationSuggestionModal (imported from 02-08-PLAN.md) or 'Buat goal dulu' state"
</must_haves>
---

<objective>
Build the Transaction quick-entry form (3-required-field budget, D-01/D-02) and wire the D-03/D-04 save-sequencing logic (loading overlay -> allocation suggestion fetch -> modal or error toast) on the Transactions page, importing the `AllocationSuggestionModal` component built in `02-08-PLAN.md`.

Purpose: This is the literal trigger point of the entire Smart Allocation flow — the single most demo-critical UI sequence in Phase 2 (ROADMAP.md's "allocation modal appears within 2 seconds" success criterion).
Output: A working transaction-entry flow that correctly triggers the allocation modal for side-income saves.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `apps/web/lib/api/client.ts` — new `resolveMock()` branch for `/api/categories`
- `apps/web/mocks/categories.json` (new file)
- `apps/web/components/TransactionForm.tsx` — form component (create mode this plan; edit-mode props added in `02-13-PLAN.md`)
- `apps/web/app/transactions/page.tsx` — `default function`, `handleSaveTransaction()` (D-03/D-04 sequencing)
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product-loop/02-CONTEXT.md
@.planning/phases/02-core-product-loop/02-UI-SPEC.md
@.planning/phases/02-core-product-loop/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TransactionForm.tsx — 3-field budget, categories mock, never sends tipe_transaksi</name>
  <files>apps/web/lib/api/client.ts, apps/web/mocks/categories.json, apps/web/components/TransactionForm.tsx</files>
  <read_first>apps/web/app/wallets/page.tsx (form pattern), apps/web/lib/api/client.ts, apps/web/lib/api/types.ts (`TransactionCreateRequest`, `Category`, `CategoriesResponse`), .planning/phases/02-core-product-loop/02-CONTEXT.md (D-01, D-02), .planning/phases/02-core-product-loop/02-UI-SPEC.md ("Transaction quick-entry form" section)</read_first>
  <action>
Add a `/api/categories` case to `client.ts`'s `resolveMock()` (exact match) returning a new `apps/web/mocks/categories.json` import. Create `apps/web/mocks/categories.json` matching `CategoriesResponse` with the same 7-category taxonomy seeded in `007_seed_kategori.sql` (5 expense + 2 income, matching `flag_pemasukan`/`flag_pengeluaran` values).

Create `apps/web/components/TransactionForm.tsx` exporting a form component with props allowing both create mode (no initial values) and edit mode (pre-filled `initialValues` + `transactionId`, used later by `02-13-PLAN.md`'s edit flow). On mount, fetch `GET /api/categories` and `GET /api/wallets` for dropdown options.

Exactly 3 required fields, visually marked as required: `nominal` (number input, `min=1`), `kategori_id` (select, populated from categories), `dompet_id` (select, populated from wallets). `tanggal_transaksi` is a 4th visible field, pre-filled with today's date (`new Date().toISOString().slice(0,10)`), editable, but NOT styled/marked as required (D-02 — does not visually compete with the 3 true required fields). Optional `catatan` (textarea) rendered visually de-emphasized (Label-size/secondary text color per UI-SPEC).

CRITICAL: the submit payload (`TransactionCreateRequest`) must never include a `tipe_transaksi` or `source` field constructed by this component — build the request object with exactly `{ nominal, tanggal_transaksi, metode_input: 'Manual', dompet_id, kategori_id, catatan }` (D-01 — type/source are derived entirely server-side from the selected category).

On submit, call the provided `onSubmit(payload)` callback (the parent page — Task 2 below — owns the actual `apiMutate` call and D-03/D-04 sequencing); show "Menyimpan..." on the submit button while awaiting, matching the existing `addLoading` pattern in `wallets/page.tsx`.
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/TransactionForm.tsx` never constructs a payload object containing a `tipe_transaksi` or `source` key (grep confirms no `tipe_transaksi:` or `source:` object-literal key inside this file)
    - Exactly 3 form fields have a `required` attribute: the `nominal`, `kategori_id`, and `dompet_id` inputs (grep count of `required` on these 3 elements only, not on `tanggal_transaksi`)
    - `tanggal_transaksi` input's default value is today's date
  </acceptance_criteria>
  <done>The form collects exactly 3 required fields + a pre-filled editable date + an optional de-emphasized note, and its submit payload never includes `tipe_transaksi`/`source`.</done>
</task>

<task type="auto">
  <name>Task 2: Transactions page — save sequencing (D-03/D-04) wiring the Allocation Modal</name>
  <files>apps/web/app/transactions/page.tsx</files>
  <read_first>apps/web/components/AllocationSuggestionModal.tsx (as built in 02-08-PLAN.md), apps/web/app/wallets/page.tsx, .planning/phases/02-core-product-loop/02-RESEARCH.md ("Frontend Integration Pattern" section), .planning/phases/02-core-product-loop/02-UI-SPEC.md ("Allocation suggestion flow" section and Copywriting Contract loading/error strings)</read_first>
  <action>
Create `apps/web/app/transactions/page.tsx` hosting `TransactionForm` (create mode) above an (empty-for-now, filled in by `02-13-PLAN.md`) transaction list placeholder. Auth-guard-on-mount per the established pattern.

Implement `handleSaveTransaction(payload)`: call `apiMutate<Transaction>('/api/transactions', 'POST', payload)`. On success:
- If `created.allocation_suggestion_available` is `false`: just close/reset the form (no overlay, no modal).
- If `true`: immediately show a full-overlay blocking loading state with the EXACT locked copy "Menghitung saran alokasi..." (D-03). The overlay's dismissal must be driven exclusively by the `await apiFetch<AllocationSuggestionResponse>('/api/transactions/' + created.id_transaksi + '/allocation-suggestion')` call settling (its own success or its own thrown error) — D-04 is a hard non-negotiable that the wait duration is never artificially shortened or capped by this page, and no secondary "taking longer than expected" message is ever shown. On success: dismiss the overlay, open `AllocationSuggestionModal` with `open=true`, `suggestion`, `transaksiId=created.id_transaksi` (the modal itself handles both the `has_active_goal: true` and `false` branches — this page does not need separate "Buat goal dulu" logic, the modal owns that). On failure (the fetch throws): dismiss the overlay, show a toast with the EXACT copy "Gagal memuat saran alokasi. Cek nanti di halaman Pending." (D-04), and leave the transaction as already-saved with no rollback/retry prompt.

On `AllocationSuggestionModal`'s `onResolved` callback: close the modal, show a brief success indication (per existing toast/banner conventions), and trigger a placeholder refresh hook the transaction-list plan (`02-13-PLAN.md`) will wire up to actually refetch data.
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/transactions/page.tsx`'s overlay-dismissal logic is driven only by the `apiFetch(.../allocation-suggestion)` promise's own resolution/rejection — no independent wait-duration cap wraps or races that call (manual code-review check during `/gsd-code-review`, since a structural grep for this behavior is unreliable — a legitimate explanatory comment about D-04 could otherwise false-fail a naive text-absence check)
    - The literal string "Menghitung saran alokasi..." appears exactly once, shown before the `apiFetch` call to the allocation-suggestion endpoint
    - The literal string "Gagal memuat saran alokasi. Cek nanti di halaman Pending." appears in the catch/error branch of that same fetch
    - `AllocationSuggestionModal` is imported and rendered conditionally on `allocation_suggestion_available`
  </acceptance_criteria>
  <done>Saving a side-income transaction shows the loading overlay with no timeout, then transitions directly to the modal on success or a toast on failure, exactly matching D-03/D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (TransactionForm) -> `POST /api/transactions` | User-controlled amount/category/wallet cross into a write whose type/label is meant to be entirely server-derived |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-09 | Tampering | Frontend accidentally constructing/sending `tipe_transaksi`/`source` | medium | mitigate | `TransactionForm.tsx`'s submit payload is built as an explicit object literal with exactly the 6 allowed keys — no spread of arbitrary form state that could smuggle in extra fields (D-01) |
| T-2-07 | Information Disclosure | JWT via `getToken()` | low | mitigate | Reuses existing Phase 1 token layer verbatim |

</threat_model>

<verification>
- `NEXT_PUBLIC_USE_MOCK=true npm run build` succeeds
- Grep-based check confirms zero `setTimeout`/`AbortController` in the allocation-suggestion fetch path
</verification>

<success_criteria>
- Transaction form collects exactly 3 required fields, never sends `tipe_transaksi`/`source`
- Save-sequencing exactly matches D-03/D-04: overlay -> modal or error toast, no client-side timeout
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-11-SUMMARY.md` when done
</output>
