---
phase: 02-core-product-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/dashboard/page.tsx
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "A logged-in user sees all 5 KPIs in the research-validated fixed order (expense breakdown, active goal progress, monthly trend, overspending alert, total balance) with total balance as the visually dominant card"
    - "Changing the period filter refetches all 5 KPIs together from a single request, not per-KPI independent filters"
    - "A user with no data yet sees section-scoped empty messages per KPI, not a blank/crashed page"
  artifacts:
    - apps/web/app/dashboard/page.tsx
  key_links:
    - "Period filter control -> single apiFetch('/api/dashboard?period=...') call -> all 5 KPI sections re-rendered from one response"
</must_haves>
---

<objective>
Build the Dashboard page rendering the 5 research-validated KPIs in their fixed order with the total-balance KPI as the primary visual anchor, plus a single period filter (this month / last month / custom range) that refetches everything together — against the existing `dashboard.json` mock, so this page is fully clickable before the backend dashboard aggregation endpoint exists.

Purpose: First vertical slice of Zarra's Dashboard/Home track; independent of every other Phase 2 plan (dashboard.json mock already exists and is fully scaffolded).
Output: A working, navigable Dashboard page.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `apps/web/app/dashboard/page.tsx` — `default function DashboardPage`
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product-loop/02-UI-SPEC.md
@.planning/phases/02-core-product-loop/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard KPI rendering — fixed order, total-balance focal card</name>
  <files>apps/web/app/dashboard/page.tsx</files>
  <read_first>apps/web/app/wallets/page.tsx, apps/web/lib/api/types.ts (`DashboardResponse` and its 5 nested KPI interfaces), apps/web/mocks/dashboard.json, .planning/phases/02-core-product-loop/02-UI-SPEC.md ("Dashboard (DASH-01, DASH-02)" section)</read_first>
  <action>
Create `apps/web/app/dashboard/page.tsx` following `wallets/page.tsx`'s structure exactly: `'use client'`, auth-guard-on-mount (`getToken()` -> redirect `/login` if absent), load `DashboardResponse` via `apiFetch<DashboardResponse>('/api/dashboard')`.

Render the 5 KPI sections in this exact top-to-bottom order — do not reorder even if a later Figma frame suggests otherwise; flag any such discrepancy back to the team instead of silently deviating (per UI-SPEC):
1. `expense_by_category` — list/bar rows showing `nama_kategori`, `total` (Rp-formatted), `pct`
2. `active_goals_summary` — cards showing `nama_goal` + `progress_pct` bar
3. `monthly_trend` — a simple table/list of `month`/`income`/`expense` rows (no charting library dependency — plain rows/bars using Tailwind widths is sufficient for MVP)
4. `overspending_alert` — a warning banner rendered only when `is_active` is true, showing `message`; renders nothing when `is_active` is false
5. `total_balance` — the single largest/most visually prominent card on the page (full-width or the largest grid cell), styled as the focal point per UI-SPEC even though it is rendered last in the fixed KPI order

Each KPI section gets its own section-scoped empty message when its data array/value is empty (D-06) — e.g., `expense_by_category.length === 0` shows "Belum ada pengeluaran" + "Belum ada transaksi pengeluaran bulan ini." + CTA "+ Tambah transaksi pertama" (navigates to `/transactions`); `active_goals_summary.length === 0` shows "Belum ada goal aktif" + "Buat goal pertama untuk mulai menabung." + CTA "+ Buat goal" (navigates to `/goals/new`). Never render zero CTAs or multiple competing CTAs within one empty state.
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/dashboard/page.tsx` exports `default function DashboardPage`
    - The 5 KPI JSX blocks appear in source order: `expense_by_category`, `active_goals_summary`, `monthly_trend`, `overspending_alert`, `total_balance` (grep for each key name and confirm line-number ordering)
    - `total_balance`'s rendering block uses a visually larger container class (e.g., a dedicated full-width/large card wrapper) than the other 4 KPI blocks
  </acceptance_criteria>
  <done>Visiting `/dashboard` (mock mode) renders all 5 KPIs in fixed order with total balance as the largest card, and an empty-data variant of the mock (each array emptied) shows the correct section-scoped empty copy with exactly one CTA per empty section.</done>
</task>

<task type="auto">
  <name>Task 2: Period filter control wired to a single combined refetch</name>
  <files>apps/web/app/dashboard/page.tsx</files>
  <read_first>apps/web/app/dashboard/page.tsx (as written in Task 1), API_CONTRACT.md (section 8, query params `?period=this_month|last_month|custom&start_date=&end_date=`)</read_first>
  <action>
Add a single period-filter control (segmented control or select: "Bulan ini" / "Bulan lalu" / custom date range) above the KPI sections. On change, call `apiFetch<DashboardResponse>('/api/dashboard?period=' + selected + (selected === 'custom' ? '&start_date=' + startDate + '&end_date=' + endDate : ''))` — one single request that updates state for all 5 KPI sections together; never issue 5 independent per-KPI requests. Default selection on mount: `this_month`. Show a lightweight loading indicator (not a full-page blocker) on the KPI area while the refetch is in flight, then swap in the new data atomically once resolved.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "apiFetch" apps/web/app/dashboard/page.tsx | grep -qE "^[2-9]|^[0-9]{2,}" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one period-filter control exists on the page (not 5 independent per-KPI filters)
    - The `onChange`/`onClick` handler for the period control calls `apiFetch` with a path containing `/api/dashboard?period=`
    - All 5 KPI state variables are updated from the single response object of that one call (no additional `apiFetch` calls introduced by this task)
  </acceptance_criteria>
  <done>Changing the period filter (this month / last month / custom range) triggers exactly one `GET /api/dashboard?period=...` request and all 5 KPIs update together from its response.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (Dashboard page) -> FastAPI `/api/dashboard` | User-controlled period/date-range query params cross to a read endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-07 | Information Disclosure | JWT via `getToken()` | low | mitigate | Reuses the existing Phase 1 `session.ts` token layer verbatim — no new token handling introduced |
| T-2-08 | Tampering | `custom` period date-range query params | low | accept | Client sends `start_date`/`end_date` as opaque strings; the backend (`02-14-PLAN.md`) is responsible for validating/bounding the date range server-side — this plan does not need to pre-validate since it has no security effect on the client alone (worst case is a malformed request returning a 4xx, handled by existing error-banner pattern) |

</threat_model>

<verification>
- `NEXT_PUBLIC_USE_MOCK=true npm run build` succeeds with no new TypeScript errors
- Toggling the period filter results in exactly one `apiFetch('/api/dashboard?period=...')` call updating all 5 KPI sections
</verification>

<success_criteria>
- Dashboard page renders all 5 KPIs in fixed, research-validated order with total balance as the focal card
- Single period filter refetches all KPIs together; section-scoped empty states render correctly per D-06
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-03-SUMMARY.md` when done
</output>
