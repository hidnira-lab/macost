---
phase: 02-core-product-loop
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/api/client.ts
  - apps/web/mocks/goal-detail.json
  - apps/web/mocks/goal-settings.json
  - apps/web/mocks/allocations-pending.json
  - apps/web/app/goals/page.tsx
autonomous: true
requirements: [GOAL-02, SAW-01, SAW-03]

must_haves:
  truths:
    - "A logged-in user sees their goals ranked by SAW priority (rank 1 first) on the Goals page, working entirely against mock data before the backend exists"
    - "The user can toggle between Quick Win and Importance-First and the toggle visibly reflects a selection (real reorder wiring lands with the real backend in a later plan, per D-05)"
    - "A user with zero goals sees a section-scoped empty message with exactly one CTA, not a blank page or crash"
  artifacts:
    - apps/web/app/goals/page.tsx
    - apps/web/mocks/goal-detail.json
    - apps/web/mocks/goal-settings.json
    - apps/web/mocks/allocations-pending.json
  key_links:
    - "apps/web/lib/api/client.ts resolveMock() -> new regex/exact cases for /api/goals/{id}, /api/goal-settings, /api/allocations/pending, consumed by this plan and by 02-07/02-08"
    - "apps/web/app/goals/page.tsx -> apiMutate PUT /api/goal-settings (D-05, real call even in mock mode) -> apiFetch GET /api/goals refetch"
</must_haves>
---

<objective>
Build the Goals list page (SAW-ranked cards, progress bars, Quick Win / Importance-First segmented toggle, section-scoped empty state) against the existing mock-first pattern, and extend `apps/web/lib/api/client.ts`'s `resolveMock()` with the 3 new mock routes this phase's Goals/Allocation pages need (`/api/goals/{id}`, `/api/goal-settings`, `/api/allocations/pending`) plus their backing JSON fixtures — so this and every later Khayyira-track plan can develop and demo without waiting on the backend.

Purpose: This is the first vertical slice of the Goals area (Khayyira's ownership) and the single place all 3 new mock plumbing pieces get added, so later plans (`02-07`, `02-08`) never need to touch `client.ts` again.
Output: A working, navigable Goals list page and 3 new mock fixtures + resolver cases.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `apps/web/lib/api/client.ts` — new `resolveMock()` branches for `/api/goals/{id}`, `/api/goal-settings`, `/api/allocations/pending`
- `apps/web/mocks/goal-detail.json`, `goal-settings.json`, `allocations-pending.json` (new files)
- `apps/web/app/goals/page.tsx` — `default function GoalsPage`
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product-loop/02-CONTEXT.md
@.planning/phases/02-core-product-loop/02-UI-SPEC.md
@.planning/phases/02-core-product-loop/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend client.ts mock resolver + add 3 new mock fixtures</name>
  <files>apps/web/lib/api/client.ts, apps/web/mocks/goal-detail.json, apps/web/mocks/goal-settings.json, apps/web/mocks/allocations-pending.json</files>
  <read_first>apps/web/lib/api/client.ts, apps/web/lib/api/types.ts, apps/web/mocks/goals.json, API_CONTRACT.md (sections 5, 6, 7)</read_first>
  <action>
In `apps/web/lib/api/client.ts`'s `resolveMock(path)`, add 3 new branches (following the existing exact-match/regex style already used for `/api/transactions/{id}/allocation-suggestion`):
1. A regex matching `/^\/api\/goals\/[^/]+$/` (single goal detail, e.g. `/api/goals/goal_001`) returning the new `goal-detail.json` import — place this check BEFORE the existing plain `/api/goals` exact-match branch so it does not shadow it.
2. An exact match for `/api/goal-settings` returning the new `goal-settings.json` import.
3. An exact match for `/api/allocations/pending` returning the new `allocations-pending.json` import.
Add the 3 corresponding static imports at the top of the file next to the existing `goalsData`/`walletsData` imports.

Create `apps/web/mocks/goal-detail.json` matching the `GoalDetailResponse` TypeScript interface (extends `Goal` with `allocation_history`): reuse `goal_001` ("Beli Laptop") fields from `goals.json` and add 2 `allocation_history` entries with `id_alokasi`, `nominal_alokasi`, `tanggal_alokasi`, `transaksi_id`.

Create `apps/web/mocks/goal-settings.json` matching `GoalSettings`: `{"strategy": "quick_win", "weights": {"personal_importance": 0.225, "progress_gap": 0.219, "saving_capacity": 0.215, "urgency": 0.178, "target_amount": 0.162}}` — the exact default weights from CLAUDE.md.

Create `apps/web/mocks/allocations-pending.json` matching `AllocationPendingResponse`: one pending entry with `transaksi_id`, `nominal`, `suggested_goal_name`, `expires_at`.
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "mocks/goal" ; echo "exit_ok"</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/lib/api/client.ts` contains a regex test for `/api/goals/` before the exact `/api/goals` match
    - `apps/web/lib/api/client.ts` contains exact-match branches for `/api/goal-settings` and `/api/allocations/pending`
    - All 3 new JSON files exist and their top-level shape matches the corresponding TypeScript interface in `apps/web/lib/api/types.ts` (`GoalDetailResponse`, `GoalSettings`, `AllocationPendingResponse`)
    - `npx tsc --noEmit` in `apps/web/` produces no new type errors referencing the 3 new mock files
  </acceptance_criteria>
  <done>`resolveMock()` correctly routes all 3 new paths; goal detail / goal settings / pending allocations pages (built in 02-07 and 02-08) can `apiFetch` them with zero further `client.ts` changes.</done>
</task>

<task type="auto">
  <name>Task 2: Goals list page — SAW-ranked cards + strategy toggle + empty state</name>
  <files>apps/web/app/goals/page.tsx</files>
  <read_first>apps/web/app/wallets/page.tsx, apps/web/lib/auth/session.ts, apps/web/lib/api/types.ts, .planning/phases/02-core-product-loop/02-UI-SPEC.md ("Goals list + SAW strategy toggle" and "Design System"/"Color" sections)</read_first>
  <action>
Create `apps/web/app/goals/page.tsx` as a client component following `wallets/page.tsx`'s exact structure: `'use client'`, auth-guard-on-mount via `getToken()` redirecting to `/login` if absent, `useState`/`useEffect`/`useCallback` for loading `GoalsResponse` via `apiFetch<GoalsResponse>('/api/goals')`.

Render each goal as a card (`bg-white/5 border border-white/10 rounded-2xl`) showing `nama_goal`, a progress bar driven by `progress_pct`, `nominal_terkumpul`/`nominal_target` formatted as `Rp {n.toLocaleString('id-ID')}`, and the `rank` badge — sorted by the array order already returned by the API (server is the source of ranking truth, do not re-sort client-side).

Add a 2-option segmented control (not a checkbox/dropdown) for "Quick Win" / "Importance-First", default-selected to `quick_win` per `goal_settings` table default. On toggle: call `apiMutate('/api/goal-settings', 'PUT', { strategy: <new value>, weights: <the weights value already held in state from the last GET /api/goal-settings> })` (D-05 — this is a real persisted preference change, not a client-only view toggle), then re-`apiFetch('/api/goals')` to get the freshly re-ranked list; show a brief dim/skeleton state on the goal list (not a full-page spinner) while this round-trip is in flight, per UI-SPEC. To have the current weights available for the PUT body, fetch `/api/goal-settings` once on mount alongside `/api/goals`.

Empty state (D-06, zero goals): render the exact copy from UI-SPEC's Copywriting Contract — heading "Belum ada goal", body "Buat goal pertama kamu dan mulai menabung menuju targetmu.", CTA "+ Buat Goal" navigating to `/goals/new` (created in `02-07-PLAN.md` — link now, page lands in the next plan). Provide a persistent "+ Buat Goal" CTA at the bottom of the page even when goals exist (mirrors the wallets page's "+ Tambah Dompet" pattern), navigating to `/goals/new`.

Apply Design System tokens from UI-SPEC: `bg-[#1e1e1e]` page background, `#ff8929` for the selected toggle segment and the primary CTA, `#298dff` reserved for inline links only (not used here yet), Helvetica/Neulis inline `style` font families matching the existing pages exactly (no shadcn — none installed, per UI-SPEC's shadcn Gate Decision).
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/goals/page.tsx` exports `default function GoalsPage`
    - Page redirects to `/login` when `getToken()` resolves to `null` (same pattern as `wallets/page.tsx` lines 36-46)
    - Toggling the segmented control triggers an `apiMutate` call to `/api/goal-settings` followed by an `apiFetch` call to `/api/goals` (grep the file for both calls)
    - With `NEXT_PUBLIC_USE_MOCK=true`, `next build` succeeds with no new TypeScript errors in this file
  </acceptance_criteria>
  <done>Visiting `/goals` (mock mode) shows the 3 mock goals ranked by `rank`, a working segmented toggle that calls the real PUT+refetch sequence, and the zero-goals empty state renders correctly when `goals: []` is substituted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (Goals page) -> FastAPI `/api/goal-settings` | User-controlled strategy toggle input crosses to a mutating endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-08 | Tampering | Goals page strategy toggle | low | accept | Client only ever sends `strategy: "quick_win" \| "importance_first"` from a fixed 2-option control (not free text) and re-sends the weights it already fetched — server-side validation of the weight-sum (T-2-03, enforced in `02-10-PLAN.md`) is the real authority regardless of what this page sends |
| T-2-07 | Information Disclosure | JWT via `getToken()` | low | mitigate | Reuses the existing Phase 1 `session.ts` token layer verbatim — no new token handling code introduced by this plan |

</threat_model>

<verification>
- `npx tsc --noEmit` in `apps/web/` shows no new errors
- `NEXT_PUBLIC_USE_MOCK=true npm run build` succeeds
</verification>

<success_criteria>
- Goals list page renders SAW-ranked mock goals, a working strategy toggle wired to the real PUT+refetch sequence, and a section-scoped empty state
- `client.ts` mock plumbing for goal detail / goal settings / pending allocations exists so no later Khayyira-track plan needs to touch it again
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-02-SUMMARY.md` when done
</output>
