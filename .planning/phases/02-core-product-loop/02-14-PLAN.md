---
phase: 02-core-product-loop
plan: 14
type: execute
wave: 5
depends_on: ["02-05", "02-09", "02-10", "02-12"]
files_modified:
  - backend/services/dashboard_service.py
  - backend/routers/dashboard.py
  - backend/main.py
  - backend/tests/test_dashboard.py
  - backend/tests/test_main_integration.py
autonomous: true
requirements: [DASH-01, DASH-02]

must_haves:
  truths:
    - "GET /api/dashboard returns the 5 KPIs in the exact fixed field order specified by API_CONTRACT.md, computed server-side, never client-reordered"
    - "Changing the period query param changes the result set for all 5 KPIs from a single request"
    - "Every Phase 2 router (categories, transactions, goals, goal_settings, allocations, dashboard) is reachable through the real backend.main:app, not just through isolated per-plan test clients"
  artifacts:
    - backend/services/dashboard_service.py
    - backend/routers/dashboard.py
  key_links:
    - "backend/main.py -> app.include_router(...) for all 6 Phase 2 routers, registered alongside the existing auth/wallets routers from Phase 1"
    - "dashboard_service.py reuses goal_service.fetch_and_rank_goals() (02-10-PLAN.md) for active_goals_summary — does not duplicate goal-progress computation"
</must_haves>
---

<objective>
Implement `GET /api/dashboard` (5 KPIs in fixed research-validated order, period filter) via a new `dashboard_service.py`, and — as this phase's final backend integration step — register all 6 Phase 2 routers (`categories`, `transactions`, `goals`, `goal_settings`, `allocations`, `dashboard`) into `backend/main.py` alongside the existing Phase 1 `auth`/`wallets` routers, with a smoke test confirming the fully-wired app boots and every new endpoint is reachable and auth-protected.

Purpose: Dashboard is the last feature plan and the natural point to centrally wire `main.py` once, avoiding the file-conflict cascade that would occur if every router-creating plan tried to edit `main.py` independently in parallel waves.
Output: Working `GET /api/dashboard`; a fully-wired `backend.main:app` with every Phase 2 endpoint reachable.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `backend/services/dashboard_service.py` — `compute_dashboard()`
- `backend/routers/dashboard.py` — `GET /dashboard`
- `backend/main.py` — registers `categories`, `transactions`, `goals`, `goal_settings`, `allocations`, `dashboard` routers (extends existing `auth`/`wallets` registration)
- `backend/tests/test_dashboard.py`, `backend/tests/test_main_integration.py`
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product-loop/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dashboard_service.py — 5-KPI aggregation, period filter</name>
  <files>backend/services/dashboard_service.py, backend/routers/dashboard.py, backend/tests/test_dashboard.py</files>
  <read_first>API_CONTRACT.md (section 8), apps/web/mocks/dashboard.json, .planning/phases/02-core-product-loop/02-RESEARCH.md ("Derived Fields" recommendation, "Pattern 2" batched aggregation), backend/services/goal_service.py (as written in 02-10-PLAN.md)</read_first>
  <behavior>
    - `compute_dashboard(user_id, period="this_month")` returns keys in exactly this order when serialized: `expense_by_category`, `active_goals_summary`, `monthly_trend`, `overspending_alert`, `total_balance`
    - `expense_by_category` sums `nominal` per `kategori_id` for `Pengeluaran` transactions within the selected period, with `pct` computed as each category's share of total period expense (rounded, summing to ~100)
    - `active_goals_summary` reuses `goal_service.fetch_and_rank_goals(user_id)`, filtered to goals with `progress_pct < 100`, reduced to `id_goal`/`nama_goal`/`progress_pct`
    - `monthly_trend` returns income/expense sums grouped by calendar month for the last 3 months regardless of the selected period filter (trend context is always 3 months)
    - `overspending_alert.is_active` is `True` when any category's current-period total exceeds that category's trailing-3-month average by more than 20%, with a Bahasa Indonesia `message` naming the category; `False` with `message: null` otherwise
    - `total_balance` is the SUM of `Pemasukan` minus `Pengeluaran` across ALL of the user's transactions (all-time, not period-scoped), consistent with the sum of individual wallet balances from `02-09-PLAN.md`
    - `compute_dashboard(user_id, period="custom", start_date=X, end_date=Y)` scopes `expense_by_category`/`total transactions in period` to that explicit range instead of a calendar month
  </behavior>
  <action>
Create `backend/services/dashboard_service.py` with `compute_dashboard(user_id: str, period: str = "this_month", start_date: str | None = None, end_date: str | None = None) -> dict`. Resolve the period into a concrete `(range_start, range_end)` date pair (`this_month`/`last_month` computed from `datetime.now(timezone.utc).date()`; `custom` uses the provided `start_date`/`end_date` directly). Fetch the user's `transaksi` rows within `[range_start, range_end]` in one query; compute `expense_by_category` and the overspending comparison from that set plus one additional query for the trailing-3-month window (or reuse a wider single fetch and filter in Python — prefer the simpler single-wider-fetch to avoid a 3rd query). Call `goal_service.fetch_and_rank_goals(user_id)` for `active_goals_summary`. Compute `total_balance` via one more query over ALL the user's transactions (all-time, unfiltered by period) summing Pemasukan minus Pengeluaran. Return the dict with keys inserted in the exact order listed in `<behavior>` (Python dict literal order is preserved by FastAPI's JSON serialization).

Create `backend/routers/dashboard.py`: `GET /dashboard` accepting `period: str = "this_month"`, `start_date: str | None = None`, `end_date: str | None = None` via `Query()`; calls `compute_dashboard` and returns its result directly.

Write RED-phase tests first for all 7 behaviors, confirm failing, implement to green.
  </action>
  <verify>
    <automated>cd /Users/macbookpro/macost && pytest backend/tests/test_dashboard.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_dashboard.py -x` passes all 7 behavior cases
    - The dict returned by `compute_dashboard` has keys in exactly the order: `expense_by_category`, `active_goals_summary`, `monthly_trend`, `overspending_alert`, `total_balance` (test asserts `list(result.keys())` equals this exact list)
    - `dashboard_service.py` imports and calls `goal_service.fetch_and_rank_goals` rather than re-implementing goal-progress computation
  </acceptance_criteria>
  <done>`GET /api/dashboard` returns all 5 KPIs in the correct fixed order with correct period-filter behavior, reusing the shared goal-ranking helper rather than duplicating it.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all 6 Phase 2 routers into main.py + full-app smoke test</name>
  <files>backend/main.py, backend/tests/test_main_integration.py</files>
  <read_first>backend/main.py, backend/routers/categories.py (02-05), backend/routers/transactions.py (02-05/02-09), backend/routers/goals.py (02-10), backend/routers/goal_settings.py (02-10), backend/routers/allocations.py (02-12), backend/routers/dashboard.py (as written in Task 1 above)</read_first>
  <action>
Modify `backend/main.py`: extend the existing `from backend.routers import auth, wallets` import to also import `categories, transactions, goals, goal_settings, allocations, dashboard`; add `app.include_router(X.router, prefix="/api")` for all 6, placed after the existing `auth`/`wallets` registrations, preserving the existing CORS middleware configuration unchanged (do not modify `allow_origins`/`allow_origin_regex` — no new origin is needed for this phase).

Create `backend/tests/test_main_integration.py` using `TestClient(app)` against the REAL `backend.main:app` (the only test file in this phase that imports the real app rather than a bare isolated `FastAPI()` instance) with `backend.core.supabase.get_supabase_admin` monkeypatched to the `fake_supabase_client` fixture: assert that an unauthenticated `GET` to each of `/api/categories`, `/api/transactions`, `/api/goals`, `/api/goal-settings`, `/api/allocations/pending`, `/api/dashboard` returns `401` (proves every new router is both registered AND still behind the shared JWT dependency — no accidentally-unprotected endpoint), and that `app.openapi()["paths"]` contains all 6 new path prefixes.
  </action>
  <verify>
    <automated>cd /Users/macbookpro/macost && pytest backend/tests/test_main_integration.py -x && pytest backend/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/main.py` registers all 6 new routers with `prefix="/api"`, alongside the unmodified `auth`/`wallets` registrations
    - `pytest backend/tests/test_main_integration.py -x` passes — every new endpoint returns `401` unauthenticated and is present in the OpenAPI schema
    - Full backend suite (`pytest backend/tests/ -q`) passes with zero failures
  </acceptance_criteria>
  <done>`backend.main:app` is fully wired for Phase 2 — every new endpoint is registered, reachable, and correctly requires authentication; the full backend test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `GET /api/dashboard` | Aggregated financial summary read, scoped per-user |
| `backend.main:app` (fully wired) -> all Phase 2 endpoints | Central point where a missing `Depends(get_current_user_id)` on any router would silently expose data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-01 | Information Disclosure | `GET /api/dashboard` cross-user leakage | high | mitigate | Every underlying query (`transaksi`, `goal` via `goal_service`) is scoped `.eq("id_pengguna", current_user_id)` |
| T-2-10 | Elevation of Privilege | Any Phase 2 router accidentally registered without its `Depends(get_current_user_id)` guard | critical | mitigate | `test_main_integration.py`'s 401-for-all-6-endpoints assertion is a regression guard specifically for this — a router missing the auth dependency would return `200`/`422` instead of `401` and fail this test immediately |

</threat_model>

<verification>
- `pytest backend/tests/ -q` passes (full suite, all Phase 2 backend plans combined)
</verification>

<success_criteria>
- Dashboard aggregation correct and in fixed KPI order (DASH-01/02)
- All 6 Phase 2 routers live in the real `backend.main:app`, every one still auth-protected
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-14-SUMMARY.md` when done
</output>
