---
phase: 02-core-product-loop
plan: 08
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - apps/web/components/AllocationSuggestionModal.tsx
  - apps/web/app/allocations/pending/page.tsx
autonomous: true
requirements: [ALLOC-03, ALLOC-04, ALLOC-05]

must_haves:
  truths:
    - "A self-contained modal component exists that shows a suggested goal + amount + percentage + up to 2 alternatives, and lets the user confirm or skip — never auto-executing"
    - "When no active goal exists, the modal shows a 'Buat goal dulu' prompt instead of a confirm/skip choice"
    - "A user can view all unresolved allocation suggestions on a dedicated page and act on any of them"
  artifacts:
    - apps/web/components/AllocationSuggestionModal.tsx
    - apps/web/app/allocations/pending/page.tsx
  key_links:
    - "AllocationSuggestionModal.tsx exposes props (open, suggestion, transaksiId, onClose, onResolved) that apps/web/app/transactions/page.tsx (02-11-PLAN.md) imports and wires into the D-03/D-04 save-transaction sequencing"
    - "AllocationSuggestionModal's internal Confirm/Skip actions call POST /api/allocations and POST /api/allocations/{id}/skip directly — the modal owns the mutation calls, callers only supply data + resolve callback"
</must_haves>
---

<objective>
Build the self-contained `AllocationSuggestionModal` component (Sitemap #16 — confirm/skip UI, "Buat goal dulu" fallback) and the Pending Allocations page (Sitemap #17), both against existing/newly-added mocks.

Purpose: The modal is the single UI artifact every allocation-related flow in this phase shares; building it as its own component with a crisp, documented prop interface lets `02-11-PLAN.md` (Zarra's transaction-save sequencing) import it without needing to know its internals.
Output: A reusable modal component + a working Pending Allocations page.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `apps/web/components/AllocationSuggestionModal.tsx` — `default function AllocationSuggestionModal`, props `{ open, suggestion, transaksiId, onClose, onResolved }`
- `apps/web/app/allocations/pending/page.tsx` — `default function` Pending Allocations page
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product-loop/02-CONTEXT.md
@.planning/phases/02-core-product-loop/02-UI-SPEC.md
@.planning/phases/02-core-product-loop/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AllocationSuggestionModal.tsx — self-contained confirm/skip modal</name>
  <files>apps/web/components/AllocationSuggestionModal.tsx</files>
  <read_first>apps/web/lib/api/types.ts (`AllocationSuggestionResponse`, `AllocationConfirmRequest`, `AllocationConfirmResponse`, `AllocationSkipResponse`), apps/web/mocks/allocation-suggestion.json, .planning/phases/02-core-product-loop/02-UI-SPEC.md ("Allocation suggestion flow" and Copywriting Contract sections), .planning/phases/02-core-product-loop/02-RESEARCH.md ("Frontend Integration Pattern" section)</read_first>
  <action>
Create `apps/web/components/AllocationSuggestionModal.tsx` exporting `default function AllocationSuggestionModal` with props: `open: boolean`, `suggestion: AllocationSuggestionResponse | null`, `transaksiId: string`, `onClose: () => void`, `onResolved: (result: 'confirmed' | 'skipped') => void`. This prop contract is a hard interface boundary — do not change these names/types without updating every caller (`02-11-PLAN.md`, `02-09` pending page in Task 2 below).

When `suggestion.has_active_goal === false`: render the "Buat goal dulu" fallback content exactly per UI-SPEC Copywriting Contract — heading "Belum ada goal aktif", body "Buat goal dulu supaya kami bisa menyarankan alokasi yang tepat.", CTA "Buat Goal" (navigates to `/goals/new`, then calls `onClose()`).

When `has_active_goal === true`: render `suggested_goal_name`, `suggested_amount` (Rp-formatted) + `suggested_pct`, and up to 2 `alternative_goals` (goal_name + rank). Two actions:
- Primary CTA "Konfirmasi Alokasi" (`#ff8929` filled button): calls `apiMutate<AllocationConfirmResponse>('/api/allocations', 'POST', { transaksi_id: transaksiId, goal_id: suggestion.suggested_goal_id, nominal_alokasi: suggestion.suggested_amount })`; on success call `onResolved('confirmed')` then `onClose()`; on failure show an inline error, keep the modal open so the user can retry.
- Secondary "Lewati" (text-link style, `#298dff`, NOT a filled button per UI-SPEC): calls `apiMutate<AllocationSkipResponse>('/api/allocations/' + transaksiId + '/skip', 'POST', null)`; on success call `onResolved('skipped')` then `onClose()`.

This component NEVER calls any endpoint on mount/automatically — both mutations only fire from an explicit user click on "Konfirmasi Alokasi" or "Lewati" (ALLOC-03's suggest-and-confirm invariant, never auto-execute).
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/components/AllocationSuggestionModal.tsx` exports a component accepting exactly `open, suggestion, transaksiId, onClose, onResolved` props
    - `apiMutate` calls to `/api/allocations` and `/api/allocations/{id}/skip` only appear inside click-handler functions, never inside a `useEffect` with an empty/mount-only dependency array (grep-based structural check: no `useEffect` in this file calls `apiMutate`)
    - The `has_active_goal === false` branch renders the literal string "Buat goal dulu supaya kami bisa menyarankan alokasi yang tepat."
  </acceptance_criteria>
  <done>The modal renders both suggestion and no-active-goal states correctly, and both mutations are 100% user-click-triggered, never automatic.</done>
</task>

<task type="auto">
  <name>Task 2: Pending Allocations page (Sitemap #17)</name>
  <files>apps/web/app/allocations/pending/page.tsx</files>
  <read_first>apps/web/app/wallets/page.tsx, apps/web/lib/api/types.ts (`AllocationPending`, `AllocationPendingResponse`), apps/web/mocks/allocations-pending.json (added in 02-02-PLAN.md), .planning/phases/02-core-product-loop/02-UI-SPEC.md (Copywriting Contract — Pending Allocations empty state)</read_first>
  <action>
Create `apps/web/app/allocations/pending/page.tsx`: auth-guard-on-mount, load via `apiFetch<AllocationPendingResponse>('/api/allocations/pending')`. Render each pending item (`nominal` Rp-formatted, `suggested_goal_name`, `expires_at`) as a clickable card.

On click, since `AllocationPending` does not carry the full suggestion (`suggested_amount`/`suggested_pct`/`alternative_goals`), re-fetch the full suggestion via `apiFetch<AllocationSuggestionResponse>('/api/transactions/' + item.transaksi_id + '/allocation-suggestion')`, then open the `AllocationSuggestionModal` (imported from `02-08-PLAN.md` Task 1) with `transaksiId={item.transaksi_id}` and the freshly-fetched `suggestion`. On `onResolved`, remove the resolved item from the local pending list and close the modal (no need to re-fetch the whole page).

Empty state (D-06, zero pending items): heading "Tidak ada saran tertunda", body "Semua saran alokasi sudah kamu proses." — no CTA (this is a resolved-state empty, not an onboarding empty, per UI-SPEC).
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `apps/web/app/allocations/pending/page.tsx` exports `default function` and imports `AllocationSuggestionModal` from `@/components/AllocationSuggestionModal`
    - Clicking a pending item triggers a `GET /api/transactions/{id}/allocation-suggestion` fetch before the modal opens
    - Empty state renders "Tidak ada saran tertunda" with zero CTA buttons (grep confirms no `<button>`/`<a>` inside the empty-state block)
  </acceptance_criteria>
  <done>Pending Allocations page lists unresolved suggestions, opens the shared modal on click with a freshly re-fetched suggestion, and shows the correct no-CTA empty state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser (Modal Confirm action) -> `POST /api/allocations` | The single most consequential mutation in this phase — moves money conceptually between a transaction and a goal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-05 | Elevation of Privilege / Tampering | Auto-execution of allocation without user consent | critical | mitigate | Both `POST /api/allocations` and `POST /api/allocations/{id}/skip` are wired exclusively to onClick handlers inside this modal — no `useEffect`, no auto-trigger on mount or on suggestion-fetch-success; this is the architectural non-negotiable (CLAUDE.md: "Smart Allocation selalu suggest & confirm, tidak pernah auto-execute") |
| T-2-07 | Information Disclosure | JWT via `getToken()` | low | mitigate | Reuses existing Phase 1 token layer verbatim |

</threat_model>

<verification>
- `NEXT_PUBLIC_USE_MOCK=true npm run build` succeeds
- Grep-based check confirms no `useEffect` in `AllocationSuggestionModal.tsx` calls `apiMutate`
</verification>

<success_criteria>
- Modal component has a stable, documented prop interface consumed by `02-11-PLAN.md`
- Pending Allocations page lists and resolves suggestions via the shared modal
- Confirm/skip are 100% user-click-triggered
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-08-SUMMARY.md` when done
</output>
