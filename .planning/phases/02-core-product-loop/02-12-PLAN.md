---
phase: 02-core-product-loop
plan: 12
type: execute
wave: 4
depends_on: ["02-10", "02-05"]
files_modified:
  - backend/models/allocation.py
  - backend/services/allocation_service.py
  - backend/routers/allocations.py
  - backend/tests/test_allocation_service.py
  - backend/tests/test_allocations.py
autonomous: true
requirements: [ALLOC-01, ALLOC-02, ALLOC-03, ALLOC-04, ALLOC-05]

must_haves:
  truths:
    - "Requesting an allocation suggestion for a side-income transaction with an active goal returns the top SAW-ranked goal, an amount between 29% and 40% of the transaction, and up to 2 alternatives"
    - "Requesting a suggestion for a user with zero goals returns has_active_goal=false, never a crash"
    - "Confirming an allocation only ever happens via an explicit POST to /api/allocations — no endpoint anywhere auto-inserts an alokasi row"
    - "Skipping or ignoring a suggestion makes it show up on GET /api/allocations/pending until it is confirmed"
  artifacts:
    - backend/services/allocation_service.py
    - backend/routers/allocations.py
  key_links:
    - "GET /api/transactions/{id}/allocation-suggestion -> goal_service.fetch_and_rank_goals() (02-10-PLAN.md) -> top-ranked goal + fixed 35% suggestion"
    - "GET /api/allocations/pending derives 'pending' implicitly: any Pemasukan/Flexible Side Income transaction with no matching alokasi.transaksi_id row, expires_at = created_at + 24h — no new table/column beyond the 6 already-pushed migrations (Open Question #1, Option b)"
</must_haves>
---

<objective>
Implement `allocation_service.py` (suggestion computation, fixed 35% per Assumption A5) and the 4 Allocations endpoints: `GET /api/transactions/{id}/allocation-suggestion`, `POST /api/allocations` (confirm), `POST /api/allocations/{id}/skip`, `GET /api/allocations/pending`.

Purpose: This is the backend half of the Smart Allocation flow — the core value proposition of the whole product. Pending state is derived implicitly (no new table) to stay within the "6 migration files only" schema scope locked in `02-01`/`02-06-PLAN.md`.
Output: Working, unit-tested allocation suggestion/confirm/skip/pending endpoints.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `backend/models/allocation.py` — `AllocationConfirmRequest`, `AllocationConfirmResponse`, `AllocationSkipResponse`, `AllocationPendingResponse`
- `backend/services/allocation_service.py` — module constant `SUGGESTED_PCT`, `get_allocation_suggestion()`
- `backend/routers/allocations.py` — `GET /transactions/{id}/allocation-suggestion`, `POST /allocations`, `POST /allocations/{id}/skip`, `GET /allocations/pending`
- `backend/tests/test_allocation_service.py`, `backend/tests/test_allocations.py`
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product-loop/02-RESEARCH.md
@.planning/phases/02-core-product-loop/02-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: allocation_service.py + GET allocation-suggestion (with re-validation guard)</name>
  <files>backend/models/allocation.py, backend/services/allocation_service.py, backend/routers/allocations.py, backend/tests/test_allocation_service.py</files>
  <read_first>backend/services/goal_service.py (as written in 02-10-PLAN.md), .planning/phases/02-core-product-loop/02-RESEARCH.md ("Full-flow allocation service sketch", "Open Question #3" re-validation guard), API_CONTRACT.md (section 7)</read_first>
  <behavior>
    - `get_allocation_suggestion(transaction, user_id)` for a user with 3 ranked goals returns `has_active_goal=True`, `suggested_goal_id` equal to the rank-1 goal's id, `suggested_amount == round(transaction["nominal"] * 0.35)`, `suggested_pct == 35`, and `alternative_goals` containing exactly the rank-2 and rank-3 goals
    - `get_allocation_suggestion(transaction, user_id)` for a user with 0 goals returns `{"has_active_goal": False}` only, no exception
    - `GET /api/transactions/{id}/allocation-suggestion` for a transaction that is NOT `Pemasukan`+`Flexible Side Income` returns a `400 VALIDATION_ERROR` (or `404`) instead of computing a nonsensical suggestion — the endpoint independently re-validates rather than trusting the caller only called it because `allocation_suggestion_available` was true at creation time
  </behavior>
  <action>
Create `backend/models/allocation.py`: `AllocationConfirmRequest(BaseModel)` with `transaksi_id: str`, `goal_id: str`, `nominal_alokasi: int = Field(gt=0)`; `AllocationConfirmResponse`, `AllocationSkipResponse`, `AllocationPendingResponse`/`AllocationPending` mirroring API_CONTRACT.md §7 shapes exactly.

Create `backend/services/allocation_service.py`: module constant `SUGGESTED_PCT = 35` (Assumption A5 — fixed default within the locked 29-40% ALLOC-02 range). Function `get_allocation_suggestion(transaction: dict, user_id: str) -> dict`: call `goal_service.fetch_and_rank_goals(user_id)`; if empty return `{"has_active_goal": False}`; else `top_goal = goals[0]`, `suggested_amount = round(transaction["nominal"] * SUGGESTED_PCT / 100)`, return the full shape including `alternative_goals` = the next 2 ranked goals (`goals[1:3]`, each reduced to `goal_id`/`goal_name`/`rank`).

Create `backend/routers/allocations.py` (this task only adds the suggestion GET; confirm/skip/pending land in Task 2): `GET /transactions/{transaction_id}/allocation-suggestion` — fetch the `transaksi` row scoped by both id and `id_pengguna` (404 if not found/not owned); re-validate `tipe_transaksi == "Pemasukan" and source_label == "Flexible Side Income"` — if not, raise `400` `{"error": {"code": "VALIDATION_ERROR", "message": "Transaksi ini bukan pemasukan side income"}}` (Open Question #3's recommended guard); else call `allocation_service.get_allocation_suggestion`.

Write RED-phase tests first for all 3 behaviors, confirm failing, implement to green.
  </action>
  <verify>
    <automated>cd /Users/macbookpro/macost && pytest backend/tests/test_allocation_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_allocation_service.py -x` passes all 3 cases
    - `suggested_amount` in every passing test equals `round(nominal * 0.35)` (grep-verifiable against the fixed 35% constant)
    - The suggestion-GET handler contains an explicit `tipe_transaksi`/`source_label` re-check before calling `get_allocation_suggestion`
  </acceptance_criteria>
  <done>Allocation suggestion computation is correct, uses the real SAW ranking, and the GET endpoint independently guards against being called on a non-side-income transaction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: POST confirm/skip + GET pending (implicit derivation, no new table)</name>
  <files>backend/routers/allocations.py, backend/tests/test_allocations.py</files>
  <read_first>backend/routers/allocations.py (as written in Task 1), .planning/phases/02-core-product-loop/02-RESEARCH.md ("Open Question #1" pending-state options, "Common Pitfalls" none additional), backend/routers/wallets.py (IDOR/structured-error conventions)</read_first>
  <behavior>
    - `POST /api/allocations` with a valid `{transaksi_id, goal_id, nominal_alokasi}` for the caller's own transaction/goal inserts one `alokasi` row and returns `201` with `goal_updated.nominal_terkumpul`/`progress_pct` reflecting the new total — and NO other endpoint in this file ever inserts into `alokasi` except this one, triggered only by this explicit call
    - `POST /api/allocations/{transaction_id}/skip` for a transaction that has no matching `alokasi` row returns `200` with `status: "skipped"` and `pending_until` computed as that transaction's `created_at + 24 hours` — this call writes NOTHING to the database (the "pending" state is already implicitly true the moment the transaction was created with no allocation; skip is a confirmation echo, not a new state transition)
    - `GET /api/allocations/pending` returns every one of the caller's `Pemasukan`+`Flexible Side Income` transactions that has NO matching row in `alokasi.transaksi_id`, each with `expires_at = created_at + 24 hours` — regardless of whether `/skip` was ever called for it (covers both the explicit-skip case AND the D-04 error-path case where the suggestion fetch failed and the user never saw the modal at all)
  </behavior>
  <action>
Add to `backend/routers/allocations.py`:

`POST /allocations` — validate the `transaksi_id` belongs to `current_user_id` and the `goal_id` belongs to `current_user_id` (404 otherwise); insert into `alokasi` with `id_pengguna=current_user_id`; re-fetch the goal's updated `nominal_terkumpul`/`progress_pct` via `goal_service.fetch_and_rank_goals` (or a lighter-weight direct SUM if performance matters, but correctness/consistency with `GET /api/goals` matters more at this scale); return the shape from API_CONTRACT.md §7 including `goal_updated`.

`POST /allocations/{transaction_id}/skip` — fetch the transaction (404 if not found/not owned), compute `pending_until = transaction["created_at"] + timedelta(hours=24)` in Python, return `{"status": "skipped", "pending_until": pending_until.isoformat()}` — document in a code comment exactly why this performs no DB write (pending state is already implicit; see `<behavior>` above and RESEARCH.md Open Question #1 Option b — this keeps schema scope to exactly the 6 migrations already pushed by Hidayat in `02-06-PLAN.md`, no 7th table/column needed).

`GET /allocations/pending` — fetch all `transaksi` rows for `current_user_id` where `tipe_transaksi='Pemasukan'` and `source_label='Flexible Side Income'`; batch-fetch `alokasi.transaksi_id` values for this user (`.in_("transaksi_id", ids)` — Pattern 2, no N+1); filter out any transaction whose id appears in that set; for the remainder, compute `expires_at = created_at + 24h` and return `{"pending": [...]}` matching `AllocationPendingResponse`.

Write RED-phase tests first for all 3 behaviors (seed fixture transaksi/alokasi rows exercising both the never-skipped and explicitly-skipped-but-not-confirmed cases — both must appear in `pending`), confirm failing, implement to green.
  </action>
  <verify>
    <automated>cd /Users/macbookpro/macost && pytest backend/tests/test_allocations.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/test_allocations.py -x` passes all 3 cases
    - The ONLY `.table("alokasi").insert(` call in the entire `backend/` tree is inside the `POST /allocations` handler (grep confirms exactly 1 occurrence across `backend/routers/` and `backend/services/`)
    - `POST /allocations/{id}/skip` contains no `.insert(`/`.update(` call (grep confirms it is read-only + computed response)
    - `GET /allocations/pending` uses `.in_("transaksi_id", ...)` for the alokasi batch-fetch, not a per-transaction loop
  </acceptance_criteria>
  <done>Confirm is the sole allocation-writing path in the entire backend; skip is a pure read+compute; pending correctly derives from the absence of an alokasi row, covering both explicit-skip and silent-error cases per D-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `POST /api/allocations` | The single money-moving mutation in this entire phase |
| Browser -> `GET /api/allocations/pending` | Read of financially-sensitive unresolved suggestions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-05 | Elevation of Privilege / Tampering | Any code path other than explicit `POST /api/allocations` inserting into `alokasi` | critical | mitigate | Verified by acceptance criteria: exactly 1 `.insert(` call into `alokasi` exists in the whole backend tree, and it only fires from this one explicit, user-confirmed endpoint |
| T-2-01 | Tampering / Information Disclosure | IDOR on `transaksi_id`/`goal_id` in `POST /api/allocations` | high | mitigate | Both ids are independently verified to belong to `current_user_id` before the insert — a user cannot allocate another user's transaction into their own goal, or vice versa |
| T-2-04 | Tampering -> Denial of Service | `nominal_alokasi <= 0` | medium | mitigate | Pydantic `Field(gt=0)` on `AllocationConfirmRequest` |

</threat_model>

<verification>
- `pytest backend/tests/test_allocation_service.py backend/tests/test_allocations.py -x` passes
</verification>

<success_criteria>
- Suggestion computation returns 29-40% (fixed 35%) with correct top-ranked goal + alternatives (ALLOC-01/02)
- Confirm is the sole write path (never auto-executed) (ALLOC-03)
- Skip/pending derive correctly without a 7th schema file (ALLOC-04/05)
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-12-SUMMARY.md` when done
</output>
