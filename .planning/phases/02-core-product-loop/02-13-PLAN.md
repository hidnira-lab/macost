---
phase: 02-core-product-loop
plan: 13
type: execute
wave: 4
depends_on: ["02-11"]
files_modified:
  - apps/web/app/transactions/page.tsx
  - apps/web/components/TransactionForm.tsx
autonomous: true
requirements: [TRAN-03, TRAN-04, TRAN-05]

must_haves:
  truths:
    - "A user can view, filter (date range/category/source), and page through their transaction history"
    - "A user can edit or delete a saved transaction using icon-only 44x44px minimum touch targets with matching aria-labels"
    - "After any create/edit/delete, wallet balances visibly refresh to reflect the new derived-SUM saldo"
    - "A user with zero transactions sees a section-scoped empty state with exactly one CTA"
  artifacts:
    - apps/web/app/transactions/page.tsx
  key_links:
    - "Delete/edit/create transaction success -> apiFetch('/api/wallets') refetch -> wallet balance display updates (consumes 02-09-PLAN.md's derived-SUM saldo)"
</must_haves>
---

<objective>
Extend the Transactions page (created in `02-11-PLAN.md`) with the transaction history list (filters, pagination), edit and delete actions, and a wallet-balance refresh trigger after any mutation — completing TRAN-03/04/05 on the frontend.

Purpose: Second half of Zarra's Transactions vertical slice; makes "wallet balance updates after transaction" (explicit ROADMAP.md ownership item) visibly true.
Output: A complete Transactions page — entry form, history, filter, edit, delete, live wallet balance.
</objective>

<artifacts_produced>
## Artifacts this phase produces
- `apps/web/app/transactions/page.tsx` — extended with history list, filters, pagination, edit/delete actions, wallet-balance refresh
- `apps/web/components/TransactionForm.tsx` — extended with `initialValues`/`transactionId` props for edit mode
</artifacts_produced>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-product-loop/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transaction history — list, filters, pagination</name>
  <files>apps/web/app/transactions/page.tsx</files>
  <read_first>apps/web/app/transactions/page.tsx (as written in 02-11-PLAN.md), apps/web/app/wallets/page.tsx, apps/web/lib/api/types.ts (`Transaction`, `TransactionsResponse`), apps/web/mocks/transactions.json, API_CONTRACT.md (section 4, GET query params)</read_first>
  <action>
Extend the transaction-list placeholder in `apps/web/app/transactions/page.tsx`: load via `apiFetch<TransactionsResponse>('/api/transactions' + queryString)` where `queryString` is built from filter state (`start_date`, `end_date`, `category_id`, `source`, `page`, `limit=20`). Render each transaction as a row: `tanggal_transaksi`, category name (resolve `kategori_id` against the already-fetched categories list from Task 1 of `02-11-PLAN.md`), `nominal` (Rp-formatted, colored green for Pemasukan / neutral for Pengeluaran), `source_label` badge when present.

Add filter controls above the list: date range (start/end), category select (reuse the categories fetched for the form), source select (`Fixed Routine` / `Flexible Side Income` / all). Changing any filter re-fetches the list with `page` reset to 1. Add simple pagination controls (prev/next) driven by the response's `total`/`page` fields.

Empty state (D-06, zero transactions matching current filters): heading "Belum ada transaksi", body "Catat transaksi pertamamu untuk mulai melacak keuanganmu.", CTA "+ Tambah Transaksi" (scrolls to / focuses the transaction form above).
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Changing any filter control resets `page` to `1` before re-fetching (grep confirms a state-reset line adjacent to the filter change handlers)
    - Empty state renders exactly one CTA when the filtered list is empty
    - `NEXT_PUBLIC_USE_MOCK=true npm run build` succeeds
  </acceptance_criteria>
  <done>Transaction history renders with working filters and pagination against the mock data, with a correct section-scoped empty state.</done>
</task>

<task type="auto">
  <name>Task 2: Edit/Delete actions (icon buttons, 44px touch target) + wallet balance refresh</name>
  <files>apps/web/app/transactions/page.tsx, apps/web/components/TransactionForm.tsx</files>
  <read_first>apps/web/app/wallets/page.tsx (delete-with-confirm pattern lines 89-98), apps/web/components/TransactionForm.tsx (as written in 02-11-PLAN.md), .planning/phases/02-core-product-loop/02-UI-SPEC.md ("Spacing Scale" 44px touch-target exception, Copywriting Contract delete-transaction string)</read_first>
  <action>
Add edit and delete icon-only buttons to each transaction row. Per UI-SPEC's spacing exception: each icon-only button must have a minimum 44x44px hit area regardless of the visual icon's size, and MUST carry an `aria-label` matching its text-equivalent action exactly (`aria-label="Edit transaksi"`, `aria-label="Hapus transaksi"`) — use inline SVG icons (no new npm icon-library dependency; UI-SPEC leaves this as an open choice and inline SVG avoids adding scope/a new package-legitimacy review for a cosmetic-only need).

Edit: clicking opens `TransactionForm` in edit mode (pass `initialValues` from the clicked row + `transactionId`) — extend `TransactionForm.tsx` to accept an optional `initialValues: Partial<TransactionCreateRequest>` and `transactionId?: string` prop; when `transactionId` is present, the parent page's submit handler calls `apiMutate('/api/transactions/' + transactionId, 'PUT', payload)` instead of `POST`.

Delete: `window.confirm('Hapus transaksi ini? Tindakan ini tidak dapat dibatalkan.')` (exact copy, matches the existing `wallets/page.tsx` pattern) then `apiMutate('/api/transactions/' + id, 'DELETE', null)`; remove the row from local state on success.

After ANY successful create (already exists from `02-11-PLAN.md`), edit, or delete: re-fetch `GET /api/wallets` and update whatever wallet-balance display exists on this page (or a shared layout element, if wallet balances are shown elsewhere) so the derived-SUM `saldo` from `02-09-PLAN.md`'s backend change is visibly reflected without a full page reload.
  </action>
  <verify>
    <automated>cd apps/web && NEXT_PUBLIC_USE_MOCK=true npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Both icon-only buttons have `aria-label` attributes with the exact strings "Edit transaksi" / "Hapus transaksi"
    - Both icon-only buttons have a class/style enforcing at least 44x44px hit area (e.g. `min-w-[44px] min-h-[44px]`)
    - A successful create/edit/delete is followed by an `apiFetch('/api/wallets')` call in the same handler (grep confirms all 3 handlers reference `/api/wallets`)
  </acceptance_criteria>
  <done>Edit/delete work correctly with accessible 44px icon buttons, and wallet balances visibly refresh after every transaction mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> `PUT`/`DELETE /api/transactions/{id}` | User-controlled transaction id crosses into mutating endpoints (backend IDOR guard already covered by `02-09-PLAN.md`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-2-07 | Information Disclosure | JWT via `getToken()` | low | mitigate | Reuses existing Phase 1 token layer verbatim |
| T-2-08 | Tampering | Client-side filter/pagination params | low | accept | Backend (`02-05`/`02-09-PLAN.md`) is the authority on scoping results to `id_pengguna`; malformed client filter values at worst produce an empty/4xx result, not a security issue |

</threat_model>

<verification>
- `NEXT_PUBLIC_USE_MOCK=true npm run build` succeeds
- Grep-based checks confirm 44px touch targets + aria-labels + wallet-refresh-after-mutation
</verification>

<success_criteria>
- Transaction history, filters, pagination, edit, delete all work correctly (TRAN-03/04/05)
- Wallet balances visibly refresh after every transaction mutation
</success_criteria>

<output>
Create `.planning/phases/02-core-product-loop/02-13-SUMMARY.md` when done
</output>
