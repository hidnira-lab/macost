---
phase: 01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte
verified: 2026-07-04T16:55:00Z
status: passed
score: 12/12 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 10/12
  gaps_closed:
    - "Project documentation (.claude/CLAUDE.md, API_CONTRACT.md) accurately reflects the real, live deployment setup"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Confirm backend/.env (local) and the Railway dashboard's 4 Supabase env vars (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY, SUPABASE_JWT_SECRET) hold identical real values (shared Supabase project for local + prod, per phase goal)"
    expected: "Local and Railway dashboard values are byte-identical for all 4 vars"
    result: "RESOLVED (2026-07-05, user-confirmed by Hidayat): local backend/.env holds 3 of the 4 vars (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY), matching Railway. SUPABASE_JWT_SECRET is intentionally absent locally — it is not used by the code (auth.py verifies via Supabase's JWKS endpoint, not a shared HS256 secret, since commit ebadf7c). The 3 vars that are actually read by the code are confirmed to match."
---

# Phase 01.1: Local dev & deployment infra Verification Report

**Phase Goal:** A team member can run `docker compose up` and get backend+frontend running locally against the shared Supabase project, unblocking the Phase 1 UAT flows (register/login/wallets); both Vercel (frontend) and Railway (backend) auto-deploy on every push to `main`.
**Verified:** 2026-07-04 (human verification item resolved 2026-07-05)
**Status:** passed
**Re-verification:** Yes — after gap closure (previous report dated 2026-07-03, status `gaps_found`, stale as of this run)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `docker-compose.yml` defines backend (8000) + frontend (3000), no local Postgres/Supabase container | ✓ VERIFIED (regression) | `docker-compose.yml` still present, unchanged; no `db`/`postgres` service |
| 2 | Backend/frontend Dockerfiles build correctly (repo-root context, anonymous `node_modules` volume) | ✓ VERIFIED (regression) | `backend/Dockerfile`, `apps/web/Dockerfile` both present and unchanged since prior verification |
| 3 | `backend/.env.example` / `apps/web/.env.example` document the exact var names the code reads (D-09) | ✓ VERIFIED (regression) | `git ls-files` confirms both files still tracked; no change since prior verification |
| 4 | `apps/web/.env.example` is git-trackable despite the blanket `.env*` gitignore pattern | ✓ VERIFIED (regression) | Confirmed via `git ls-files` |
| 5 | Backend CORS allows the deployed Vercel origin (literal + `*.vercel.app` preview regex) while pre-existing origins still work | ✓ VERIFIED (regression) | `backend/main.py` lines 11-19: `"https://macost.vercel.app"` + `allow_origin_regex=r"https://macost.*\.vercel\.app"` still present, unchanged |
| 6 | No secret value hardcoded in any committed deploy config | ✓ VERIFIED (regression) | `render.yaml` still absent; no plaintext secrets found in Dockerfiles/compose/main.py |
| 7 | Pushing to `main` triggers an automatic Vercel deployment of `apps/web`, reachable over HTTPS | ✓ VERIFIED (live, this session) | `curl -sf -m 8 -I https://macost.vercel.app/` → `HTTP/1.1 200 OK` (network-reachable this session, stronger evidence than the prior no-network-access verification) |
| 8 | Pushing to `main` triggers an automatic Railway deployment of `backend/`, `/health` responds 200 on the live prod URL | ✓ VERIFIED (live, this session) | `curl -sf -m 8 https://macost-production.up.railway.app/health` → `{"status":"ok"}` |
| 9 | The deployed Vercel origin can call the deployed backend without a browser CORS error | ✓ VERIFIED (live, this session) | `curl -H "Origin: https://macost.vercel.app" -I https://macost-production.up.railway.app/health` → `access-control-allow-origin: https://macost.vercel.app` |
| 10 | Backend stays warm via an UptimeRobot monitor pinging `/health` under 15 minutes, surviving both GET and HEAD probes | ✓ VERIFIED (regression) | `backend/main.py` line 34: `@app.api_route("/health", methods=["GET", "HEAD"])` unchanged; UptimeRobot dashboard status not independently re-checkable, but PANDUAN_TEKNIKAL_TIM.md (2026-07-04) reconfirms "status dashboard 'Up'" |
| 11 | Local `backend/.env` and Railway dashboard env vars point at the same shared Supabase project | ✓ VERIFIED (human-confirmed 2026-07-05) | Hidayat confirmed local `backend/.env` holds the 3 vars the code actually reads (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ANON_KEY`), matching Railway. `SUPABASE_JWT_SECRET` is intentionally not set locally since it's unused (JWKS-based verification, commit `ebadf7c`) |
| 12 | Project documentation (`.claude/CLAUDE.md`, `API_CONTRACT.md`) accurately reflects the real, live deployment setup | ✓ VERIFIED — gap closed | Both files re-read in full this session. `.claude/CLAUDE.md` line 69: "Backend base URL dev: `http://localhost:8000`; prod: `https://macost-production.up.railway.app` (Railway, live)". Line 80: "Backend: Railway ... Live at `https://macost-production.up.railway.app`". Zero remaining occurrences of "pending first deploy" anywhere in the repo (`grep -rln "pending first deploy" .` → empty). `API_CONTRACT.md` line 5: "**Base URL (prod):** `https://macost-production.up.railway.app` (Railway, live)" — the dead `macost-api.onrender.com` Render domain is gone from both files. Confirmed via `git show 03d185c` diff: this exact commit made both edits, and it predates (not postdates) the stale VERIFICATION.md commit `0a918d3`, per PANDUAN_TEKNIKAL_TIM.md's own documented git-order note |

**Score:** 12/12 truths verified. Previous run: 10/12 (1 failed, 1 uncertain).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `docker-compose.yml` | Two services, backend+frontend, no DB container | ✓ VERIFIED | Unchanged since prior verification |
| `backend/Dockerfile` | `python:3.12-slim`, repo-root COPY paths, `${PORT:-8000}` | ✓ VERIFIED | Unchanged |
| `apps/web/Dockerfile` | `node:20-alpine`, `npm ci`, `npm run dev` | ✓ VERIFIED | Unchanged |
| `.dockerignore` / `apps/web/.dockerignore` | Mirror respective `.gitignore` + `.git`/`Dockerfile*` | ✓ VERIFIED | Unchanged |
| `backend/.env.example` / `apps/web/.env.example` | Code-verified var names, placeholders only | ✓ VERIFIED | Still tracked, unchanged |
| `apps/web/.gitignore` | `!.env.example` negation added | ✓ VERIFIED | Unchanged |
| `backend/main.py` | CORS + `/health` GET+HEAD | ✓ VERIFIED | Unchanged, both re-confirmed by grep this session |
| `render.yaml` | N/A — intentionally deleted | ✓ VERIFIED (by design) | Still absent |
| `.claude/CLAUDE.md` | Documents Vercel/Railway/docker-compose accurately, including live status | ✓ VERIFIED (was ⚠️ STALE CONTENT — gap now closed) | Both "pending first deploy" lines rewritten to the live Railway URL in commit `03d185c` |
| `API_CONTRACT.md` | Prod Base URL matches the live backend | ✓ VERIFIED (was stale Render URL — gap now closed) | Line 5 now reads `https://macost-production.up.railway.app`, confirmed by direct read of the current file |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `docker-compose.yml` backend service | `backend/Dockerfile` | `build.context: .` + `dockerfile: backend/Dockerfile` | ✓ WIRED | Unchanged |
| `docker-compose.yml` frontend service | `apps/web/Dockerfile` | `build.context: ./apps/web` | ✓ WIRED | Unchanged |
| Vercel dashboard Root Directory | `apps/web/` static export | GitHub push → build | ✓ WIRED (live, this session) | `curl -I https://macost.vercel.app/` → 200, run directly by this verifier |
| Railway deploy | `backend/main.py` `/health` | GitHub push → auto-deploy → Dockerfile build | ✓ WIRED (live, this session) | `curl https://macost-production.up.railway.app/health` → `{"status":"ok"}`, run directly by this verifier |
| Deployed Vercel origin | Deployed Railway backend | CORS `allow_origin_regex` | ✓ WIRED (live, this session) | `curl -H "Origin: https://macost.vercel.app" -I .../health` → matching `access-control-allow-origin` header, run directly by this verifier |
| `.claude/CLAUDE.md` / `API_CONTRACT.md` | Live Railway URL | Direct text reference | ✓ WIRED — gap closed | Both files now cite `https://macost-production.up.railway.app`; confirmed by direct read of current file content (not by trusting SUMMARY.md or commit message wording) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| INFRA-01 | 01.1-01 | docker-compose.yml, both Dockerfiles, no local Postgres | ✓ SATISFIED | Truths #1, #2 |
| INFRA-02 | 01.1-01 | `.env.example` files, code-verified names, git-trackable | ✓ SATISFIED | Truths #3, #4 |
| INFRA-03 | 01.1-03 | Vercel deployment of `apps/web` static export | ✓ SATISFIED | Truth #7 (live curl, this session) |
| INFRA-04 | 01.1-02, 01.1-03 | Backend deploy (Render→Railway pivot), reachable `/health` | ✓ SATISFIED | Truth #8 (live curl, this session) |
| INFRA-05 | 01.1-02, 01.1-03 | Auto-deploy on push to `main`, no staging, both services | ✓ SATISFIED | Truths #7, #8 |
| INFRA-06 | 01.1-02, 01.1-03 | Backend CORS accepts the deployed Vercel origin | ✓ SATISFIED | Truth #9 (live curl, this session) |

No formal `REQUIREMENTS.md` entries exist for `INFRA-*` (re-confirmed this session — `grep INFRA .planning/REQUIREMENTS.md` returns nothing). All 6 informal IDs remain satisfied; none orphaned.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.planning/codebase/STACK.md`, `.planning/codebase/INTEGRATIONS.md`, `.planning/codebase/ARCHITECTURE.md` | various | Still reference the dead `macost-api.onrender.com` Render domain | ℹ️ Info (not a blocker) | These are internal `/gsd-map-codebase` snapshot docs, not the team-facing living contracts (`.claude/CLAUDE.md`, `API_CONTRACT.md`) that the original gap flagged and that project `CLAUDE.md` instructs the team to "SELALU rujuk." `.claude/CLAUDE.md`'s "Technology Stack" section carries a `source:codebase/STACK.md` marker, so if that file is ever regenerated from `.planning/codebase/STACK.md` without re-running the fix, the stale URL could resurface. Not in scope of this phase's declared gap and not blocking the phase goal, but worth a follow-up doc-sync task |
| `backend/.env.example` | 4 | Still lists `SUPABASE_JWT_SECRET=your-jwt-secret-here` as a required var | ℹ️ Info (not a blocker) | `backend/dependencies/auth.py` no longer reads `SUPABASE_JWT_SECRET` anywhere (confirmed via grep) — JWT verification moved to Supabase's JWKS endpoint in commit `ebadf7c`. Surfaced during the 2026-07-05 human-verification pass when Hidayat noted local `.env` intentionally omits this var. Not blocking (the var being unset locally causes no failure), but the `.env.example` placeholder is misleading to a new team member setting up local dev. Worth a follow-up doc-sync task, same category as the STACK.md/ARCHITECTURE.md note above |

No `TBD`/`FIXME`/`XXX` markers found in the two previously-flagged files (`.claude/CLAUDE.md`, `API_CONTRACT.md`) or any phase-modified infra file. No remaining occurrences of "pending first deploy" anywhere in the repo. No stub/placeholder-render patterns found — this is an infra/docs phase with no UI components.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Railway backend `/health` is live | `curl -sf -m 8 https://macost-production.up.railway.app/health` | `{"status":"ok"}` | ✓ PASS |
| Vercel frontend is live | `curl -sf -m 8 -I https://macost.vercel.app/` | `HTTP/1.1 200 OK` | ✓ PASS |
| CORS allows the live Vercel origin on the live Railway backend | `curl -H "Origin: https://macost.vercel.app" -I .../health` | `access-control-allow-origin: https://macost.vercel.app` | ✓ PASS |
| No "pending first deploy" text remains anywhere in the repo | `grep -rln "pending first deploy" .` | Empty output | ✓ PASS |
| No `onrender.com` reference remains in `.claude/CLAUDE.md` or `API_CONTRACT.md` | `grep -n "onrender" .claude/CLAUDE.md API_CONTRACT.md` | No match | ✓ PASS |

This session had direct network access (unlike the prior verification run), so all three live-deployment claims were independently re-executed rather than accepted on SUMMARY.md's word alone.

## Gaps Summary

Both gaps from the stale 2026-07-03 `01.1-VERIFICATION.md` are confirmed closed by direct re-reading of the current file contents (not by trusting the commit message or SUMMARY.md):

1. **`.claude/CLAUDE.md` "pending first deploy" wording** — both occurrences (Configuration section, Platform Requirements section) now state the live Railway URL. Confirmed via direct `Read` of the current file and a repo-wide grep for the literal stale phrase (zero hits).
2. **`API_CONTRACT.md` dead Render URL** — the prod Base URL now reads `https://macost-production.up.railway.app`, confirmed via direct `Read` of the current file.

Both fixes trace to commit `03d185c` ("docs: fix stale Railway URL references from Phase 01.1 verification"), verified via `git show 03d185c` diff output rather than assumed from the commit title.

No new gaps were introduced. A minor, out-of-scope observation: three `.planning/codebase/*.md` snapshot docs (`STACK.md`, `INTEGRATIONS.md`, `ARCHITECTURE.md`) still cite the dead Render domain. These are not the artifacts the original gap named, are not consulted directly by the team per project `CLAUDE.md`, and do not block the phase goal — flagged as an informational note only, not a gap.

The pre-existing human-verification requirement (local `backend/.env` vs. Railway dashboard Supabase secret parity) was resolved 2026-07-05: Hidayat confirmed the 3 vars actually read by the code (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ANON_KEY`) match between local and Railway. `SUPABASE_JWT_SECRET` is absent locally by design, since JWT verification no longer uses it (JWKS-based, commit `ebadf7c`) — flagged as a minor `.env.example` doc-staleness anti-pattern above, not a gap.

**Overall status: `passed`** — the phase's core technical deliverable (docker-compose local dev, dual Vercel+Railway auto-deploy, CORS, and now-accurate documentation) is fully verified with live evidence gathered directly in this session, and the one outstanding human-verification item is now confirmed.

---
*Verified: 2026-07-04*
*Verifier: Claude (gsd-verifier)*
