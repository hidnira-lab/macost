---
phase: 01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte
plan: 2
subsystem: infra
tags: [fastapi, cors, render, deployment, blueprint]

# Dependency graph
requires:
  - phase: 01.1 (Plan 01)
    provides: backend/.env.example and env var naming corrections used by render.yaml's envVars list
provides:
  - backend/main.py CORS allow-list accepting the deployed Vercel origin (prod + preview URLs)
  - render.yaml Blueprint enabling zero-edit Render "New Blueprint" GitHub connect for the FastAPI backend
affects: [01.1-03 (Render/Vercel/Supabase manual connect), Phase 2 integration testing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "render.yaml Blueprint pattern: rootDir left unset (repo root context) so `backend.main:app` and `from backend.routers import ...` absolute imports resolve; buildCommand/startCommand always reference paths relative to repo root"
    - "CORS origin coverage: explicit allow_origins list for exact known origins + scoped allow_origin_regex for a controlled subdomain pattern (never a bare wildcard when allow_credentials=True)"

key-files:
  created: [render.yaml]
  modified: [backend/main.py]

key-decisions:
  - "Used https://macost.vercel.app as a best-guess canonical prod origin (per D-03/D-06) — flagged for a follow-up one-line edit once Plan 01.1-03's Render/Vercel connect reports the actual assigned domain"
  - "render.yaml rootDir left unset (not set to backend/) so the build context stays at repo root, matching backend/main.py's `from backend.routers import auth, wallets` absolute import namespace (Pitfall 5)"
  - "All 4 Supabase/JWT env vars in render.yaml declared with sync: false — no secret values committed; real values entered via Render dashboard in Plan 01.1-03"

patterns-established:
  - "Vercel preview URL coverage via allow_origin_regex=r\"https://macost.*\\.vercel\\.app\" instead of enumerating individual preview URLs"

requirements-completed: [INFRA-04, INFRA-06]

coverage:
  - id: D1
    description: "Backend CORS allow-list accepts the deployed Vercel frontend origin (prod literal + scoped preview regex) while all 4 pre-existing Tauri/localhost origins remain intact"
    requirement: "INFRA-06"
    verification:
      - kind: unit
        ref: "python -c \"import ast; ast.parse(open('backend/main.py').read())\" && grep -c 'macost.vercel.app' backend/main.py && grep -c 'allow_origin_regex' backend/main.py"
        status: pass
    human_judgment: false
  - id: D2
    description: "render.yaml Blueprint at repo root targets backend.main:app via $PORT, wires healthCheckPath to /health, and declares all 4 Supabase/JWT secrets with sync: false (no rootDir override, no hardcoded port)"
    requirement: "INFRA-04"
    verification:
      - kind: unit
        ref: "grep -c '\\$PORT' render.yaml && grep -c 'sync: false' render.yaml && grep -c 'healthCheckPath: /health' render.yaml && grep -c 'backend/requirements.txt' render.yaml"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-07-02
status: complete
---

# Phase 01.1 Plan 2: Backend CORS + Render Blueprint Summary

**Vercel origin added to FastAPI CORS allow-list (literal + scoped preview regex) and a git-tracked render.yaml Blueprint created so Render's "New Blueprint" connect deploys backend/ without a ModuleNotFoundError or a hardcoded port**

## Performance

- **Duration:** 12 min
- **Tasks:** 2 completed
- **Files modified:** 2 (1 created, 1 modified)

## Accomplishments
- `backend/main.py`'s `CORSMiddleware` now accepts `https://macost.vercel.app` plus any `https://macost*.vercel.app` preview deployment, with all 4 pre-existing Tauri/localhost origins left byte-identical and the middleware registration order unchanged (still before both `app.include_router` calls)
- `render.yaml` created at repo root: `type: web`, `runtime: python`, `plan: free`, `buildCommand: pip install -r backend/requirements.txt`, `startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT` (dynamic port, never hardcoded 8000), `healthCheckPath: /health`, `rootDir` left unset so the `backend.` import namespace resolves correctly, and 4 Supabase/JWT secret env vars declared with `sync: false`

## Task Commits

Each task was committed atomically:

1. **Task 1: Add the Vercel origin to backend CORS** - `7e1ac09` (feat)
2. **Task 2: render.yaml Blueprint for the backend** - `44dfaa3` (feat)

_No TDD tasks in this plan — both were config/plain-code edits with `<verify><automated>` shell checks, not test-file-driven._

## Files Created/Modified
- `backend/main.py` - Added `https://macost.vercel.app` as a 5th `allow_origins` entry and `allow_origin_regex=r"https://macost.*\.vercel\.app"` to the existing `CORSMiddleware` call
- `render.yaml` - New Render Blueprint declaring the `macost-api` web service, build/start commands, health check, and 4 `sync: false` secret env vars

## Decisions Made
- Used `https://macost.vercel.app` as a best-guess canonical prod domain (per CONTEXT.md D-03/D-06) since Plan 01.1-03's actual Vercel deploy hasn't happened yet — this may need a one-line follow-up edit once the real assigned Vercel URL is known
- `render.yaml`'s `rootDir` intentionally left unset (not `backend/`) so the build context stays at repo root, matching `backend/main.py:4`'s `from backend.routers import auth, wallets` absolute import namespace (avoids RESEARCH.md Pitfall 5's `ModuleNotFoundError: No module named 'backend'`)
- All 4 Supabase/JWT env var names in `render.yaml` (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_ANON_KEY`, `SUPABASE_JWT_SECRET`) exactly match `backend/.env.example` from Plan 01.1-01, per D-09

## Deviations from Plan

None - plan executed exactly as written. Both tasks matched the `<action>` and `<acceptance_criteria>` blocks precisely; no auto-fixes, no blocking issues, no architectural questions arose.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required by this plan. Plan 01.1-03 (not yet executed) handles the actual Render/Vercel/Supabase dashboard connect steps that consume this plan's `render.yaml` and CORS changes.

## Next Phase Readiness
- `render.yaml` is ready for a zero-edit Render "New Blueprint" GitHub connect once Plan 01.1-03 runs
- `backend/main.py`'s CORS allow-list will accept the deployed Vercel frontend without further code changes, assuming the actual prod domain matches `https://macost.vercel.app` or a `macost*.vercel.app` preview subdomain — if Plan 01.1-03 reports a different canonical domain, a one-line follow-up edit to the literal `allow_origins` entry is needed
- No blockers for Plan 01.1-03

---
*Phase: 01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte*
*Completed: 2026-07-02*

## Self-Check: PASSED

- FOUND: backend/main.py
- FOUND: render.yaml
- FOUND: .planning/phases/01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte/01.1-02-SUMMARY.md
- FOUND commit: 7e1ac09
- FOUND commit: 44dfaa3
- FOUND commit: 03a446d
