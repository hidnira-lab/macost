---
phase: 01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte
plan: 1
subsystem: infra
tags: [docker, docker-compose, dockerfile, env-vars, gitignore, fastapi, nextjs]

# Dependency graph
requires:
  - phase: 01-foundation
    provides: backend/main.py (FastAPI app, /health route), backend/core/supabase.py, backend/dependencies/auth.py, apps/web/lib/api/client.ts (env var read sites)
provides:
  - Two-service docker-compose.yml (backend :8000, frontend :3000), no local Postgres container
  - backend/Dockerfile (python:3.12-slim, repo-root build context for backend. import namespace)
  - apps/web/Dockerfile (node:20-alpine, npm ci + next dev)
  - Root .dockerignore and apps/web/.dockerignore
  - backend/.env.example and apps/web/.env.example with code-verified var names (D-09)
  - apps/web/.gitignore fix (!.env.example negation) making the example file git-trackable
affects: [01.1-02, 01.1-03, phase-2-integration-testing]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Docker Compose v2 syntax (no top-level version: key)"
    - "Backend Docker build context = repo root + dockerfile: backend/Dockerfile, to preserve the backend. absolute import namespace"
    - "Frontend anonymous /app/node_modules volume after the bind mount to prevent host shadowing"
    - ".env.example var names must exactly match os.environ[...]/process.env reads, verified via direct source grep, not inferred from discussion notes"

key-files:
  created:
    - docker-compose.yml
    - backend/Dockerfile
    - apps/web/Dockerfile
    - .dockerignore
    - apps/web/.dockerignore
    - backend/.env.example
    - apps/web/.env.example
  modified:
    - apps/web/.gitignore

key-decisions:
  - "Used docker-compose.yml build.context=. + build.dockerfile=backend/Dockerfile (not build: ./backend) so backend.main:app import resolves inside the image, per D-verified Pitfall 5"
  - "Env var names in both .env.example files match D-09's code-verified corrections (SUPABASE_SERVICE_ROLE_KEY, SUPABASE_JWT_SECRET, NEXT_PUBLIC_API_BASE_URL, NEXT_PUBLIC_USE_MOCK), not CONTEXT.md's original illustrative D-04 names"

patterns-established:
  - "Pattern: any future Dockerfile/deploy config for backend/ must use repo-root build context, never backend/ itself, to preserve the backend. import namespace"
  - "Pattern: .env.example content is sourced from a direct grep of os.environ[...]/process.env reads in the consuming code, never from discussion/context notes alone"

requirements-completed: [INFRA-01, INFRA-02]

coverage:
  - id: D1
    description: "docker-compose.yml with backend (8000) + frontend (3000) services, no local Postgres container, validated via docker compose config -q"
    requirement: "INFRA-01"
    verification:
      - kind: other
        ref: "docker compose config -q (repo root) — exit 0, COMPOSE_VALID"
        status: pass
    human_judgment: true
    rationale: "docker compose up --build (full container build/run smoke test) could not be executed in this session because Docker Desktop's engine was not running/reachable from the execution shell (named pipe dockerDesktopLinuxEngine not found). Syntax/structural validation passed; a human with a running Docker Desktop must confirm docker compose up --build actually starts both services and /health responds before this is fully proven."
  - id: D2
    description: "backend/.env.example and apps/web/.env.example committed with the exact env var names backend/core/supabase.py, backend/dependencies/auth.py, and apps/web/lib/api/client.ts read; apps/web/.env.example is git-trackable after the .gitignore negation fix"
    requirement: "INFRA-02"
    verification:
      - kind: other
        ref: "grep -c SUPABASE_SERVICE_ROLE_KEY / SUPABASE_JWT_SECRET backend/.env.example (1 each); grep -c NEXT_PUBLIC_API_BASE_URL apps/web/.env.example (1); git check-ignore -q apps/web/.env.example (exit 1, TRACKABLE)"
        status: pass
    human_judgment: false

duration: 3min
completed: 2026-07-02
status: complete
---

# Phase 01.1 Plan 1: Docker Compose, Dockerfiles, and .env.example files Summary

**Two-service docker-compose.yml (FastAPI backend :8000, Next.js dev server :3000, no local Postgres) with matching Dockerfiles and code-verified .env.example files, unblocking Phase 1's register/login/wallets UAT**

## Performance

- **Duration:** 3 min
- **Started:** 2026-07-02T13:51:24Z
- **Completed:** 2026-07-02T13:54:24Z
- **Tasks:** 2
- **Files modified:** 8 (7 created, 1 modified)

## Accomplishments
- `docker compose config -q` validates cleanly with zero errors — no obsolete `version:` key, backend service uses `build.context: .` + `build.dockerfile: backend/Dockerfile` (preserving the `backend.` import namespace), frontend service includes the anonymous `/app/node_modules` volume to prevent host bind-mount shadowing
- `backend/Dockerfile` and `apps/web/Dockerfile` created matching the exact base images, COPY paths, and CMD values specified in the plan and 01.1-RESEARCH.md
- `backend/.env.example` and `apps/web/.env.example` committed with the exact env var names verified by direct grep against `backend/core/supabase.py`, `backend/dependencies/auth.py`, and `apps/web/lib/api/client.ts` (per D-09), correcting CONTEXT.md's original D-04 illustrative names
- `apps/web/.gitignore` fixed with a `!.env.example` negation so the example file is git-trackable despite the blanket `.env*` pattern

## Task Commits

Each task was committed atomically:

1. **Task 1: docker-compose.yml, both Dockerfiles, and .dockerignore files** - `f859e08` (feat)
2. **Task 2: .env.example files with corrected var names, plus apps/web/.gitignore fix** - `1324f73` (feat)

**Plan metadata:** (pending — final docs commit follows this summary)

## Files Created/Modified
- `docker-compose.yml` - Two-service Compose file (backend :8000, frontend :3000), repo-root build context for backend, anonymous node_modules volume for frontend
- `backend/Dockerfile` - python:3.12-slim, installs backend/requirements.txt, CMD runs uvicorn backend.main:app
- `apps/web/Dockerfile` - node:20-alpine, npm ci, CMD runs npm run dev
- `.dockerignore` - Root-level, mirrors root .gitignore plus .git and Dockerfile*
- `apps/web/.dockerignore` - Mirrors apps/web/.gitignore plus .git and Dockerfile*
- `backend/.env.example` - SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY, SUPABASE_JWT_SECRET, AI_VISION_API_KEY (5 lines, placeholders only)
- `apps/web/.env.example` - NEXT_PUBLIC_API_BASE_URL, NEXT_PUBLIC_USE_MOCK (no Supabase vars — apps/web never talks to Supabase directly)
- `apps/web/.gitignore` - Added `!.env.example` negation line directly after the existing `.env*` pattern

## Decisions Made
- Env var names follow D-09's code-verified corrections exactly (not CONTEXT.md's original placeholder names) — confirmed by direct grep against the three consuming source files before writing either `.env.example` file.
- Backend Docker build context is the repo root with `dockerfile: backend/Dockerfile` (not `build: ./backend`), matching the `from backend.routers import auth, wallets` absolute import in `backend/main.py`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Created placeholder `backend/.env` and `apps/web/.env.local` to unblock `docker compose config -q`**
- **Found during:** Task 1 (docker-compose.yml verification)
- **Issue:** Task 1's automated verify step (`docker compose config -q`) failed with `env file ... apps/web/.env.local not found` because Compose validates that `env_file:`-referenced paths exist on disk, and neither `backend/.env` nor `apps/web/.env.local` exist yet (they are gitignored and only ever created locally by a developer copying from `.env.example`).
- **Fix:** Created `backend/.env` and `apps/web/.env.local` locally with the same placeholder content the plan's own overall `<verification>` step 2 instructs ("Copy backend/.env.example to backend/.env and apps/web/.env.example to apps/web/.env.local — placeholder values are sufficient"). Confirmed both paths are gitignored (`.gitignore:11:.env`, `apps/web/.gitignore:34:.env*`) before creating them, so they are never staged or committed.
- **Files modified:** `backend/.env` (untracked, gitignored), `apps/web/.env.local` (untracked, gitignored) — not part of any commit
- **Verification:** `docker compose config -q && echo COMPOSE_VALID` passed after creating the files; `git status --short` confirms neither file appears (correctly gitignored)
- **Committed in:** N/A — intentionally not committed (gitignored placeholder files, matching the plan's own instructed verification setup)

---

**Total deviations:** 1 auto-fixed (1 blocking)
**Impact on plan:** Necessary to satisfy Task 1's own acceptance criteria (`docker compose config -q exits 0`); no scope creep — the fix used exactly the placeholder content the plan's overall verification section already specifies, and no gitignored file was committed.

## Issues Encountered

**Docker Desktop engine unreachable in this execution session.** After both tasks passed their `docker compose config -q` structural validation, the plan's overall `<verification>` steps 3-6 (`docker compose up -d --build`, curl health checks on both services, `docker compose down`) could not be executed: `docker info`/`docker compose up` failed with `open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified` — Docker Desktop's backend engine was not running/reachable from this worktree's shell, even with the sandbox disabled for the check. This is a host-machine state issue, not a defect in any file created by this plan. `docker-compose.yml`, both Dockerfiles, and both `.dockerignore` files are syntactically and structurally correct per every automated check that could run (`docker compose config -q`, path/CMD acceptance criteria). A human with Docker Desktop actually running should run `docker compose up -d --build` followed by `curl -sf http://localhost:8000/health` and `curl -sf -I http://localhost:3000` to close out the full functional verification loop before Phase 1's UAT is considered unblocked.

## User Setup Required

None — no external service configuration required by this plan (Vercel/Render/Supabase deployment wiring and UptimeRobot are handled in the other 01.1 plans). Locally: a developer must start Docker Desktop, then run `docker compose up --build` after copying `backend/.env.example` → `backend/.env` and `apps/web/.env.example` → `apps/web/.env.local` with real Supabase credentials (per D-05, shared manually via chat/DM).

## Next Phase Readiness
- `docker-compose.yml`, both Dockerfiles, and both `.env.example` files are committed and structurally valid — the root cause of the Phase 1 UAT block (missing Compose file, missing env var documentation) is resolved at the file level.
- Full functional smoke test (`docker compose up --build` + health check curls) is deferred to a session with Docker Desktop's engine actually running — flagged above under Issues Encountered, not a blocker for this plan's own task-level acceptance criteria.
- Plans 01.1-02 and 01.1-03 (Vercel/Render/Supabase deployment wiring, UptimeRobot) can proceed independently of this open functional-test item.

---
*Phase: 01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte*
*Completed: 2026-07-02*
