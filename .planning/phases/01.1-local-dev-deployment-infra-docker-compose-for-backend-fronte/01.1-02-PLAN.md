---
phase: 01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - render.yaml
autonomous: true
requirements:
  - INFRA-04
  - INFRA-06

must_haves:
  truths:
    - "Backend CORS allows requests from the Vercel-deployed frontend origin (a literal best-guess prod domain plus a scoped *.vercel.app regex for previews) while every pre-existing Tauri/localhost origin still works"
    - "render.yaml declares buildCommand/startCommand/healthCheckPath that exactly match backend/main.py's real import structure (backend.main:app) and existing /health route, so a fresh Render Blueprint connect deploys without a ModuleNotFoundError"
    - "No secret value is hardcoded anywhere in render.yaml — every Supabase/JWT env var uses sync: false"
  artifacts:
    - backend/main.py
    - render.yaml
  key_links:
    - "render.yaml startCommand runs `uvicorn backend.main:app --host 0.0.0.0 --port $PORT` with rootDir left unset (repo root), matching backend/main.py:4's `from backend.routers import auth, wallets` absolute import"
    - "CORSMiddleware allow_origins list keeps all 4 pre-existing Tauri/localhost origins and gains a 5th Vercel origin plus allow_origin_regex; registration stays before app.include_router calls"
---

<objective>
Update `backend/main.py`'s CORS allow-list to accept requests from the deployed Vercel frontend, and create a git-tracked `render.yaml` Blueprint so Render can deploy `backend/` correctly on first connect.

Purpose: Without this, the deployed Vercel frontend will get blocked by CORS on every API call (RESEARCH.md Pitfall 6), and a fresh Render deploy will crash with `ModuleNotFoundError: No module named 'backend'` if the build context/start command don't match the existing `backend.` import namespace (RESEARCH.md Pitfall 5).

Output: `backend/main.py` accepts the production Vercel origin and any `*.vercel.app` preview URL; `render.yaml` at the repo root is ready for Render's "New Blueprint" GitHub connect flow with `sync: false` secrets and a dynamic `$PORT` binding.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte/01.1-CONTEXT.md
@.planning/phases/01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte/01.1-RESEARCH.md
@.planning/phases/01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte/01.1-PATTERNS.md
@backend/main.py
@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Vercel origin to backend CORS</name>
  <files>backend/main.py</files>

  <read_first>
    backend/main.py — the existing CORSMiddleware block, lines 8-20 (allow_origins list of 4 entries, allow_credentials=True, allow_methods=["*"], allow_headers=["*"]); note the comment on line 8 that CORS must be registered before routes, and that `app.include_router` calls happen at lines 22-23
    01.1-RESEARCH.md — "Pitfall 6: Backend CORS allow-list doesn't include the Vercel origin" and "## Code Examples" → "Updated CORS config"
    01.1-PATTERNS.md — "backend/main.py (edit — CORS update, INFRA-06)" section for the exact required edit
  </read_first>

  <action>
    In the existing `app.add_middleware(CORSMiddleware, ...)` call at backend/main.py (currently lines 9-20), keep all 4 existing `allow_origins` entries ("tauri://localhost", "https://tauri.localhost", "http://localhost", "http://localhost:3000") exactly as they are, and append a 5th literal entry: "https://macost.vercel.app" (best-guess canonical prod domain, per D-03/D-06 — this may need a follow-up one-line edit once Plan 01.1-03's checkpoint reports back the actual assigned Vercel URL). Add a new keyword argument to the same `CORSMiddleware` call: `allow_origin_regex=r"https://macost.*\.vercel\.app"` — this covers Vercel preview-deployment URLs (e.g. `https://macost-git-branch-team.vercel.app`) that the literal list can't enumerate in advance. Do not change `allow_credentials`, `allow_methods`, or `allow_headers`. Do not move the middleware registration relative to the `app.include_router` calls — it must stay before them, per the existing comment on line 8.
  </action>

  <acceptance_criteria>
    backend/main.py's CORSMiddleware call contains exactly 5 `allow_origins` entries, including "https://macost.vercel.app"
    backend/main.py's CORSMiddleware call contains an `allow_origin_regex` keyword argument set to a pattern matching `https://macost.*\.vercel\.app`
    The 4 pre-existing origins ("tauri://localhost", "https://tauri.localhost", "http://localhost", "http://localhost:3000") remain present, unmodified
    `app.add_middleware(CORSMiddleware, ...)` still appears before both `app.include_router(...)` calls
    `python -c "import ast; ast.parse(open('backend/main.py').read())"` exits 0 (valid Python syntax)
  </acceptance_criteria>

  <verify>
    <automated>cd "C:/Users/hiday/WebstormProjects/Zephyra/macost" && python -c "import ast; ast.parse(open('backend/main.py').read()); print('SYNTAX_OK')" && grep -c "macost.vercel.app" backend/main.py && grep -c "allow_origin_regex" backend/main.py</automated>
  </verify>

  <done>backend/main.py's CORS allow-list includes the Vercel prod origin and a scoped preview-URL regex, with every pre-existing origin intact and the middleware registration order unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: render.yaml Blueprint for the backend</name>
  <files>render.yaml</files>

  <read_first>
    backend/main.py — the existing `GET /health` route (lines 31-33) that `healthCheckPath` must target; no new endpoint is needed
    backend/requirements.txt — confirms the exact path `backend/requirements.txt` that `buildCommand` installs from
    01.1-RESEARCH.md — "Pattern 4: Render Blueprint (render.yaml) for the backend", "Anti-Patterns to Avoid" (hardcoded $PORT), "Pitfall 5" (build context vs. import namespace)
    01.1-PATTERNS.md — "render.yaml (config, root-level, new)" section for the exact structure to use
  </read_first>

  <action>
    Create `render.yaml` at the repo root with one service of `type: web`, `name: macost-api`, `runtime: python`, `plan: free`. Set `buildCommand: pip install -r backend/requirements.txt`. Set `startCommand: uvicorn backend.main:app --host 0.0.0.0 --port $PORT` — always reference `$PORT` (Render assigns this dynamically at runtime); never hardcode `8000` here, since a hardcoded port makes the deployed service unreachable even though the process itself starts. Set `healthCheckPath: /health`, wired to the already-implemented route. Do NOT set a `rootDir` key — leave it unset/default so the build context is the repo root and the `backend/requirements.txt` relative path in `buildCommand` resolves (per Pitfall 5, matching backend/main.py:4's `from backend.routers import auth, wallets` absolute import namespace). Add an `envVars` list with: one entry `key: PYTHON_VERSION`, `value: "3.12.2"`; then four entries each with `sync: false` (real values entered manually via the Render dashboard, never committed) for `key: SUPABASE_URL`, `key: SUPABASE_SERVICE_ROLE_KEY`, `key: SUPABASE_ANON_KEY`, `key: SUPABASE_JWT_SECRET` — these 4 names must match `backend/.env.example` produced in Plan 01.1-01, per D-09.
  </action>

  <acceptance_criteria>
    render.yaml's `startCommand` contains the literal `$PORT` and does not contain the literal port number 8000
    render.yaml's `envVars` list contains SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY, SUPABASE_JWT_SECRET, each paired with `sync: false`
    render.yaml has no `rootDir` key anywhere
    render.yaml's `healthCheckPath` is exactly `/health`
    render.yaml's `buildCommand` references the exact path `backend/requirements.txt`
  </acceptance_criteria>

  <verify>
    <automated>cd "C:/Users/hiday/WebstormProjects/Zephyra/macost" && grep -c "PORT" render.yaml && grep -c "sync: false" render.yaml && grep -c "healthCheckPath: /health" render.yaml && grep -c "backend/requirements.txt" render.yaml</automated>
  </verify>

  <done>render.yaml exists at the repo root, targets backend.main:app via $PORT, points healthCheckPath at the existing /health route, and never commits a secret value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (any origin) → backend API | CORSMiddleware is the single enforcement point deciding which origins may make credentialed cross-origin requests |
| Git history → Render dashboard | render.yaml is committed and public-ish (repo-visible); the real secret values it references must never appear inside it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-03 | Spoofing | backend/main.py CORSMiddleware | medium | mitigate | Explicit `allow_origins` list plus a scoped `allow_origin_regex` restricted to `macost*.vercel.app` — never a wildcard `"*"` combined with `allow_credentials=True` |
| T-01.1-07 | Information Disclosure | render.yaml | high | mitigate | Every Supabase/JWT secret env var declared with `sync: false`; real values are entered once via the Render dashboard UI in Plan 01.1-03, never committed to git |
</threat_model>

<verification>
Overall checks for this plan:

1. `python -c "import ast; ast.parse(open('backend/main.py').read())"` — syntax check passes
2. `grep -c "macost.vercel.app" backend/main.py` returns >= 1 and `grep -c "allow_origin_regex" backend/main.py` returns >= 1
3. The 4 pre-existing CORS origins are still present verbatim (manual diff review against the original block)
4. `grep -c "\$PORT" render.yaml` returns >= 1 and `grep -c "8000" render.yaml` under the `startCommand` line specifically shows no hardcoded port
5. `grep -c "sync: false" render.yaml` returns exactly 4
6. This render.yaml is only exercised for a real deploy once Plan 01.1-03's Render Blueprint connect happens
</verification>

<success_criteria>
- backend/main.py's CORS allow-list covers the deployed Vercel origin and preview URLs without breaking any existing Tauri/localhost origin
- render.yaml is ready for a zero-edit Render "New Blueprint" GitHub connect
- No secret value is committed anywhere in this plan's files
</success_criteria>

<output>
Create `.planning/phases/01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte/01.1-02-SUMMARY.md` when done
</output>
