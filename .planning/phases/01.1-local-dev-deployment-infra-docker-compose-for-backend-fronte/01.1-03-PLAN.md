---
phase: 01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte
plan: 3
type: execute
wave: 2
depends_on:
  - 01.1-01
  - 01.1-02
files_modified:
  - .claude/CLAUDE.md
autonomous: false
requirements:
  - INFRA-03
  - INFRA-04
  - INFRA-05
user_setup:
  - service: vercel
    why: "Auto-deploy apps/web's static export to a public URL on every push to main (D-03, D-06)"
    env_vars:
      - name: NEXT_PUBLIC_API_BASE_URL
        source: "Set to https://macost-api.onrender.com (the Render prod URL) in Vercel Project Settings -> Environment Variables, BEFORE the first build — it is baked into the static bundle at build time, per RESEARCH.md Pitfall 4"
      - name: NEXT_PUBLIC_USE_MOCK
        source: "Set to false in Vercel Project Settings -> Environment Variables, before the first build"
    dashboard_config:
      - task: "Import this GitHub repo as a new Vercel project"
        location: "vercel.com/new -> Import Git Repository"
      - task: "Set Root Directory to apps/web"
        location: "Vercel Project Settings -> General -> Root Directory"
  - service: render
    why: "Auto-deploy backend/ on every push to main (D-06); backend needs Supabase credentials to reach the shared project (D-07)"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL — use the SAME value as local backend/.env, per D-07 (one shared project, not separate dev/prod)"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role secret — same value as local backend/.env"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon public key — same value as local backend/.env"
      - name: SUPABASE_JWT_SECRET
        source: "Supabase Dashboard -> Project Settings -> API -> JWT Settings -> JWT Secret — same value as local backend/.env"
    dashboard_config:
      - task: "New -> Blueprint -> connect this GitHub repo (auto-detects the committed render.yaml from Plan 01.1-02)"
        location: "dashboard.render.com"
  - service: uptimerobot
    why: "Keep the Render free-tier service warm so a 30-60s cold start doesn't break Phase 2's ALLOC-01 <=2 second allocation-modal requirement (D-08)"
    dashboard_config:
      - task: "Create an HTTP(s) monitor targeting https://macost-api.onrender.com/health, interval under 15 minutes"
        location: "uptimerobot.com -> Add New Monitor"

must_haves:
  truths:
    - "Pushing a commit to main triggers an automatic Vercel deployment of apps/web's static export, reachable over HTTPS, per D-03/D-06"
    - "Pushing a commit to main triggers an automatic Render deployment of backend/, and GET /health responds 200 on the live prod URL, per D-06"
    - "The deployed Vercel origin can call the Render API without a browser CORS error — this proves Plan 01.1-02's allow_origins/allow_origin_regex actually matches the real assigned domain"
    - "Render stays warm via an UptimeRobot monitor pinging /health at an interval under 15 minutes, per D-08"
    - "Local backend/.env and Render's dashboard env vars point at the same Supabase project, per D-07"
  artifacts:
    - .claude/CLAUDE.md
  key_links:
    - "Vercel dashboard Root Directory=apps/web + GitHub repo connection = auto-deploy on push to main"
    - "Render Blueprint connect reads the committed render.yaml from the repo root; the 4 sync:false vars are filled once via the dashboard with the same values as local backend/.env"
    - "UptimeRobot monitor URL (https://macost-api.onrender.com/health) matches render.yaml's healthCheckPath exactly"
---

<objective>
Connect the repo to Vercel and Render so both auto-deploy on push to main (D-06), stand up the UptimeRobot keep-alive monitor (D-08), and document the new Vercel/Render/docker-compose local-dev setup in `.claude/CLAUDE.md` — closing out the phase goal: a team member can run `docker compose up`, get backend+frontend running locally against the shared Supabase project, and both Vercel and Render auto-deploy on push to main.

Purpose: Plans 01.1-01 and 01.1-02 produced every config file (docker-compose.yml, both Dockerfiles, render.yaml, the corrected CORS allow-list) needed for this — what remains is account-level dashboard work that requires an authenticated human session neither the CLI nor the API in this environment has credentials for, plus a documentation update reflecting the new deploy targets.

Output: A live Vercel deployment of apps/web, a live Render deployment of backend/, an active UptimeRobot monitor, and `.claude/CLAUDE.md` updated to reflect all three.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte/01.1-CONTEXT.md
@.planning/phases/01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte/01.1-RESEARCH.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Vercel + Render + docker-compose in .claude/CLAUDE.md</name>
  <files>.claude/CLAUDE.md</files>

  <read_first>
    .claude/CLAUDE.md — "## Platform Requirements" section (currently lists only Render for backend and Tauri Android APK/PWA for frontend) and "## Configuration" section (currently has a placeholder line "No Docker Compose file found in root (referenced in CLAUDE.md but not yet created)")
    01.1-CONTEXT.md — canonical_refs note: "this phase ADDS Vercel as a new frontend deploy target not previously documented; update this doc during/after execution"
  </read_first>

  <action>
    In `.claude/CLAUDE.md`'s `## Platform Requirements` section, update the "Frontend/Mobile" line so it also names Vercel as the primary web deploy target (apps/web's static export, auto-deploys on push to main per D-06), keeping the existing Tauri Android APK / PWA fallback wording intact. Keep the existing "Backend: Render.com (FastAPI via Uvicorn)" line as-is, but note it auto-deploys on push to main per D-06 (no manual approval step, no staging environment).

    In `.claude/CLAUDE.md`'s `## Configuration` section, replace the placeholder line "No Docker Compose file found in root (referenced in CLAUDE.md but not yet created)" with a line stating local dev now runs via `docker compose up` from the repo root (two services: backend on :8000, frontend on :3000; no local Postgres — Supabase stays hosted-only per D-02).
  </action>

  <acceptance_criteria>
    .claude/CLAUDE.md contains the string "Vercel" within the Platform Requirements section
    .claude/CLAUDE.md still contains the string "Render.com" for the backend deploy target
    .claude/CLAUDE.md no longer contains the string "No Docker Compose file found"
    .claude/CLAUDE.md's Configuration section mentions `docker compose up`
  </acceptance_criteria>

  <verify>
    <automated>cd "C:/Users/hiday/WebstormProjects/Zephyra/macost" && grep -c "Vercel" .claude/CLAUDE.md && grep -c "Render.com" .claude/CLAUDE.md && ( grep -c "No Docker Compose file found" .claude/CLAUDE.md | grep -qx 0 && echo PLACEHOLDER_REMOVED ) && grep -c "docker compose up" .claude/CLAUDE.md</automated>
  </verify>

  <done>.claude/CLAUDE.md accurately documents Vercel (frontend), Render (backend), and docker-compose (local dev) as this project's actual deployment and local-dev setup.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Connect Vercel, Render, and UptimeRobot dashboards</name>

  <read_first>
    01.1-RESEARCH.md — "Pitfall 4: NEXT_PUBLIC_* vars are frozen at build time, not read at runtime" (set Vercel env vars BEFORE the first build); "Pitfall 7: Render free-tier cold starts break demo timing expectations" (why UptimeRobot matters)
    01.1-CONTEXT.md — D-05 (real secrets shared manually via chat/DM, no secrets manager), D-06 (auto-deploy on push to main, no staging), D-07 (one shared Supabase project for local + prod), D-08 (UptimeRobot in scope)
    render.yaml (produced in Plan 01.1-02) — the exact envVars list Render will prompt for
  </read_first>

  <acceptance_criteria>
    Vercel project exists, Root Directory=apps/web, both NEXT_PUBLIC_* env vars set before the first build, and the deployment is reachable over HTTPS
    Render service "macost-api" exists (via Blueprint connect reading render.yaml), all 4 sync:false env vars filled with the SAME values as local backend/.env per D-07, and GET /health returns 200 on the live URL
    UptimeRobot monitor exists targeting https://macost-api.onrender.com/health at an interval under 15 minutes
    The live Vercel origin can call the live Render API without a browser CORS error
  </acceptance_criteria>

  <action>Connect the repo to Vercel and Render, and stand up the UptimeRobot keep-alive monitor — none of these three can be completed via CLI/API in this environment: all three require an authenticated browser dashboard session (GitHub OAuth connect for Vercel/Render; account signup for UptimeRobot) that no stored credentials exist for here.</action>
  <instructions>
    Plans 01.1-01 and 01.1-02 already committed docker-compose.yml, both Dockerfiles, render.yaml, and the CORS update — the repo is ready to connect. Do these 3 things:

    1. Vercel: go to vercel.com/new, import this GitHub repo, set Root Directory to apps/web. Before the first deploy runs, add environment variables NEXT_PUBLIC_API_BASE_URL=https://macost-api.onrender.com and NEXT_PUBLIC_USE_MOCK=false (Vercel Project Settings -> Environment Variables) — per RESEARCH.md Pitfall 4 these are baked in at build time, so set them before the first build, not after. Vercel should auto-detect Next.js and use the committed output:'export' config from apps/web/next.config.ts.

    2. Render: go to dashboard.render.com, New -> Blueprint, connect this GitHub repo. Render auto-detects the committed render.yaml. When prompted for the 4 sync:false env vars (SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY, SUPABASE_JWT_SECRET), use the SAME real values you're using in local backend/.env — per D-07 this is one shared Supabase project for both local dev and prod, not two separate ones. Share these real values with Fertika/Khayyira/Zarra via chat/DM per D-05, not a secrets manager.

    3. UptimeRobot: go to uptimerobot.com, Add New Monitor, HTTP(s) type, URL = https://macost-api.onrender.com/health (only reachable after step 2 finishes deploying), interval under 15 minutes (5 min is a safe choice) — this keeps the Render free-tier service from cold-starting before Phase 2's <=2 second allocation-modal requirement.

    4. Once Vercel gives you its deployment URL (e.g. https://macost-<hash>.vercel.app, or a custom domain), tell me the exact URL — if it doesn't match the https://macost.vercel.app literal / *.vercel.app regex already in backend/main.py's CORS allow-list from Plan 01.1-02, I'll update it in a follow-up edit.
  </instructions>
  <verification>
    Once you confirm all three are connected and the first deploy finished, I'll run: `curl -sf https://macost-api.onrender.com/health` (expect {"status":"ok"}); `curl -sf -I <your-vercel-url>` (expect HTTP 200); and `curl -sf -H "Origin: <your-vercel-url>" -I https://macost-api.onrender.com/health` (expect an access-control-allow-origin header matching your Vercel origin, proving the CORS regex from Plan 01.1-02 actually matches the real domain).
  </verification>
  <resume-signal>Reply with your live Vercel URL once all three services are connected and the first deploy has finished (or describe what went wrong).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human ↔ Vercel/Render/UptimeRobot dashboards | Account-level OAuth/session boundary; only the human with dashboard access can complete these connections |
| Team chat/DM → 3 teammates | D-05's manual secret-sharing channel — the only path real Supabase/JWT values travel outside the dashboards themselves |
| Deployed Vercel origin → deployed Render API | The first real (not localhost) cross-origin request this project makes; validates Plan 01.1-02's CORS config against a live domain |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.1-05 | Denial of Service (informational) | Render free-tier cold start | low | accept | UptimeRobot keep-alive (this plan) mitigates but does not eliminate first-ever-request cold start; acceptable for an academic MVP demo per RESEARCH.md |
| T-01.1-06 | Information Disclosure | Vercel/Render dashboard secret entry, team secret sharing | medium | mitigate | Real Supabase/JWT values are typed directly into the Vercel/Render dashboard UI (never git) and shared with teammates only via direct chat/DM per D-05 — never posted in a shared/public channel |
</threat_model>

<verification>
Overall checks for this plan (executed by Claude after the human-action checkpoint resumes):

1. `curl -sf https://macost-api.onrender.com/health` returns `{"status":"ok"}`
2. `curl -sf -I <vercel-url>` returns HTTP 200
3. `curl -sf -H "Origin: <vercel-url>" -I https://macost-api.onrender.com/health` includes an `access-control-allow-origin` header matching `<vercel-url>`
4. `grep -c "Vercel" .claude/CLAUDE.md` returns >= 1
</verification>

<success_criteria>
- Vercel and Render both auto-deploy on push to main (D-06)
- Render's /health is reachable at the live prod URL and covered by an UptimeRobot monitor under a 15-minute interval (D-08)
- The live Vercel origin can call the live Render API without a CORS error
- Local dev and prod share one Supabase project (D-07)
- .claude/CLAUDE.md reflects the real deployment setup
</success_criteria>

<output>
Create `.planning/phases/01.1-local-dev-deployment-infra-docker-compose-for-backend-fronte/01.1-03-SUMMARY.md` when done
</output>
