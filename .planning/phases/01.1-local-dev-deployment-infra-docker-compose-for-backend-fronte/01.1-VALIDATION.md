---
phase: 01.1
slug: local-dev-deployment-infra-docker-compose-for-backend-fronte
status: approved
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-02
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None configured — this is an infra phase with no application business logic; "tests" here are smoke/config validation, not unit tests (confirmed via `.claude/CLAUDE.md`: no test framework exists yet in `apps/web` or `backend`) |
| **Config file** | none — smoke checks run directly via `docker compose` / `curl` / `grep` |
| **Quick run command** | `docker compose config -q` (validates Compose YAML syntax without starting containers, <5s) |
| **Full suite command** | `docker compose up -d --build` + curl smoke checks against both services (see Per-Task Verification Map) |
| **Estimated runtime** | ~30-60 seconds (build + start) for full suite; <5s for quick config validation |

---

## Sampling Rate

- **After every task commit:** Run `docker compose config -q`
- **After every plan wave:** Run `docker compose up -d --build` + curl smoke checks against both services
- **Before `/gsd-verify-work`:** Full suite must be green; manual UAT re-run of the originally-blocked Phase 1 flows (register/login/wallets) against the now-working local environment
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01.1-01 | 1 | INFRA-01 | T-01.1-01 | docker-compose.yml has no top-level `version:` key; backend service builds from repo root (not `./backend`) | smoke | `docker compose config -q && echo COMPOSE_VALID` | ❌ W0 (created by this task) | ⬜ pending |
| 01.1-01-02 | 01.1-01 | 1 | INFRA-02 | T-01.1-02 | `.env.example` uses code-verified var names (`SUPABASE_SERVICE_ROLE_KEY`, `SUPABASE_JWT_SECRET`, `NEXT_PUBLIC_API_BASE_URL`); `apps/web/.env.example` is git-trackable (gitignore negation applied) | smoke | `grep -c "SUPABASE_SERVICE_ROLE_KEY" backend/.env.example && grep -c "SUPABASE_JWT_SECRET" backend/.env.example && grep -c "NEXT_PUBLIC_API_BASE_URL" apps/web/.env.example && (git check-ignore -q apps/web/.env.example && echo STILL_IGNORED && exit 1 \|\| echo TRACKABLE)` | ❌ W0 (created by this task) | ⬜ pending |
| 01.1-02-01 | 01.1-02 | 1 | INFRA-06 | T-01.1-03 | `backend/main.py` CORS allow-list includes the Vercel origin without breaking existing localhost/Tauri origins | smoke | `python -c "import ast; ast.parse(open('backend/main.py').read()); print('SYNTAX_OK')" && grep -c "macost.vercel.app" backend/main.py && grep -c "allow_origin_regex" backend/main.py` | ✅ `backend/main.py` exists, edited in place | ⬜ pending |
| 01.1-02-02 | 01.1-02 | 1 | INFRA-04 | T-01.1-04 | `render.yaml` Blueprint declares `PORT`, marks secrets `sync: false` (not committed in plaintext), sets `/health` as the health check path, and installs from `backend/requirements.txt` | smoke | `grep -c "PORT" render.yaml && grep -c "sync: false" render.yaml && grep -c "healthCheckPath: /health" render.yaml && grep -c "backend/requirements.txt" render.yaml` | ❌ W0 (created by this task) | ⬜ pending |
| 01.1-03-01 | 01.1-03 | 2 | INFRA-05 | — | `.claude/CLAUDE.md` documents Vercel/Render/docker-compose setup, removing the stale "No Docker Compose file found" placeholder | smoke | `grep -c "Vercel" .claude/CLAUDE.md && grep -c "Render.com" .claude/CLAUDE.md && (grep -c "No Docker Compose file found" .claude/CLAUDE.md \| grep -qx 0 && echo PLACEHOLDER_REMOVED) && grep -c "docker compose up" .claude/CLAUDE.md` | ✅ `.claude/CLAUDE.md` exists, edited in place | ⬜ pending |
| 01.1-03-02 | 01.1-03 | 2 | INFRA-03, INFRA-04, INFRA-05 | — | Vercel/Render/UptimeRobot dashboard connections use authenticated human action, not stored/scripted credentials | checkpoint:human-action | N/A — requires authenticated browser session (Vercel/Render/GitHub OAuth, UptimeRobot signup); no CLI/API credentials available in this environment | ❌ W0 (dashboard-side, not a repo file) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

N/A — this is an infra phase, not application code with a test framework. The "Wave 0 gaps" are the infra artifacts themselves (`docker-compose.yml`, both `Dockerfile`s, `.env.example` files, `render.yaml`), which are created directly by Wave 1 tasks (see "File Exists" column above) rather than pre-staged by a separate test-scaffolding wave.

*Existing infrastructure (backend `/health` route, `backend/main.py`, `.claude/CLAUDE.md`) covers the "File Exists: ✅" rows above.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Vercel build succeeds and serves the static export | INFRA-03 | No local Vercel build emulator is set up in this phase; requires a live Vercel project connection | After Task 01.1-03-02 connects the Vercel dashboard, push to `main` and confirm the Vercel deployment succeeds and the deployed URL serves the app |
| Render build + start succeeds, `/health` reachable at prod URL | INFRA-04 | Requires a live Render service connection; not reproducible locally | After Task 01.1-03-02 connects the Render dashboard, push to `main` and run `curl -sf https://macost-api.onrender.com/health` |
| CORS allows the deployed Vercel origin | INFRA-06 | Depends on knowing the actual assigned Vercel domain, only known after first deploy | `curl -sf -H "Origin: https://<actual-vercel-domain>" -I http://localhost:8000/health` and confirm the `access-control-allow-origin` response header matches |
| `docker compose up` unblocks the original Phase 1 UAT flows (register/login/wallets) | phase goal | End-to-end user-flow UAT, not a single assertion | Re-run the Phase 1 UAT script against the now-running local docker-compose stack per `/gsd-verify-work 1` |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (5 of 6 tasks have automated smoke checks; the 6th is an explicit human-action checkpoint, not a silent gap)
- [x] Wave 0 covers all MISSING references (infra artifacts created by their own Wave 1 tasks; see Wave 0 Requirements note)
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-07-02
