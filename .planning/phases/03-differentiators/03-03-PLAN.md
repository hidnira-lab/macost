---
phase: 03-differentiators
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/components/QuickAccessPanel.tsx
  - apps/web/app/home/page.tsx
autonomous: false
requirements: [QAP-01]

must_haves:
  truths:
    - "A logged-in user landing on Home sees exactly 4 shortcuts at the top of the page: add transaction, scan receipt, top active goal (name + progress %), balance summary"
    - "Each of the 4 shortcuts navigates to its target in one tap"
    - "The top-goal shortcut shows the SAW rank-1 goal, not an arbitrary goal"
    - "QAP reuses Home's existing dashboard/goals fetches — no new API endpoint is added"
    - "When there is no active goal, the top-goal shortcut shows 'Belum ada goal' and routes to goal creation instead of rendering an empty progress bar"
  artifacts:
    - path: "apps/web/components/QuickAccessPanel.tsx"
      provides: "4-shortcut panel component taking goals/remainingBudget as props, no internal fetch"
  key_links:
    - "apps/web/app/home/page.tsx -> apps/web/components/QuickAccessPanel.tsx: mounted at the top of the page body, passed goals + remainingBudget already in HomePage's existing useState"
    - "QuickAccessPanel's scan-receipt shortcut -> /transactions/scan (route built in 03-05-PLAN.md — QAP links to it even though 03-05 has not executed yet in this wave; Next.js router.push to a not-yet-existing route is safe, it 404s gracefully until 03-05 ships)"
</must_haves>
---

<objective>
Build the Quick Access Panel (QAP-01) — a new component mounted at the top of the existing, already-API-wired `apps/web/app/home/page.tsx`, giving the user exactly 4 one-tap shortcuts (add transaction, scan receipt, top active goal, balance summary) without adding any new backend endpoint.

This is a pure frontend composition task per RESEARCH.md's Architectural Responsibility Map ("Quick Access Panel (QAP): Browser / Client (Next.js) only — pure composition over already-fetched dashboard/goals data on the Home page; no new endpoint"). D-06 locks the shape: reuse Home's existing `dashboard` + `goals` fetches, do not fetch anything new inside `QuickAccessPanel` itself.

Purpose: Directly targets the 47%-friction-churn hypothesis from the PRD by surfacing the app's most common actions one tap away from login, without adding page-load cost (zero new network calls).
Output: `apps/web/components/QuickAccessPanel.tsx` mounted at the top of `apps/web/app/home/page.tsx`.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-differentiators/03-UI-SPEC.md
@.planning/phases/03-differentiators/03-PATTERNS.md
@apps/web/app/home/page.tsx
@apps/web/lib/api/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build QuickAccessPanel.tsx as a pure-props component</name>
  <files>apps/web/components/QuickAccessPanel.tsx</files>
  <action>
    Create `apps/web/components/QuickAccessPanel.tsx` as a client component (`'use client'`) accepting props `{ goals: Goal[]; remainingBudget: number }` (types imported from `@/lib/api/types`) plus a `router` obtained internally via `useRouter()` from `next/navigation` (matching `home/page.tsx`'s existing pattern) — do NOT accept `router` as a prop, call the hook directly inside this component like every other page/component in the codebase does.

    Render exactly 4 shortcuts in a responsive grid (2x2 on mobile, matching the existing "Quick Actions (bento)" `grid-cols-2` pattern in `home/page.tsx` lines 230-258 — reuse that exact button shell/gradient/shadow styling as the base, per 03-PATTERNS.md's `QuickAccessPanel.tsx` section):
    1. **Add transaction** — reuse the existing filled-blue-gradient button verbatim (`router.push('/transactions')`, `Plus` icon from `lucide-react`, label "Tambah" / "Transaksi").
    2. **Scan receipt** — same shell as the Home page's currently-`disabled` "Segera hadir" scan button (lines 245-257), but **enabled** this time: `router.push('/transactions/scan')`, `ScanLine` icon, label "Scan" / "Struk". Remove the `disabled` attribute and the "Segera hadir" title tooltip — this feature now exists (built in 03-05).
    3. **Top active goal** — derive the SAW rank-1 goal from the `goals` prop via `goals.find(g => g.rank === 1)` (goals are already SAW-ranked server-side, per `GET /api/goals`'s `rank` field — do not re-sort or re-rank in this component, per RESEARCH.md's "no second SAW implementation" rule). If found, render its `nama_goal` + `progress_pct`, accent-orange progress-fill styling (per UI-SPEC.md "QAP top active goal tile progress fill" = accent orange `#ff8929`), tapping navigates to `/goals/${goal.id_goal}`. If no goal exists (`goals.length === 0` or no rank-1 found), render the locked copy "Belum ada goal" (UI-SPEC.md's exact string) and tapping navigates to the goal-creation route (`/goals/new` — verify this exact path against the existing Goals area's create-goal route before hardcoding; grep `apps/web/app/goals/` for the actual folder name).
    4. **Balance summary** — render `remainingBudget` in the Bricolage Grotesque hero-stat treatment (`font-display`, matching `home/page.tsx`'s existing "Sisa Anggaran" `text-[32px] font-extrabold` styling), tapping navigates to `/dashboard`.

    Every icon-only or compact tap target must have a minimum 44x44px hit area and an `aria-label` matching its text-equivalent action, per UI-SPEC.md's inherited Phase 2 accessibility rule. Use `lucide-react` icons (`Plus`, `ScanLine`, `Target` or similar for the goal tile, `Wallet` or similar for balance) — this phase standardizes on `lucide-react` per UI-SPEC.md's Design System table.
  </action>
  <verify>
    <automated>cd "D:\COLLEGE\UII S4\ZEPHYRA\CODE\macost\apps\web" && npx tsc --noEmit 2>&1 | grep -i "QuickAccessPanel" | grep -v "^$" ; echo "exit: $?"</automated>
  </verify>
  <done>QuickAccessPanel.tsx exists, compiles cleanly under `tsc --noEmit` (no type errors referencing this file), exports a default component accepting exactly `{ goals, remainingBudget }` as props with zero internal API calls.</done>
</task>

<task type="auto">
  <name>Task 2: Mount QuickAccessPanel at the top of Home and remove the now-superseded disabled scan button</name>
  <files>apps/web/app/home/page.tsx</files>
  <action>
    In `apps/web/app/home/page.tsx`, import `QuickAccessPanel` from `@/components/QuickAccessPanel` and mount it as the very first element inside the page body's main content area — above the existing "Sisa Anggaran" `<section>` (currently the first content section after the top app bar/error banner), passing `goals={goals}` and `remainingBudget={remainingBudget}` from the component's existing `useState` (both already populated by the existing `loadHome()` fetch — no new state, no new fetch).

    Remove the now-redundant standalone "Quick Actions (bento)" `<section>` (lines 230-258 in the current file: the 2-button "Tambah Transaksi" + disabled "Scan Struk" grid) — its functionality is now fully superseded by `QuickAccessPanel`, which already includes an "Add transaction" shortcut. Do not leave two separate add-transaction entry points competing for the same visual slot; the existing FAB (floating action button at the bottom, lines 357-364) stays unchanged as a secondary always-visible add-transaction affordance, per the current design.

    Confirm no other page references the removed bento section's specific button markup (grep `apps/web` for any test/reference expecting that exact `disabled`+"Segera hadir" scan button before deleting it).
  </action>
  <verify>
    <automated>cd "D:\COLLEGE\UII S4\ZEPHYRA\CODE\macost\apps\web" && npx tsc --noEmit 2>&1 | grep -i "home/page" ; grep -c "QuickAccessPanel" app/home/page.tsx</automated>
  </verify>
  <done>home/page.tsx imports and mounts QuickAccessPanel above the "Sisa Anggaran" section, the old disabled bento scan button is removed, `tsc --noEmit` shows no new errors in this file.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual verification of QAP's 4 shortcuts on Home</name>
  <what-built>QuickAccessPanel mounted at the top of Home with 4 shortcuts: add transaction, scan receipt (routes to /transactions/scan — will 404 until 03-05 ships, which is expected at this point in the wave sequence), top active goal (SAW rank-1, or "Belum ada goal" empty state), and balance summary.</what-built>
  <how-to-verify>
    1. Run `npm run dev` in `apps/web` (or use the existing dev server), log in, and land on `/home`.
    2. Confirm exactly 4 shortcut tiles appear at the top of the page, above "Sisa Anggaran" — not 3, not 5.
    3. Tap "Tambah Transaksi" — confirm it navigates to `/transactions`.
    4. Tap "Scan Struk" — confirm it attempts to navigate to `/transactions/scan` (a 404/blank page is EXPECTED right now since 03-05 hasn't executed yet in this wave; re-verify this specific tap after 03-05 ships).
    5. Tap the top-goal tile — if you have an active goal, confirm it shows the correct SAW rank-1 goal's name + progress % (cross-check against `/goals` page's rank-1 entry) and navigates to that goal's detail page. If you have no goals, confirm it shows "Belum ada goal" and routes to goal creation.
    6. Tap the balance summary tile — confirm it navigates to `/dashboard` and the displayed figure matches the dashboard's total balance.
    7. Confirm every tile's tap target feels comfortably thumb-reachable (no tiny hitboxes) and the old "Segera hadir" disabled scan button from the previous bento section is gone (not duplicated with the new QAP scan shortcut).
  </how-to-verify>
  <resume-signal>Type "approved" once all 4 shortcuts render and navigate correctly (accounting for the expected /transactions/scan 404 until 03-05 ships), or describe what's wrong.</resume-signal>
  <files>none (manual browser verification of Task 1/2's output; creates no new repo files)</files>
  <action>Run the app, log in, land on /home, and step through the 7 checks in &lt;how-to-verify&gt; above.</action>
  <verify>
    <automated>MISSING — this is a manual visual/interaction check with no CLI/API path; verification is the human confirmation described in &lt;how-to-verify&gt;.</automated>
  </verify>
  <done>All 4 QAP shortcuts render and navigate correctly per the 7 checks above (accounting for the expected /transactions/scan 404 until 03-05 ships).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan adds zero new backend endpoints and zero new data-fetch paths — it is pure client-side composition over data Home already fetches through existing, already-threat-modeled Phase 2 endpoints (GET /api/dashboard, GET /api/goals) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-06 | — | QuickAccessPanel | low | accept | No new trust boundary is introduced; QAP only renders props already sourced from authenticated, already-scoped API responses. Rationale: any threat surface here (e.g. XSS via goal name rendering) is identical to and already covered by the existing Home page's rendering of the same `goals`/`dashboard` data, not a new risk introduced by this plan. |

</threat_model>

<verification>
- `npx tsc --noEmit` in `apps/web` shows no new type errors in QuickAccessPanel.tsx or home/page.tsx
- Manual checkpoint (Task 3) confirms all 4 shortcuts render and navigate correctly
</verification>

<success_criteria>
- QAP-01 fully satisfied: exactly 4 shortcuts, SAW rank-1 goal used (not an arbitrary goal), zero new API calls, "Belum ada goal" empty state handled
- No regression to Home page's existing dashboard/goals/transactions rendering
</success_criteria>

<output>
Create `.planning/phases/03-differentiators/03-03-SUMMARY.md` when done
</output>
