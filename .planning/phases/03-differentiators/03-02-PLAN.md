---
phase: 03-differentiators
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - API_CONTRACT.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/03-differentiators/03-CONTEXT.md
autonomous: false
requirements: [AIINS-02, SAW-04]
user_setup:
  - service: railway
    why: "AI_VISION_API_KEY is a NEW backend env var required by all three Gemini-backed endpoints (scan-receipt, upload-statement, ai-insight) — per CLAUDE.md platform-ownership rule, Hidayat is the sole Railway account holder and this task must not block Fertika (backend logic develops against 03-01's mocked client) or Zarra/Khayyira (frontend develops against mocks)"
    env_vars:
      - name: AI_VISION_API_KEY
        source: "Google AI Studio (https://aistudio.google.com/apikey) — create a free-tier API key, then set it in Railway Dashboard -> macost-production service -> Variables"
    dashboard_config:
      - task: "Add AI_VISION_API_KEY to Railway service Variables, redeploy, and confirm the backend process starts without an env var error"
        location: "Railway Dashboard -> macost-production service -> Variables tab"

must_haves:
  truths:
    - "All 4 team members have explicitly agreed to the API_CONTRACT.md change adding action_verb and related_category_id to GET /api/ai-insight before any router implements the new shape (03-07 depends on this)"
    - "API_CONTRACT.md's GET /api/ai-insight section reflects the extended response shape verbatim as agreed"
    - "REQUIREMENTS.md's SAW-04 text reads '±0.002' tolerance, matching the already-implemented backend validator in goal_settings.py"
    - "AI_VISION_API_KEY exists as a real (non-placeholder) value in the live Railway backend deployment, unblocking end-to-end testing of 03-05/03-06/03-07 against the real Gemini API"
  artifacts:
    - path: "API_CONTRACT.md"
      provides: "GET /api/ai-insight §9 updated with action_verb (enum) and related_category_id (nullable) fields per the team-approved 03-CONTRACT-CHANGE-PROPOSAL.md"
    - path: ".planning/REQUIREMENTS.md"
      provides: "SAW-04 requirement text updated from ±0.001 to ±0.002 tolerance"
  key_links:
    - "03-CONTRACT-CHANGE-PROPOSAL.md -> API_CONTRACT.md: draft proposal applied verbatim only after team sign-off, not silently"
    - "API_CONTRACT.md's updated GET /api/ai-insight shape -> 03-07-PLAN.md's InsightItem model and ai_insight router: 03-07 implements exactly what this plan locks here"
</must_haves>
---

<objective>
Resolve the two MANDATORY prerequisite follow-ups flagged in 03-CONTEXT.md's `<deferred>` section before any AI-insight implementation work begins, and provision the real `AI_VISION_API_KEY` on Railway so 03-05/03-06/03-07 can be tested end-to-end against the live Gemini API instead of only against 03-01's mocked client.

This plan has two independent halves that can proceed in any order within the plan: (1) a **team sign-off checkpoint** for the `API_CONTRACT.md` change proposed in `03-CONTRACT-CHANGE-PROPOSAL.md` — per CLAUDE.md's hard rule, "Setiap perubahan shape endpoint di API_CONTRACT.md harus dikomunikasikan ke 4 anggota tim sebelum diimplementasikan" — plus the `.planning/REQUIREMENTS.md` SAW-04 tolerance text fix; and (2) a **Hidayat-only Railway env var task** that must NOT block Fertika (backend logic already develops against 03-01's mocked Gemini client) or Zarra/Khayyira (frontend develops against mocks per the established Phase 1/2 pattern).

Purpose: Unblock 03-07 (AI Financial Insights) from silently implementing an un-agreed contract shape, and unblock live (non-mocked) verification of all three Gemini-backed features.
Output: Team-approved `API_CONTRACT.md` §9 extension, corrected `REQUIREMENTS.md` SAW-04 text, `03-CONTEXT.md` deferred-items marked resolved, and a live `AI_VISION_API_KEY` on Railway.
</objective>

<execution_context>
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/hiday/WebstormProjects/Zephyra/macost/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@API_CONTRACT.md
@.planning/phases/03-differentiators/03-CONTRACT-CHANGE-PROPOSAL.md
@.planning/phases/03-differentiators/03-CONTEXT.md
@docs/PANDUAN_TEKNIKAL_TIM.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Team sign-off on API_CONTRACT.md ai-insight extension + SAW-04 tolerance text fix</name>
  <decision>Approve the API_CONTRACT.md change extending GET /api/ai-insight with action_verb + related_category_id (03-CONTRACT-CHANGE-PROPOSAL.md Perubahan #1), and the REQUIREMENTS.md SAW-04 tolerance text fix (Perubahan #2)</decision>
  <context>
    CLAUDE.md locks a hard rule: any change to an endpoint's request/response shape in API_CONTRACT.md must be communicated to and agreed by all 4 team members (Hidayat, Fertika, Khayyira, Zarra) before implementation. 03-CONTEXT.md's D-03 decision requires extending GET /api/ai-insight to add a required `action_verb` field (enum: Alokasikan/Kurangi/Pertimbangkan) and a nullable `related_category_id` field, because AIINS-02 requires every insight to link to a goal OR a category, and the current contract only supports linking to a goal. Without this change, 03-07 cannot satisfy AIINS-02. Separately, D-05 requires updating SAW-04's requirement text from "±0.001" to "±0.002" tolerance, because the locked n=62 research default weights sum to 0.999 (not 1.0 exactly) and would fail a ±0.001 check — this is already how `backend/models/goal_settings.py`'s validator is implemented (Phase 2), so this is a documentation correction, not a new decision.
  </context>
  <options>
    <option id="approve-both">
      <name>Approve both changes as drafted in 03-CONTRACT-CHANGE-PROPOSAL.md</name>
      <pros>Unblocks 03-07 immediately with the exact shape already reasoned through in CONTEXT.md/RESEARCH.md/UI-SPEC.md; SAW-04 text fix simply documents an already-shipped Phase 2 backend behavior — zero new backend risk</pros>
      <cons>Requires an actual conversation with Fertika, Khayyira, and Zarra before this checkpoint can be marked resolved — cannot be rubber-stamped solo without violating the CLAUDE.md rule this checkpoint exists to enforce</cons>
    </option>
    <option id="modify-and-approve">
      <name>Approve with modifications (e.g., different field name, different tolerance value)</name>
      <pros>Allows the team to course-correct the exact shape before it's implemented in 03-07</pros>
      <cons>Any modification here must be reflected back into 03-CONTEXT.md/03-RESEARCH.md/03-UI-SPEC.md's already-locked D-03/D-05 decisions and the downstream 03-07-PLAN.md's InsightItem model — higher coordination cost this close to the demo timeline</cons>
    </option>
  </options>
  <resume-signal>Type "approve-both" once all 4 team members have confirmed agreement (in person, chat, or however this team coordinates) to proceed with API_CONTRACT.md and REQUIREMENTS.md updates exactly as drafted in 03-CONTRACT-CHANGE-PROPOSAL.md, or describe the specific modification if the team wants something different.</resume-signal>
  <files>none (this task records a team decision; the actual file edits happen in Task 1)</files>
  <action>Confirm with Fertika, Khayyira, and Zarra (via whatever channel this team uses) that they agree to the API_CONTRACT.md and REQUIREMENTS.md changes drafted in 03-CONTRACT-CHANGE-PROPOSAL.md, per the options above.</action>
  <verify>
    <automated>MISSING — this is a human team-coordination decision with no CLI/API path; verification is the explicit sign-off captured in the resume-signal.</automated>
  </verify>
  <done>All 4 team members have explicitly agreed (approve-both or an approved modification) before Task 1 applies any file changes.</done>
</task>

<task type="auto">
  <name>Task 1: Apply the approved contract change to API_CONTRACT.md and REQUIREMENTS.md</name>
  <files>API_CONTRACT.md, .planning/REQUIREMENTS.md, .planning/phases/03-differentiators/03-CONTEXT.md</files>
  <action>
    Only run after Task 0 resolves to "approve-both" (or an approved modification). Edit `API_CONTRACT.md` §9 `GET /api/ai-insight`, "Response (200) — sukses" block: add `"action_verb": "Alokasikan"` (string, required, enum `Alokasikan` | `Kurangi` | `Pertimbangkan`) and `"related_category_id": null` (string UUID, nullable) to the example JSON, immediately following `"message"` and alongside the existing `"related_goal_id"` field, per 03-CONTRACT-CHANGE-PROPOSAL.md's "SESUDAH (usulan)" block verbatim. Add a short prose note directly below the JSON block documenting the three field-level rules from the proposal: `action_verb` is required with the closed 3-value enum; `related_category_id` is nullable; `related_goal_id` is nullable (clarify it was previously shown as always `"string"` but may be `null`); and the invariant that at least one of `related_goal_id`/`related_category_id` must be non-null. Do NOT touch the "Response (200) — fallback" block, which is unchanged. Also add a one-line clarifying note under `PUT /api/goal-settings`'s validation sentence (§6) that the weight-sum tolerance is ±0.002, per 03-CONTRACT-CHANGE-PROPOSAL.md's "Catatan sinkronisasi".

    Edit `.planning/REQUIREMENTS.md`'s `SAW-04` line, changing `(toleransi 0.001)` to `(toleransi 0.002)` — exact text per 03-CONTRACT-CHANGE-PROPOSAL.md Perubahan #2 "SESUDAH". Update the Traceability table's SAW-04 row status if it references the tolerance value anywhere (it currently does not — only status "Pending" — leave status alone, this task only fixes the requirement description text).

    Edit `.planning/phases/03-differentiators/03-CONTEXT.md`'s `<deferred>` → "Required Follow-Ups" section: mark both bullet items (API contract extension, requirement text fix) as resolved, appending "— RESOLVED 03-02, team sign-off confirmed [date]" to each.

    Use `Edit` (scoped replacement) for all three files — never `Write` a full-file rewrite of `API_CONTRACT.md` or `.planning/REQUIREMENTS.md`, both of which contain many unrelated sections that must be preserved verbatim.
  </action>
  <verify>
    <automated>grep -c "action_verb" "API_CONTRACT.md" && grep -c "related_category_id" "API_CONTRACT.md" && grep -c "toleransi 0.002" ".planning/REQUIREMENTS.md"</automated>
  </verify>
  <done>API_CONTRACT.md §9 shows action_verb + related_category_id in the GET /api/ai-insight success response; REQUIREMENTS.md's SAW-04 line reads "toleransi 0.002"; 03-CONTEXT.md's two Required Follow-Ups are marked resolved.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: [Hidayat-only, non-blocking] Provision AI_VISION_API_KEY on Railway</name>
  <what-built>Nothing in the repo — this is an external dashboard action. 03-01-PLAN.md's `gemini_client.py` already reads `AI_VISION_API_KEY` from `os.environ` lazily, so the backend runs fine without this variable set (it simply never gets called in tests, which use the mocked client). This task provisions the REAL key so 03-05/03-06/03-07's live/manual verification steps can exercise the actual Gemini API rather than only the mocked unit-test path.</what-built>
  <how-to-verify>
    1. Go to https://aistudio.google.com/apikey and create a new API key (or reuse an existing Google AI Studio free-tier key if one already exists for this project, per CLAUDE.md's "Google AI Studio (Gemini)" AI Vision provider lock).
    2. Open the Railway Dashboard for the `macost-production` service (same project documented in STATE.md / `docs/PANDUAN_TEKNIKAL_TIM.md`).
    3. Navigate to the service's Variables tab and add `AI_VISION_API_KEY` with the key value from step 1.
    4. Trigger a redeploy (Railway usually redeploys automatically on a variable change — confirm via the Deployments tab that a new deployment succeeded).
    5. Confirm the backend is still healthy: `curl https://macost-production.up.railway.app/health` returns `{"status":"ok"}`.
    6. Also add `AI_VISION_API_KEY=<same-or-a-separate-dev-key>` to `backend/.env` (local dev, gitignored) and to `backend/.env.example` as a placeholder line (`AI_VISION_API_KEY=your_key_here`) so Fertika's local dev environment documents the requirement without leaking the real value.
  </how-to-verify>
  <resume-signal>Type "provisioned" once the Railway Variables tab shows AI_VISION_API_KEY set and the health check passes, or describe the blocker if something failed.</resume-signal>
  <files>backend/.env, backend/.env.example</files>
  <action>Create a Google AI Studio API key, add it to Railway's macost-production service Variables, redeploy, and add a placeholder line to backend/.env.example — see &lt;how-to-verify&gt; for the exact steps.</action>
  <verify>
    <automated>MISSING — this is a live Railway Dashboard action with no CLI/API path in this project; verification is the manual health-check confirmation described in &lt;how-to-verify&gt;.</automated>
  </verify>
  <done>AI_VISION_API_KEY is set in Railway's live Variables tab, the backend redeployed successfully, the health check returns 200, and backend/.env.example documents the required variable name without leaking the real value.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Team communication channel -> API_CONTRACT.md | A contract change agreed informally (chat/in-person) becomes the binding written source of truth all 4 developers implement against |
| Hidayat's Railway Dashboard session -> live production env vars | Direct write access to the production backend's secrets (AI_VISION_API_KEY joins SUPABASE_URL/SERVICE_ROLE_KEY as a live secret) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-04 | Tampering | API_CONTRACT.md drift if the contract change is implemented before sign-off | high | mitigate | Task 0 is a hard `checkpoint:decision` gate — Task 1 (the actual file edit) is sequenced to only run after Task 0 resolves, per the CLAUDE.md rule this plan exists to enforce |
| T-3-05 | Information Disclosure | AI_VISION_API_KEY committed to a tracked file instead of `.env`/Railway Variables | medium | mitigate | Task 2 explicitly adds the real value only to `backend/.env` (gitignored, confirmed via existing `.gitignore` pattern already covering `backend/.env`) and Railway Variables; `backend/.env.example` gets only a placeholder string, never the real key |

</threat_model>

<verification>
- `grep -c "action_verb" API_CONTRACT.md` >= 1 and `grep -c "related_category_id" API_CONTRACT.md` >= 1 — contract change applied
- `grep -c "toleransi 0.002" .planning/REQUIREMENTS.md` >= 1 — requirement text fixed
- Manual: `curl https://macost-production.up.railway.app/health` returns 200 after Task 2's redeploy
</verification>

<success_criteria>
- API_CONTRACT.md and REQUIREMENTS.md changes are applied ONLY after explicit team sign-off (Task 0), never silently
- AI_VISION_API_KEY is live on Railway without having blocked any other Wave 1/Wave 2 plan's progress
- 03-CONTEXT.md's two mandatory follow-ups are marked resolved, removing the ambiguity 03-RESEARCH.md's Open Question #3 flagged
</success_criteria>

<output>
Create `.planning/phases/03-differentiators/03-02-SUMMARY.md` when done
</output>
